Ocena zewnetrzna sprawdzila demo, repozytorium i paczke ZIP. Nie uruchamiala pipeline'u ani testow — wiec wszystko, co znalazla, bylo widoczne z zewnatrz. Osiem napraw, kazda z testem padajacym przed nia.

SYSTEMU NIE DALO SIE SKONFIGUROWAC Z PANELU, W KTORYM SIE GO INSTALUJE. Dzial 4 dobiera wlasciciela procesu po polach uzytkownika `mp_sw_country`, `mp_sw_langs` i `mp_sw_active`. Konto z sama rola „Handlowiec”, bez tych pol, nie bylo kandydatem dla ZADNEGO procesu — pipeline konczyl sie kodem `no_owner`, proces nie powstawal, a zgloszenie po prostu lezalo. Ustawic te pola dalo sie wylacznie przez `wp user meta update` albo wprost w bazie. Klient, ktoremu PRZECZYTAJ-MNIE.txt kaze wgrac wtyczki przez „Wtyczki -> Dodaj nowa”, nie mial jak dokonczyc konfiguracji tam, gdzie ja zaczal: system po instalacji przyjmowal zgloszenia i po cichu nie robil z nimi nic. Nazywalismy to „konfiguracja wdrozeniowa”. Z punktu widzenia kogos, kto ma to uruchomic, byla to konfiguracja bez interfejsu. Pola sa teraz na standardowym ekranie profilu uzytkownika, czyli tam, gdzie administrator i tak nadaje role.

ZERO INFRASTRUKTURY TLUMACZEN PRZY 367 TLUMACZONYCH CIAGACH. Wszystkie trzy wtyczki wolaly `load_plugin_textdomain( ..., '/languages' )`, ale katalogu `languages/` nie bylo w zadnej, pliku `.pot` nie bylo w zadnej, naglowka `Domain Path` nie bylo w zadnej. Tlumacz nie mial z czym usiasc, a WordPress szukal katalogu, ktorego nie dostarczono. Braku nie bylo widac, bo zrodla sa po polsku i `__()` oddaje wtedy ciag wejsciowy — widac go dopiero na witrynie w innym jezyku, czyli u klienta. LP.3 prowadzi korespondencje w wielu jezykach (kolumna `lang` w tabeli procesow), wiec nie bylo to wymaganie teoretyczne.

PACZKA WYSYLALA NA SERWER PRODUKCYJNY 47 PLIKOW DEWELOPERSKICH NA 101. Testy, raport z audytu, raport z debugowania, dokumentacja wewnetrzna wszystkich dzialow — wszystko ladowalo do `wp-content/plugins/`, czyli w miejsce, o ktorym ten sam PRZECZYTAJ-MNIE.txt pisze, ze jest dostepne publicznie. Cztery pliki PHP nie mialy przy tym zabezpieczenia przed bezposrednim wywolaniem, w tym dwa harnessy dzialajace bez WordPressa. Strazniki `ABSPATH` zamykaja jednak tylko PLIKI PHP: na `.md` nie robia nic, bo serwer ich nie uruchamia, tylko oddaje jako tekst. Sprawdzone zadaniem HTTP — `docs/SECURITY.md` odpowiadal 200 i 9966 bajtow typu `text/markdown`. Najgorsze, ze projekt sam to wiedzial: `mp-offer-builder/.distignore` wymienial te katalogi od poczatku, tylko nikt tego pliku nie czytal, bo paczki skladalismy recznie. Budowanie slucha teraz deklaracji wtyczki, a jej brak przerywa prace — milczenie nie moze znaczyc „wyslij wszystko”.

PIERWSZA STRONA, KTORA WIDZI KLIENT, NIE ISTNIALA W REPOZYTORIUM. `PRZECZYTAJ-MNIE.txt` przepisywany byl z paczki poprzedniego wydania skryptem zyjacym w katalogu tymczasowym. Nikt nie mogl go zrecenzowac w commicie, nikt nie mogl go poprawic bez archeologii w archiwach, a po restarcie maszyny (katalog tymczasowy jest w tmpfs) nie dalo sie go odtworzyc w ogole. Zrodla tekstow i skrypt budujacy paczki sa teraz czescia repozytorium jak reszta dostawy.

DEMO LAMALO WLASNA POLITYKE PRYWATNOSCI. Strona „Kontakt” osadzala ramke Google Maps przy kazdym wejsciu, a polityka prywatnosci deklarowala „wylacznie niezbedne pliki cookies”. Zdanie bylo nieprawdziwe na stronie, ktora je glosila — w produkcie, ktorego osia sprzedazowa jest RODO. Google Fonts i zdjecia z Unsplash tez wychodzily na zewnatrz i tez nie byly nigdzie wymienione. Mapa laduje sie teraz po kliknieciu i wskazuje adres, ktory strona podaje, zamiast ogolnego „Warszawa Centrum”.

DEKLAROWANA ZGODNOSC ROZJEZDZALA SIE Z TESTOWANA. `Tested up to` mowilo 6.6 we wtyczce 1 i 6.8 w pozostalych, a regresja chodzi na WordPressie 7.0. `README.md` nie podawal przy tym wymagan srodowiska — a wtyczka 2 wymaga PHP 8.1, podczas gdy 1 i 3 dzialaja od 7.4. Na serwerze z PHP 8.0 klient zainstalowalby 1 i 3, a 2 by odmowila: proces bez srodkowego kroku, czyli bez ofert.

Regresja: 81 plikow testowych, wszystkie PASS. PHPCS: 160 plikow, kod wyjscia 0.
