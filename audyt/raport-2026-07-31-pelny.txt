=== WYNIK AUDYTU ===

KRYTYCZNE (9):
  [1.25] Zdarzenie `offer.approved` jest mapowane wprost na status `offer_sent`, ale slownik przejsc dopuszcza `offer_sent` wylacznie z `offer_draft`, a zaden typ zdarzenia nie prowadzi do `offer_draft`. Automatyczna sciezka P1→P2→P3 nie ma wiec jak przejsc dalej niz `assigned`.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:103
        status: prawdopodobne
        skutek: Lead wpada z P1 (`lead.created`): status `new` → `assigned`.
                Handlowiec zatwierdza oferte w P2, przychodzi `offer.approved`:
                from=`assigned`, to=`offer_sent`, is_allowed() = false → K5.1 zwraca
                fail `illegal_transition` z http_status 409. Proces zostaje na
                `assigned` na zawsze: brak EFFECT_SCHEDULE_FOLLOWUPS (follow-upy
                d+3/d+7 nigdy nie powstana) i brak EFFECT_NOTIFY_CLIENT. Jedynym
                ratunkiem jest reczne `status.change` na `offer_draft`, czyli
                automatyzacja procesu nie dziala.
        naprawa: Albo dodac w target_status() mapowanie zdarzenia tworzacego wersje robocza oferty na STATUS_OFFER_DRAFT, albo dopuscic w transitions() przejscie `STATUS_ASSIGNED => STATUS_OFFER_SENT` (ew. rozwiazac to dwuetapowo w Dziale 8: assigned→offer_draft→offer_sent w jednej transakcji).

  [1.25] Zdarzenie `status.change` bez pola `to_status` (lub z pustym/bialoznakowym) trafia do tej samej galezi co `task.due`/`dashboard.view` i konczy sie sukcesem z `allowed=true`. Zadana zmiana statusu nie zachodzi, a pipeline raportuje powodzenie. Ryzyko podniesione przez to, ze `type` czytane jest z `$event['type']`, a `to_status` z zupelnie innego miejsca — plaskiego `$context->all()`.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:160
        status: prawdopodobne
        skutek: Przychodzi `status.change` z literowka w kluczu, z `to_status`
                schowanym w podtablicy koperty (`event['data']['to_status']`) albo z
                wartoscia "   ". target_status() zwraca '' → agent zwraca
                allowed=true, changes_status=false → K5.1 przepuszcza (sprawdza
                tylko `allowed`), QA5 przepuszcza (`qa_allowed` + `qa_version`
                niepuste), Dzial 8 nie ma czego zapisac. Uzytkownik/API dostaje
                odpowiedz sukcesu, choc status nie zostal zmieniony — i nie ma
                zadnego sladu bledu w dzienniku.
        naprawa: Rozdzielic dwa przypadki: dla `EVENT_STATUS_CHANGE` brak/pusty `to_status` musi konczyc sie odmowa (np. fail z kodem `missing_target_status`, http 400), a galaz `'' === $to` z `allowed=true` zostawic wylacznie dla typow zdarzen, ktore z definicji nie ruszaja statusu (`task.due`, `dashboard.view`). Dodatkowo czytac `to_status` z tego samego miejsca co `type` (z koperty `event`), a nie z plaskiego `$context->all()`.

  [1.25] Nieudane zaplanowanie przebiegu kolejki kończy się wynikiem `ok` i statusem `queue_scheduled = false`, którego nikt nie sprawdza — a wartość `false` znaczy jednocześnie „już zaplanowane" i „nie udało się zaplanować".
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:175
        status: prawdopodobne
        skutek: Zdarzenie z niepustą listą `notifications`, zapis zatwierdzony
                (`committed = true`, `writes > 0`). `wp_schedule_single_event()`
                zwraca false (np. filtr `pre_schedule_event` z wtyczki kolejkowej,
                blokada duplikatu w rdzeniu WP, nieudany zapis do `wp_options`).
                Powiadomienia zostają zapisane w bazie, ale żadne zadanie nie
                zostanie uruchomione — nikt ich nie wyśle. Wywołujący dostaje
                `ok: true`, `http_status: 200` i `notifications: N`, dział 9
                raportuje sukces, a odbiorcy HOOK_QUEUE_READY dostali już sygnał,
                że kolejka jest gotowa. Awaria jest cicha: w wyniku nie ma po niej
                śladu, bo `false` jest nieodróżnialne od normalnego „termin już
                istnieje".
        naprawa: Rozdzielić trzy stany zamiast jednego boola: `already_scheduled` / `scheduled_now` / `schedule_failed` (np. `schedule_queue()` zwraca string albo null przy błędzie, a `wp_next_scheduled()` sprawdzane osobno). W A9.1 przy `schedule_failed` zwrócić `MP_SW_Result::fail( ..., 'queue_not_scheduled' )`, a w K9.1/QA9 dodać warunek: jeśli `notifications` niepuste, to musi istnieć zaplanowany przebieg kolejki (`wp_next_scheduled( CRON_QUEUE )` niepuste). Emisję HOOK_QUEUE_READY przenieść PO udanym zaplanowaniu.

  [1.25] Payload hooka `mp_lead_created` podstawia domyslna wartosc 'checked' dla `vat_status` — brak ustalonego statusu VAT w kontekscie jest raportowany na zewnatrz jako weryfikacja wykonana pomyslnie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:96
        status: prawdopodobne
        skutek: Lead przechodzi pipeline, w ktorym krok weryfikacji NIP/VAT nie
                zapisal `vat_status` do kontekstu (usluga VIES niedostepna, krok
                pominiety przy leadzie bez NIP, wznowienie pipeline'u od pozniejszego
                dzialu). Dzial 11 i tak emituje `mp_lead_created` z
                `vat_status='checked'`. Subskrybent w pluginie 2 traktuje kontrahenta
                jako zweryfikowanego i buduje szkic oferty na tej podstawie, mimo ze
                weryfikacja nigdy sie nie odbyla — falszywy status przekracza
                granice wtyczki i nie da sie go pozniej odroznic od prawdziwej
                weryfikacji.
        naprawa: Zmienic domyslna wartosc na neutralna/negatywna (np. `$context->get( 'vat_status', 'unknown' )`) albo nie wysylac klucza, gdy kontekst go nie zawiera; wartosc 'checked' ma pochodzic wylacznie z dzialu, ktory faktycznie wykonal weryfikacje.

  [1.25] Aktor zapisany w kopercie zdarzenia pochodzi z wejścia wywołującego i nigdy nie jest porównywany z rzeczywiście zalogowanym użytkownikiem. A1.1 buduje `actor` wyłącznie z `$context->get('actor')`, A1.2 osobno wylicza `$auth['user_id'] = get_current_user_id()`, a K1.2 sprawdza tylko, że ten drugi jest niepusty — żaden krok nie wymusza ich zgodności.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-01.php
        status: prawdopodobne
        skutek: Handlowiec z uprawnieniem `mp_sw_handle_event` wysyła ręczne
                żądanie z poprawnym nonce'em i ciałem
                `{"source":"manual","type":"dashboard.view","actor":{"user_id":<id
                innego handlowca>}}`. K1.1 przepuszcza (user_id jest liczbą
                dodatnią), K1.2 przepuszcza (sesyjny `get_current_user_id()` jest
                niezerowy, uprawnienie jest), a Dział 3 dostaje w kopercie cudzego
                aktora i buduje listę procesów w jego zakresie. Wariant drugi: to
                samo żądanie z `actor.user_id = 0` przechodzi obie bramki i
                zdarzenie zostaje utrwalone jako wykonane przez „aktora
                systemowego”, mimo że wykonał je konkretny zalogowany użytkownik
                — ślad audytowy wskazuje na system.
        naprawa: Przy `source === manual` nadpisać `event.actor.user_id` wartością `get_current_user_id()` (albo dodać w K1.2 twardy warunek `(int) $data['event']['actor']['user_id'] === (int) $auth['user_id']` z odmową 403 przy rozjeździe). Aktora z wejścia akceptować tylko dla źródeł `system` i `cron`.

  [1.25] Agent 10.3 nie sprawdza, czy wp_json_encode() zwrocilo false — pole result_ready (i tym samym bramka QA10) wynika wylacznie z $r['success'], czyli z lead_id > 0, a nie z faktu udanego zakodowania payloadu. Dzial deklarowany jako 'finalizacja payloadu JSON' konczy sie sukcesem mimo nieudanej operacji kodowania.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-10.php:87
        status: prawdopodobne
        skutek: Lead zapisany poprawnie (lead_id = 41), ale w response trafia lancuch
                z niepoprawnym UTF-8 (np. imie/komentarz z formularza przekazany dalej
                w polu message lub w danych handlowca). wp_json_encode() zwraca false.
                Wtedy response_json = false, ale success = true, wiec result_ready =
                true, K10.3 przepuszcza, QA10 przepuszcza (lead_id 41 > 0) i dzial 10
                raportuje 'wynik gotowy'. Konsument pluginu, ktory czyta
                response_json, dostaje false zamiast JSON-a — pusta/nieprawidlowa
                odpowiedz HTTP przy statusie sukcesu, bez zadnego sladu bledu.
        naprawa: W 10.3 zapisac wynik do zmiennej i uzaleznic od niej status: $json = wp_json_encode( $r ); jesli false === $json, zwrocic MP_Result::fail( 'Nie udalo sie zakodowac payloadu', ..., 'json_encode_failed' ) albo co najmniej ustawic 'result_ready' => ( ! empty( $r['success'] ) && false !== $json ) i przekazac kod bledu dalej.

  [1.25] Asymetryczna ochrona kluczy: przy on_sale=true kod czyta $price_row['sale_price'] bez isset(), podczas gdy regular_price ma zabezpieczenie. Brakujaca/pusta cena promocyjna daje cicho 0 groszy, a pozycja i tak zostaje oznaczona jako 'sale'.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:64
        status: prawdopodobne
        skutek: Snapshot z Dzialu 2 zawiera price_row z on_sale=true i bez klucza
                sale_price (albo z sale_price = '' — WooCommerce zwraca pusty string
                dla nieustawionej ceny). PHP zglasza undefined key, $price_value =
                null/'', to_grosze() zwraca 0, pozycja trafia do oferty i PDF z cena
                0,00 zl przy etykiecie price_source='sale'. QA Dzialu 4 tego nie
                wychwytuje (0 jest poprawnym intem, suma sie zgadza), wiec brama jest
                zielona i klient dostaje oferte z darmowa pozycja.
        naprawa: Symetrycznie zabezpieczyc obie galezie i nie zamieniac braku ceny na 0: pobrac wartosc przez isset()/'' -> jesli cena zrodlowa jest pusta lub nienumeryczna, zwrocic MP_OB_Result::fail() z kodem np. 'missing_price' zamiast kontynuowac. Dodatkowo ustawiac price_source='regular' tylko wtedy, gdy sale_price faktycznie istnieje i jest > 0.

  [1.25] Brak ceny w snapshocie jest tlumaczony na 0 groszy zamiast na blad — dane niekompletne przechodza dalej jako poprawna wycena.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:61
        status: prawdopodobne
        skutek: Dzial 2 nie dostarczyl wiersza cen dla jednej pozycji (produkt bez
                ustawionej ceny, wariant, produkt usuniety w trakcie). $price_row =
                array(), on_sale=false, regular_price nieustawione -> unit_grosze = 0.
                Agent 4.2 policzy line_grosze = 0, subtotal bedzie zanizony o pelna
                wartosc tej pozycji, QA przepusci (suma spojna), a oferta wychodzi z
                realnie zla kwota — bez zadnego sygnalu w logu ani statusie dzialu.
        naprawa: W Agencie 4.1 rozroznic 'brak wiersza cen' od 'cena rowna zero': gdy brakuje $prices[$i] albo obu cen, zwrocic MP_OB_Result::fail( 'Brak ceny dla pozycji.', array( 'index' => $i ), 'missing_price_row' ) zamiast wpisywac 0.

  [1.26] Zadanie follow-up po terminie jest domykane jako WYKONANE takze wtedy, gdy zadne powiadomienie follow-up nie powstalo — ani w kolejce, ani na liscie pominietych. Warunek na STATUS_UNDELIVERED wymaga jednoczesnie ( ! $poszlo && $odpadlo ), wiec przypadek „nic nie wyprodukowano" wpada w galaz sukcesu.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:310
        status: prawdopodobne
        skutek: Zdarzenie EVENT_TASK_DUE dla procesu, ktory nie ma przypisanego
                handlowca (assigned_user_id = 0, przypisanie zastepcze nie zadzialalo)
                albo dla ktorego Dzial 7 z innego powodu nie zbudowal wiadomosci
                TPL_FOLLOWUP_DUE. Wtedy klucz 'notifications' nie zawiera tego
                szablonu i klucz 'skipped_notifications' rowniez nie (nie bylo czego
                pomijac). Zadanie zostaje zapisane ze statusem STATUS_DONE, wiec
                handlowiec/kierownik widzi na liscie zadan i w dzienniku aktywnosci
                wpis „wykonane" dla przypomnienia, ktorego nikt nigdy nie dostal;
                cron nie wroci po to zadanie, bo jest domkniete.
        naprawa: Odwrocic warunek: STATUS_DONE tylko gdy $poszlo === true, w kazdym innym przypadku STATUS_UNDELIVERED (albo osobny status „brak odbiorcy"), tak by galaz „nie powstalo zadne powiadomienie" nie byla raportowana czlowiekowi jako sukces.

SREDNIE (99):
  [1.16] Zapytanie do bazy wewnatrz petli (2 w jednym obiegu).
        mp-lead-intake/includes/class-mp-privacy.php:117
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 80
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (3 w jednym obiegu).
        mp-offer-builder/includes/class-mp-offer-builder-privacy.php:195
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona
                120 zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie
                to przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.25] resolve_wl() traktuje odpowiedź 200 bez pola result.subject.statusVat jako udane sprawdzenie: zwraca company_status_checked=true przy company_status=null i dodatkowo zapisuje null do transienta na 12 h.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:322
        status: potwierdzone
        skutek: NIP nieobecny w Wykazie albo odpowiedź MF z pustym `subject` (200 OK,
                `result.subject` = null): dział raportuje company_status_checked=true
                przy company_status=null, więc konsument nie odróżni 'sprawdzone,
                firma nieznana' od 'sprawdzone, status znany'. Równocześnie null
                trafia do cache na 12 h — kolejne żądania dla tego NIP-u wracają
                gałęzią cache (`false !== $cached` jest prawdą dla wartości
                pustej zapisanej przez transient) z source='cache' i checked=true, bez
                ponownego odpytania MF, także po tym jak MF zacznie zwracać poprawny
                podmiot.
        naprawa: Zapisywać do transienta i raportować checked=true tylko gdy `$status` jest niepustym stringiem; przy braku `statusVat` zwracać `company_status => null, company_status_checked => false` (dokumentacyjne 'nieustalony') i nie cache'ować, albo cache'ować krótkim TTL pod wartością wartownikową odróżnialną od realnego statusu.

  [1.25] QA Agent 6 sprawdza tożsamość, którą sam Agent 6.2 przed chwilą wyliczył (`gross = net + vat`), więc warunek FAIL jest nieosiągalny — bramka jakości działu zawsze zwraca `totals_consistent => true` niezależnie od tego, czy VAT jest prawdziwy.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-06.php:312
        status: prawdopodobne
        skutek: Dowolna z sytuacji opisanych w ustaleniach powyżej (VAT = 0 przy
                pustych `lines`, VAT liczony od zaniżonej podstawy). QA6 raportuje
                SUKCES i status `totals_consistent = true`, `MP_OB_Quality_Gate`
                akceptuje dział, a jedyna kontrola jakości Działu 6 nie wykrywa
                błędnej kwoty podatku — status nie odpowiada temu, co naprawdę
                się stało.
        naprawa: QA6 musi weryfikować niezależnie, a nie tożsamościowo: np. sprawdzić `net_grosze === subtotal_grosze - discount_total`, że przy `tax_mechanism === 'domestic'` i wszystkich stawkach > 0 zachodzi `vat_grosze > 0` dla `net_grosze > 0`, oraz przeliczyć VAT z `lines`/`tax_rates` drugim, niezależnym torem i porównać z `vat_grosze`.

  [1.25] Zabezpieczenie S4-02 przed nieznanym kodem kraju działa tylko wtedy, gdy WooCommerce jest załadowane; bez WC ta sama literówka w kodzie kraju znów dostaje ciche 0% VAT z fałszywą podstawą prawną. Dodatkowo wywołanie `WC()->countries` łamie deklarację docblocka pliku („CZYSTA FUNKCJA... zero wywołań WC_* albo wpdb”), która jest podstawą do uruchamiania działu poza kontekstem WC.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-06.php:96
        status: potwierdzone
        skutek: Klient z `country = 'DR'` (literówka zamiast 'DE'), oferta generowana
                w kontekście, w którym WC() nie jest zbootstrapowane (test
                jednostkowy działu jako czystej funkcji, WP-CLI/cron przed
                inicjalizacją WooCommerce, WooCommerce chwilowo nieaktywne). Format 2
                liter przechodzi, blok WC jest pominięty, wynik: `tax_mechanism =
                'out_of_scope'`, `tax_basis = 'Kraj spoza UE — poza zakresem
                dyrektywy VAT.'`, VAT = 0 — dokładnie ten skutek, który komentarz
                S4-02 deklaruje jako naprawiony.
        naprawa: Walidować kod kraju o statyczną listę ISO 3166-1 alpha-2 w pliku (tak jak już zrobiono z `EU_COUNTRIES`), niezależną od WooCommerce; jeśli lista referencyjna ma pochodzić z WC, brak WC musi kończyć się FAIL (`country_list_unavailable`), a nie po cichu pomijać kontrolę.

  [1.25] Ta sama wartość `$vat_valid` jest interpretowana w jednej funkcji dwiema różnymi regułami: kolumna `vat_valid` używa testu luźnego (truthy), a `vat_status` testu ścisłego (`true === $vat_valid`). Wartość prawdziwa, ale nie będąca literalnym boolem `true` (np. 1, '1', 'true' z cache/JSON), daje wiersz wewnętrznie sprzeczny: `vat_valid = 1` i jednocześnie `vat_status = 'checked'`.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:147
        status: potwierdzone
        skutek: Dział weryfikacji VAT zapisuje do kontekstu wynik odtworzony z
                cache/serializacji jako 1 lub '1' zamiast bool true. Lead trafia do BD
                z `vat_valid = 1`, ale `vat_status = 'checked'`. Zgodnie z komentarzem
                F2 wtyczka 2 rozpoznaje ważny VAT UE po `vat_status = 'valid'`, więc
                dla tego leada odwrotne obciążenie pozostaje nieosiągalne, mimo że
                VIES potwierdził numer.
        naprawa: Znormalizować `$vat_valid` raz, na wejściu (np. `$vat_valid = is_null( $v ) ? null : (bool) $v;`), i wyprowadzać z tej jednej znormalizowanej wartości zarówno kolumnę `vat_valid`, jak i `vat_status`.

  [1.25] Dedup 7.1 porzucił zapytanie do BD i reużywa `leads` z działu 1, ale klucz unikalności to (country, nip), a `country` jest normalizowany dopiero w dziale 4 — czyli PO dziale 1, a PRZED zapisem w dziale 7. Komentarz uzasadnia reużycie wyłącznie identycznością NIP-u, nie mówi nic o zgodności `country`.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:40
        status: potwierdzone
        skutek: Zgłoszenie z krajem podanym jako „pl"/„Polska" dla NIP-u, który
                ma już aktywnego leada zapisanego z „PL". Dział 1 szuka po
                nieznormalizowanym kraju i nic nie znajduje → 7.1 zwraca
                `is_duplicate = false`, `unique_ok = true`, krytyk K7.1 przepuszcza.
                7.3 robi INSERT z już znormalizowanym „PL" → naruszenie
                UNIQUE(country, nip) → agent kończy generycznym `insert_failed`
                zamiast poprawnym STOP-em na duplikacie; użytkownik dostaje błąd
                zapisu zamiast informacji o duplikacie.
        naprawa: Albo wykonać dedup po normalizacji (dział 1.1 uruchamiać po dziale 4, względnie przepuścić `country` przez tę samą normalizację przed zapytaniem), albo w 7.1 ponownie odpytać BD-3 kluczem (znormalizowany country, nip) — tak samo jak robi to już `get_archived_lead_by_nip( $nip, $country )` kilka linii niżej.

  [1.25] Gałąź `else` ustawia `vat_status = 'checked'` i wypełnia `vat_checked_at` także wtedy, gdy `vat_valid` jest `null`, czyli gdy weryfikacja nie dała żadnego rozstrzygnięcia. Wiersz twierdzi, że sprawdzenie się odbyło (data sprawdzenia + status „sprawdzony"), choć wynik nie istnieje, a status nie jest `pending`, więc weryfikator w tle go nie podejmie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:149
        status: potwierdzone
        skutek: Weryfikacja VAT kończy się bez rozstrzygnięcia i bez ustawienia
                flagi `vat_pending` (np. błąd usługi obsłużony jako „brak
                wyniku", kraj spoza UE, brak numeru). Lead zapisuje się z `vat_valid
                = NULL`, `vat_status = 'checked'`, `vat_checked_at = <teraz>`.
                Ponieważ status nie jest `pending`, weryfikator w tle nie skoryguje
                ani statusu, ani score'u (brak +30 z VIES) — lead na stałe zostaje
                z zaniżonym score'em i z fałszywą informacją, że VAT sprawdzono.
        naprawa: Rozdzielić przypadki: `null` bez flagi pending → osobny status (np. `unknown`/`pending`) i `vat_checked_at = NULL`; `'checked'` rezerwować dla rozstrzygnięcia negatywnego (`false === $vat_valid`).

  [1.25] Agent 2.3 bierze tylko PIERWSZY wiersz stawki zwrocony przez WC_Tax::get_base_tax_rates(), mimo ze API zwraca TABLICE pasujacych stawek.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:275
        status: potwierdzone
        skutek: Sklep ma dla klasy podatkowej wiecej niz jeden wiersz stawki dla kraju
                bazowego (np. stawka podstawowa + stawka zlozona/compound albo druga
                stawka lokalna). Snapshot zapisuje wylacznie pierwsza z nich,
                pozostale gina bezpowrotnie (zasada "jeden odczyt" — Dzial 6 nie
                moze juz doczytac). Oferta i dokument handlowy u klienta maja zanizony
                VAT, a wszystkie bramki QA przechodza, bo arytmetyka jest wewnetrznie
                spojna.
        naprawa: Zapisac w snapshocie cala tablice wierszy (rate/label/compound/priority/shipping) i sumowac je w Dziale 6 przez WC_Tax::calc_tax(); albo — jesli kontrakt ma zostac jednostawkowy — jawnie FAIL-owac ('multiple_tax_rates'), gdy count($found) > 1, zamiast po cichu odrzucac reszte.

  [1.25] Agent 2.3 wymaga skonfigurowanej stawki VAT rowniez dla klas podatkowych produktow zwolnionych (tax_status = 'none'), ktore i tak nigdy nie beda opodatkowane.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:249
        status: potwierdzone
        skutek: Handlowiec dodaje do oferty pozycje zwolniona z VAT (tax_status =
                'none') przypisana do klasy podatkowej, dla ktorej sklep nie ma
                zadnego wiersza stawki (typowe dla klas typu 'zero-rate' / wlasnej
                klasy dla pozycji nieopodatkowanych). Dzial 2 konczy sie FAIL
                'missing_tax_rate' i cala poprawna oferta nie powstaje, mimo ze dla
                tej pozycji stawka nigdy nie bylaby uzyta.
        naprawa: Pomijac przy budowie listy $tax_classes pozycje z tax_status === 'none' (lub zbierac ich klasy z rate = 0.0 zamiast wymagac wiersza w WC), zachowujac twardy FAIL tylko dla pozycji faktycznie opodatkowanych.

  [1.25] Agent 2.3 konczy sie statusem OK z pusta sekcja 'tax_rates', gdy w kontekscie nie ma produktow — sukces mimo braku odczytu jakiejkolwiek stawki.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:246
        status: potwierdzone
        skutek: Klucz 'products' nie trafia do kontekstu (zmiana nazwy klucza / brak
                scalenia wyniku Agenta 2.1) albo zadanie przychodzi z pusta lista
                pozycji. Caly Dzial 2 raportuje PASS i znacznik db_reads=1, a QA Agent
                2 widzi sekcje jako obecna. Dzial 6 dostaje pusta mape stawek i
                nalicza 0% VAT albo wywraca sie daleko od miejsca, w ktorym powstal
                blad.
        naprawa: Na wejsciu Agenta 2.3 (i analogicznie 2.1/2.2) FAIL-owac, gdy zrodlowa tablica jest pusta lub gdy klucz w kontekscie nie istnieje (np. 'empty_products' / 'missing_context'), zamiast zwracac ok() z pusta sekcja.

  [1.25] Agent 2.1 deklaruje filtrowanie "tylko opublikowane i mozliwe do sprzedazy", ale is_purchasable() jest wylacznie zapisywane w snapshocie — nigdy nie odrzuca pozycji.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:84
        status: potwierdzone
        skutek: Do oferty trafia produkt opublikowany, dla ktorego WooCommerce zwraca
                is_purchasable() === false (np. produkt zgrupowany/zewnetrzny albo
                produkt wylaczony ze sprzedazy przez filtr woocommerce_is_purchasable
                innej wtyczki). Agent 2.1 konczy sie PASS z komunikatem sugerujacym,
                ze sprawdzil sprzedawalnosc, i pozycja przechodzi dalej — handlowiec
                dostaje oferte na towar, ktorego nie da sie kupic.
        naprawa: Albo dolozyc warunek odrzucenia dla ! $product->is_purchasable() (z osobnym komunikatem bledu), albo zmienic opis agenta tak, by nie deklarowal kontroli, ktorej nie robi, i udokumentowac, ktory dzial faktycznie konsumuje pole 'purchasable'.

  [1.25] Walidacja limitów DDL liczy BAJTY (strlen) zamiast ZNAKÓW — inna jednostka niż varchar(N) w MySQL.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:185
        status: potwierdzone
        skutek: Klient o nazwie z polskimi znakami, np. 120 znaków, z których 40 to
                ą/ć/ę/ł/ń/ó/ś/ż/ź (w UTF-8 po 2 bajty) → strlen = 160…
                przy dłuższej nazwie firmy (np. 150 znaków, 45 diakrytyków = 195
                bajtów) warunek 195 > 191 jest prawdziwy, mimo że MySQL taką
                wartość przyjmie bez obcięcia. Agent 10.1 zwraca
                fail('ddl_violation'), cały zapis oferty jest odrzucony, a komunikat
                kłamie: mówi o „limicie 191 znaków”, choć nazwa ma ich 150.
                Analogicznie client_email z IDN i pdf_path z diakrytykami w numerze
                oferty.
        naprawa: Liczyć znaki tak, jak liczy je kolumna: mb_strlen( (string) $header[$field], 'UTF-8' ) > $max (z fallbackiem na strlen, gdy brak mbstring), albo — jeśli intencją jest bajtowy limit — poprawić komunikat na „bajtów” i udokumentować rozjazd ze schematem.

  [1.25] Ceny pozycji brane z $lines[$i] z cichym fallbackiem do 0; brak jakiejkolwiek kontroli, że tablica lines odpowiada tablicy items — plan przechodzi walidację z zerowymi kwotami.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:149
        status: potwierdzone
        skutek: Wejście: kontekst, w którym 'lines' nie jest tablicą (klucz
                nieustawiony przez wcześniejszy dział albo pod inną nazwą) lub
                jest przeindeksowane inaczej niż 'items' (np. Dział 4/5 odfiltrował
                pozycję przez unset, zostawiając klucze items 0 i 2, a lines
                zostały przenumerowane na 0 i 1). Skutek: pozycje trafiają do planu
                z price_base_grosze=0 i price_final_grosze=0, walidacja przechodzi
                (item_rows nie jest puste, limity długości nie dotyczą intów),
                Agent 10.2 zapisuje wiersze i pipeline robi COMMIT. W bazie powstaje
                oferta, w której suma pozycji ≠ net_grosze/gross_grosze nagłówka,
                a PDF (wyrenderowany wcześniej z 'lines') pokazuje inne kwoty niż
                zapisane pozycje — status działu to sukces mimo nieprawdziwego
                wyniku.
        naprawa: Przed zbudowaniem planu zweryfikować spójność: count($lines) === count($items) oraz istnienie $lines[$i]['unit_grosze']/['line_grosze'] dla każdego $i; w razie braku zwrócić fail (np. kod 'lines_mismatch'). Dodatkowo dopisać do $errors kontrolę, że suma price_final_grosze pozycji zgadza się z net_grosze nagłówka (po rabacie z Działu 5).

  [1.25] Blokada optymistyczna cicho wyłącza się dla istniejącej oferty, gdy numbering nie zawiera 'existing_lock_version' — i dodatkowo cofa licznik lock_version do 1.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:94
        status: potwierdzone
        skutek: Wejście: dokończenie draftu (offer_id>0), w którym tablica
                'numbering' nie ma klucza 'existing_lock_version' (Dział 2 nie
                ustawił go dla tej ścieżki, np. draft z hooka mp_lead_created, albo
                klucz przyszedł pod inną nazwą). Skutek: expected_lock_version=null
                → UPDATE bez wartownika (WHERE tylko po id), a lock_version zostaje
                ustawiony na 1, mimo że w bazie było np. 4. Dwaj handlowcy
                zapisujący korektę tej samej oferty równolegle obaj dostają sukces
                i wygrywa ostatni — zmiany pierwszego znikają bez śladu. Po
                cofnięciu lock_version do 1 kolejny zapis oparty na starym odczycie
                (expected=4) trafia 0 wierszy i fałszywie raportuje konflikt, a zapis
                z odczytem 1 przechodzi mimo nieaktualnych danych.
        naprawa: Uczynić brak tokenu błędem zamiast cichej degradacji: jeśli $offer_id > 0 && null === $existing_lock_version → MP_OB_Result::fail( 'Brak tokenu blokady optymistycznej dla istniejącej oferty.', ..., 'missing_lock_version' ). Alternatywnie w Agencie 10.2 traktować brak expected_lock_version przy is_update jako fail, nigdy jako UPDATE bez wartownika.

  [1.25] Brak właściciela draftu wykrywany wyłącznie przez `null`, podczas gdy created_by=0 (draft utworzony bez zalogowanego użytkownika) przechodzi jako „właściciel 0” i wywraca zarówno przypisanie właściciela, jak i kontrolę IDOR.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:85
        status: potwierdzone
        skutek: Wejście: draft założony w kontekście bez zalogowanego użytkownika
                (get_current_user_id() zwraca wtedy 0 — np. draft powstały z hooka
                po formularzu front-end), więc w bazie created_by=0 i Dział 2 zwraca
                existing_created_by=0, nie NULL. Skutek: (a) warunek `null !== 0` jest
                prawdziwy, więc handlowiec bez manage_options dostaje
                fail('not_offer_owner') i NIE MOŻE dokończyć draftu —
                dokumentowana ścieżka „pierwsze dokończenie draftu bez
                właściciela” jest zablokowana; (b) jeśli zapisu dokona
                administrator, $created_by = (int) 0 = 0, więc oferta zostaje na
                trwałe bez właściciela, a deklarowana reguła „pierwszy
                handlowiec zostaje właścicielem na stałe” nigdy się nie
                wykonuje.
        naprawa: Traktować 0 tak samo jak null: $existing_created_by = ( isset( $numbering['existing_created_by'] ) && (int) $numbering['existing_created_by'] > 0 ) ? (int) $numbering['existing_created_by'] : null; — wtedy zarówno fallback na get_current_user_id(), jak i kontrola własności działają zgodnie z komentarzem.

  [1.25] A7.1 cicho podstawia jezyk 'pl', gdy proces nie ma ustawionego jezyka — co unieważnia gwarancję deklarowaną przez K7.1 (brak wersji językowej ma zatrzymywać przebieg, a nie schodzić do polskiego).
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:328
        status: prawdopodobne
        skutek: Proces zalozony bez kolumny `lang` (proces prowadzony recznie/bez
                wtyczki 1 albo wiersz starszy niz kolumna) + zdarzenie bez klucza
                `lang`. Efekt EFFECT_NOTIFY_CLIENT → recipient dostaje lang='pl'.
                Anglojezyczny klient otrzymuje wiadomosc TPL_OFFER_SENT po polsku, a
                pipeline raportuje sukces — dokladnie ten skutek, ktoremu K7.1 mial
                zapobiec.
        naprawa: Nie zwracac domyslnego 'pl' w process_lang() dla odbiorcy AUDIENCE_CLIENT — zwrocic pusty lancuch i pozwolic K7.1 zakonczyc przebieg kodem `template_lang_missing`, albo dodac osobny kod bledu 'process_lang_unknown' w agencie. Domyslne 'pl' ma sens wylacznie dla LANG_SALESMAN, gdzie jest wymogiem z diagramu.

  [1.25] Adres handlowca pochodzi wylacznie z kontekstowego klucza `assignment`; nie ma zapasu w wierszu procesu. Gdy w danym przebiegu nie ma przydzialu, powstaje wiersz kolejki z pustym adresem, a A7.1 i K7.1 koncza sie sukcesem.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:161
        status: prawdopodobne
        skutek: Zmiana statusu procesu przydzielonego wczesniej (kontekst bez klucza
                `assignment`) → `$owner_id = 0` → find_salesman nie znajduje
                nikogo → wiersz kolejki z `email = ''`. Dzial 9 probuje wyslac bez
                adresata: powiadomienie o zmianie statusu nigdy nie dociera do
                handlowca, a przebieg audytu pokazuje status OK. Dodatkowo przy
                `$owner_id = 0` warunek `(int) $entry['user_id'] === 0` dopasuje
                pierwszy wpis snapshotu z brakujacym/pustym `user_id` — wtedy
                powiadomienie trafia do PRZYPADKOWEGO handlowca zamiast do
                wlasciciela.
        naprawa: Ustalac wlasciciela w kolejnosci: `assignment['user_id']` → kolumna wlasciciela w `$flow['row']`, a przy `$owner_id <= 0` w ogole nie dopasowywac po petli (early return). W K7.1 (albo w A7.3) dodac twardy warunek: pusty `email` odbiorcy = FAIL z kodem `recipient_without_email`, zamiast kolejkowania niewysylalnego wiersza.

  [1.25] render() i MARKER opisuja dwie rozne skladnie znacznika: regex dopuszcza spacje i wielkie litery, str_replace wymaga dokladnego `{{klucz}}` malymi literami. Znacznik zapisany w dopuszczonej przez regex formie nigdy nie zostanie podstawiony.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:137
        status: prawdopodobne
        skutek: Autor szablonu wpisuje `Dzień dobry {{ client_name }}` (spacje —
                forma jawnie tolerowana przez MARKER). render() zostawia tekst bez
                zmian mimo poprawnie przekazanej zmiennej `client_name`;
                unresolved_markers() zglasza `client_name` jako nierozwiazany. Skutek:
                albo K7.2 „puste-pola” zatrzymuje kazdy przebieg dla poprawnie
                napisanego szablonu, albo — gdy kontrola nie obejmuje danego pola
                — klient dostaje maila z surowym `{{ client_name }}` w tresci.
        naprawa: Podstawiac ta sama regula, ktora sluzy do wykrywania: `preg_replace_callback( self::MARKER, ... )` z normalizacja klucza (`strtolower`) i podmiana z tablicy `$vars`, zamiast str_replace po kluczach. Alternatywnie zwezic MARKER do `/\{\{([a-z0-9_]+)\}\}/` bez `\s*` i bez `/i`, zeby obie funkcje opisywaly ten sam jezyk szablonu.

  [1.25] Zanonimizowany proces nadal generuje wiersz kolejki do klienta — blokada dotyczy tylko zrodla adresu, nie samego powstania powiadomienia.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:292
        status: prawdopodobne
        skutek: Klient zada usuniecia danych, proces zostaje zanonimizowany, po czym
                handlowiec zmienia status wywolujacy EFFECT_NOTIFY_CLIENT. Do kolejki
                trafia wiadomosc zaadresowana na zaslepke po anonimizacji; Dzial 9
                wykonuje na niej proby wysylki, zapelnia licznik prob i kolumne bledu,
                a proces zamkniety wobec klienta produkuje trwale bledne wiersze
                powiadomien (a przy zaslepce w domenie routowalnej — maila do obcego
                adresata).
        naprawa: W A7.1 pomijac odbiorce AUDIENCE_CLIENT, gdy `MP_SW_Privacy::is_anonymized()` jest prawda dla wiersza procesu (i odnotowac powod w danych wyniku, np. `skipped[] = 'client_anonymized'`), zamiast kolejkowac wiadomosc na adres-zaslepke.

  [1.25] Status domknięcia zadań po terminie (`$status_po_terminie`) liczony jest RAZ dla całego zdarzenia, na podstawie samego szablonu TPL_FOLLOWUP_DUE, a potem przypisany wszystkim zadaniom z `$tasks['fire']`. Nie ma powiązania konkretnego powiadomienia z konkretnym zadaniem.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Cron uruchamia jedno zdarzenie TASK_DUE z dwoma wymagalnymi
                follow-upami: dla zadania A powiadomienie trafia do kolejki, dla
                zadania B Agent 7.2 pomija je z powodu niedostarczalnego adresu
                handlowca. `$poszlo` = true (znalezione dla A), więc OBA zadania są
                domykane jako STATUS_DONE — zadanie B pokazuje „wykonane”, choć
                handlowiec nic nie dostał, dokładnie wbrew regule zapisanej w
                komentarzu nad tym blokiem. Kierunek odwrotny: gdy w tej samej partii
                wypala zadanie innego typu (bez szablonu follow-up), a jedyny
                follow-up został pominięty, to zadanie innego typu dostaje
                STATUS_UNDELIVERED, mimo że jego własne powiadomienie poszło
                poprawnie. W obu przypadkach kryterium 5.5 (odtworzenie historii) daje
                fałszywy obraz.
        naprawa: Wyznaczać status osobno dla każdego zadania z `$tasks['fire']`: dopasowywać powiadomienie/pominięcie po identyfikatorze zadania (task_id / entity_ref), a nie po samym `template`, i dopiero brak dopasowanej wysyłki przy obecnym pominięciu tego zadania dawać STATUS_UNDELIVERED.

  [1.25] Plan zapisu nie niesie pominiętych powiadomień, więc wpis dziennika LOG_NOTIFICATION_SKIPPED nie ma z czego powstać.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Powiadomienie wewnętrzne do handlowca z niedostarczalnym adresem jest
                pomijane przez Agenta 7.2. Jeśli para A8.3 (dziennik) buduje wpisy z
                planu — a plan jest kontraktem między A8.1 a resztą działu — to
                zdarzenie kończy się bez śladu: w dzienniku nie ma ani
                `notification.queued`, ani `notification.skipped`. Przy reklamacji
                „dlaczego nikt nie oddzwonił” historia wysyłek pokazuje brak
                zdarzenia zamiast powodu pominięcia.
        naprawa: Dodać do `empty_plan()` klucz na pominięte powiadomienia (np. `notifications_skipped`), wypełniać go w A8.1 z `skipped_notifications` (wraz z powodem i odbiorcą) i generować z niego wpisy `notification.skipped` w tej samej transakcji. Jeśli A8.3 czyta kontekst z pominięciem planu — udokumentować to, bo dziś kontrakt planu tego nie pokazuje.

  [1.25] `lead_id` do wiersza procesu i do dziennika bierzemy z koperty zdarzenia, choć nazwa i e-mail klienta pochodzą ze snapshotu — niespójność z inwariantem I-3 opisanym w tym samym bloku.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Zdarzenie zaadresowane wyłącznie ofertą (koperta z
                `entity.offer_id`, bez `entity.lead_id`), dla którego Dział 2 i tak
                zbudował snapshot leada — INSERT tworzy wiersz procesu z `lead_id =
                0`, a wpis dziennika też dostaje `lead_id = 0`. Proces jest nie do
                powiązania z leadem w raportach i na liście, a przy więzie FK do
                tabeli leadów (dokumentacja działu, źródło 3) INSERT z
                wartością 0 wywraca całą transakcję — łącznie z dziennikiem i
                kolejką.
        naprawa: Ustalać `lead_id` z pierwszeństwem dla snapshotu (`$lead_row['id']`), a kopertę traktować jako źródło zapasowe — tak samo jak przy `client_name`/`client_email`; przy `lead_id === 0` przerwać plan błędem zamiast zapisywać zero.

  [1.25] Walidacja pozycji sprawdza wartosci PO rzutowaniu na int, ale do wyniku trafia oryginalna, nierzutowana wartosc — qty i product_id opuszczaja Dzial 1 jako string/float.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-01.php:156
        status: prawdopodobne
        skutek: Zadanie z items[0].qty = "3.9" (albo 3.9 z JSON-a): (int)"3.9" = 3 >
                0, wiec walidacja przechodzi, ale dalsze dzialy dostaja 3.9. Dzial
                wyceny liczy cena x 3.9 (kwota jak za 3.9 sztuki), a zapis do kolumny
                calkowitoliczbowej w BD-2 obcina do 3 — PDF i pozycja w bazie
                pokazuja inna ilosc niz podstawa wyliczonej kwoty. Analogicznie
                product_id = "12abc" przechodzi jako "12abc".
        naprawa: Po walidacji normalizowac wartosci przed wpisaniem do $items[$i]: $item['qty'] = (int) $item['qty']; $item['product_id'] = (int) $item['product_id']; (oraz variation_id, jesli obecny), tak zeby wynik agenta zawieral dokladnie te wartosci, ktore byly sprawdzane.

  [1.25] Brak kontroli, ze pola client.* i items.* sa skalarami — wartosc bedaca tablica przechodzi walidacje wymagalnosci i jest przekazywana dalej.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-01.php:119
        status: prawdopodobne
        skutek: Zadanie
                {"client":{"name":{"a":1},"email":"x@y.pl","nip":"123","country":"PL"},
                ...}: (string) na tablicy daje w PHP 8 warning "Array to string
                conversion" i wartosc "Array", wiec trim(...) !== '' i pole uznane za
                poprawne. Do dalszych dzialow (zapis oferty w Dziale 10, render PDF)
                idzie tablica zamiast napisu, a przy WP_DEBUG warning wypisany przed
                JSON-em psuje odpowiedz AJAX. Tak samo items[0].qty = [1] i
                items[0].product_id = [1] przechodza walidacje i trafiaja dalej jako
                tablice.
        naprawa: Przed testem wymagalnosci odrzucac nieskalarne wartosci, np. w petli po kluczach client: if ( ! is_scalar( $client[$key] ?? null ) ) { blad 'client.$key'; continue; }, a w petli po pozycjach analogiczny is_scalar() dla product_id, variation_id i qty.

  [1.25] Deklarowana w komentarzu SR1-01 nierozroznialnosc "cudzy szkic" vs "szkic nie istnieje" nie jest osiagnieta — oba przypadki daja rozny ZESTAW bledow w danych porazki.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-01.php:96
        status: prawdopodobne
        skutek: Uzytkownik z capability mp_offer_builder_manage_offers wysyla zadanie
                z poprawnymi items/wariant/lang, CELOWO bez obiektu client i z
                podstawianym offer_id = 1,2,3,... Dla nieistniejacego offer_id wynik
                porazki zawiera 5 bledow (offer_id + client.name + client.email +
                client.nip + client.country), dla istniejacego CUDZEGO szkicu tylko 1
                blad (offer_id), bo client zostal wypelniony ze snapshotu. Roznica
                liczby bledow jest wyrocznia pozwalajaca zmapowac, ktore sekwencyjne
                offer_id to realne szkice innych handlowcow — dokladnie to, czemu
                SR1-01 mial zapobiec.
        naprawa: W galezi IDOR nie dociagac danych klienta ze szkicu (zamienic bezwarunkowe przypisanie $client na przypisanie tylko w gałęzi bez bledu, np. przez else / continue), zeby oba przypadki produkowaly identyczny zestaw bledow niezaleznie od tego, czy szkic istnieje.

  [1.25] Licznik `db_reads` (bramka QA2 'jeden-odczyt: db_reads = 1') mierzy fazę, a nie liczbę odczytów: rośnie dokładnie raz, podczas gdy Dział 2 wykonuje 1 + N zapytań (N = liczba handlowców) plus zapytania aktora i cztery zapytania bezpośrednie. Dokumentacja działu deklaruje co innego: 'Klasa czyta wp_users i wp_usermeta jednym zapytaniem'.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Instalacja z 300 handlowcami: jedno zdarzenie crona wykonuje ~305
                zapytań SQL, a raport bramki QA2 pokazuje db_reads = 1 i przechodzi
                na zielono. Przy 50 zdarzeniach w jednym przebiegu crona to ~15 000
                zapytań, których bramka z definicji nie zobaczy; tak samo nie
                zobaczy regresji polegającej na tym, że Dział 3-7 sięgnie do bazy
                własnym `get_userdata()` bez wywołania `count_db_read()`.
        naprawa: 1) Zasilić cache meta jednym zapytaniem — `'fields' => 'all_with_meta'` albo `cache_users( $ids )` / `update_meta_cache( 'user', $ids )` po zapytaniu; 2) mierzyć rzeczywistość, a nie deklarację: zapisać `$wpdb->num_queries` przed i po budowie snapshotu i to porównanie raportować bramce (limit fazowy zamiast stałej 1).

  [1.25] Zespół aktora czytany jest bez normalizacji, którą stosuje lista handlowców (`trim`). Ta sama wartość meta daje w snapshocie dwie różne postacie.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Kierownik ma w profilu `mp_sw_team` = 'Zachod ' (spacja na końcu,
                typowa przy wklejeniu z arkusza). W sekcji `salesmen` jego zespół to
                'Zachod', w sekcji `roles.actor` — 'Zachod '. Porównanie
                równościowe w `read_team_members()` i w Działach 3-7 nie trafia:
                `team_members` wychodzi puste, więc kierownik z uprawnieniem
                `view_team` nie widzi ani jednego procesu swojego zespołu i jest
                traktowany jak osoba z zewnątrz. Wariant odwrotny: wartość
                złożona z samych spacji — handlowiec trafia na listę `incomplete`
                z brakiem `mp_sw_team`, a ten sam ciąg u aktora przechodzi jako
                zespół niepusty.
        naprawa: Wydzielić jeden helper normalizujący (`trim`, ewentualnie ujednolicenie wielkości liter, jak przy `country`) i użyć go w obu miejscach odczytu meta `mp_sw_team`.

  [1.25] Brak `lead_id` w kopercie nie jest uzupełniany z wiersza oferty, mimo że wiersz jest w tym samym strzale odczytany i zawiera `lead_id`. Dane klienta jadą dalej puste.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Wtyczka 2 zgłasza `offer.approved` z `entity = array( 'offer_id' =>
                42 )` (numer oferty to jej klucz naturalny, leada nie musi podawać).
                Snapshot wychodzi z `lead.exists = false`, `lead.email = ''` oraz
                `offer.belongs = false` (bo warunek `$lead_id > 0` nie jest
                spełniony), więc Dział 8 nie ma adresu odbiorcy, a bramka odrzuca
                proces jako 'oferta nie należy do leada' — chociaż w tym samym
                strzale odczytano `wp_mp_ob_offers.lead_id = 7`. Poprawna oferta nie
                zostaje wysłana.
        naprawa: Zamienić kolejność: najpierw `read_offer()`, potem `read_lead()`; gdy koperta nie niesie `lead_id`, wziąć go z wiersza oferty (analogicznie do fallbacku `offer_id` z wiersza procesu) i oznaczyć źródło. Powiązanie ustalone z samego wiersza bazy jest z definicji potwierdzone, więc `belongs` może być wtedy `true` bez naruszenia I-3 (adres i tak czytany jest z tabeli leadów).

  [1.25] Kontrola "treść PDF zgodna z kopertą" jest tautologiczna: Agent 9.1 sam wpisuje do metadanych wartości wzięte z kontekstu, a Agent 9.2 porównuje odczytane metadane z tym samym kontekstem. Weryfikowany jest wyłącznie round-trip zapisu metadanych, a nie to, co widnieje na stronie dokumentu.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:230
        status: prawdopodobne
        skutek: Dział 7 scala do HTML błędną cenę (np. netto zamiast brutto albo
                nieaktualną sumę). PDF pokazuje klientowi złą kwotę, ale metadane
                MPOfferGross zawierają poprawną wartość z koperty, więc Agent
                9.2, Krytyk 9.2 i QA 9 przechodzą, a dział raportuje "numer i kwota
                wyciągnięte z pliku = zgodne z kopertą". Oferta z błędną ceną
                wychodzi jako zweryfikowana.
        naprawa: Albo zawęzić deklarację działu i kryterium odbioru do "metadane zgodne z kopertą" (bez roszczenia o treści dokumentu), albo weryfikować kwotę/numer na źródle renderu — np. przed renderem sprawdzić, że w $html występuje sformatowana kwota brutto i numer oferty (to samo źródło, które trafia na stronę).

  [1.25] Brak strażnika dla gross_grosze — w przeciwieństwie do offer_number. Domyślne 0 po stronie zapisu i po stronie kontroli powoduje, że brak sumy brutto w kontekście przechodzi kontrolę bez śladu.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:64
        status: prawdopodobne
        skutek: Dział liczący sumy nie ustawia (albo ustawia pod inną nazwą)
                klucza gross_grosze w kontekście — dokładnie ten sam błąd
                wiązania, przed którym broni się osobno numer oferty. Agent 9.1
                zapisuje do PDF MPOfferGross="0", Agent 9.2 porównuje "0" z "0" i
                zwraca OK. Dział raportuje zweryfikowaną kwotę brutto równą 0 gr,
                a plik z takim metadanym idzie do Działu 10 jako poprawny.
        naprawa: Dodać w Agencie 9.1 twardy warunek symetryczny do numeru oferty: jeśli w kontekście nie ma klucza gross_grosze albo wartość <= 0, zwrócić fail('missing_gross') zamiast domyślnego 0.

  [1.25] Podstawienie numeru oferty do HTML zostaje w zmiennej lokalnej — kontekst nadal niesie wersję dokumentu z niepodstawionym znacznikiem, a bramka "żaden znacznik nie zostaje pusty" sprawdza tylko kopię lokalną.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:74
        status: prawdopodobne
        skutek: Po Dziale 9 kontekst zawiera document['html'] wciąż ze znacznikiem
                {{offer_number}}. Jeśli Dział 10/11 archiwizuje, pokazuje w
                podglądzie lub ponownie renderuje document['html'], klient/operator
                widzi dokument bez numeru oferty (albo z surowym znacznikiem),
                rozjeżdżający się z wydanym PDF-em — mimo że bramka
                anty-znacznikowa w Dziale 9 przeszła.
        naprawa: Po fill_deferred zwrócić także zaktualizowany dokument w danych wyniku (np. 'document' => array_merge( $document, array( 'html' => $html ) )), żeby kontekst niósł dokładnie tę treść, która trafiła do PDF.

  [1.25] Do wtyczek 1 i 2 wychodzi zdarzenie `mp_sw_flow_updated` z pustym `status`, gdy w kopercie nie ma przejścia statusu — pole jest obecne, ale niesie nieprawdziwą (pustą) wartość zamiast być pominięte.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:159
        status: prawdopodobne
        skutek: Zdarzenie, które zapisuje coś innego niż zmiana statusu (np.
                przypisanie handlowca albo domknięcie zadania): `writes > 0`,
                `committed = true`, ale `transition` puste. A9.1 wystawia
                `mp_sw_flow_updated` z `status => ''`. Odbiorca we wtyczce 1 lub 2,
                który mapuje pole `status` z payloadu na własny rekord (tak jak ta
                wtyczka mapuje `mp_lead_created`), nadpisze poprawny status leada
                pustą wartością — proces zniknie z filtrów po statusie mimo że
                nikt statusu nie zmieniał.
        naprawa: Nie wstawiać klucza `status` do payloadu, gdy `transition['to']` nie istnieje (`if ( isset( $transition['to'] ) ) { $payload['status'] = (string) $transition['to']; }`), albo dodać jawny znacznik `'status_changed' => isset( $transition['to'] )`. To samo dla `lead_id`, który przy braku `event['entity']['lead_id']` wychodzi jako `0`.

  [1.25] Agent 11.2 deklaruje sprzatanie stanu tymczasowego, ale nie wykonuje zadnej operacji sprzatania — zawsze zwraca `temp_cleaned = true`, a krytyk K11.2 sprawdza wylacznie obecnosc tej flagi.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:53
        status: prawdopodobne
        skutek: Pipeline konczy sie raportem, w ktorym para 11.2/K11.2 jest zielona,
                wiec operator i QA maja dowod, ze stan tymczasowy zostal usuniety.
                Faktycznie zadne dane robocze (transienty, wpisy tymczasowe,
                pozostalosci kontekstu) nie sa kasowane i narastaja przy kazdym
                leadzie — awaria jest niewidoczna, bo kontrola jakosci potwierdza
                czynnosc, ktorej nigdy nie wykonano.
        naprawa: Albo zaimplementowac faktyczne sprzatanie i ustawiac `temp_cleaned` na podstawie jego wyniku, albo — jesli to swiadomy stub — zaznaczyc to w opisie agenta i w naglowku pliku tak jak przy 11.1, zeby raport nie twierdzil, ze operacja sie odbyla.

  [1.25] Bramka QA dzialu 11 sprawdza `pipeline_status = completed`, ale ta wartosc ustawia bezwarunkowo agent 11.3 w tym samym dziale — warunek odrzucenia nigdy nie bedzie prawdziwy, jesli 11.3 sie wykonal.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:137
        status: prawdopodobne
        skutek: Dowolny lead, przy ktorym wczesniejsze dzialy przekazaly niekompletne
                dane (brak `started_ts`, brak `lead_id`, brak `vat_status`), i tak
                dostaje `pipeline_status='completed'` oraz zielona bramke QA11 —
                raport koncowy stwierdza pomyslne domkniecie procesu, mimo ze czesc
                pipeline'u nie dostarczyla wyniku. Bramka nie moze wykryc zadnej
                takiej regresji, bo weryfikuje wlasny literal, a nie stan procesu.
        naprawa: Wyliczac `pipeline_status` z faktycznego stanu (np. wymagane klucze kontekstu: `lead_id > 0`, `started_ts > 0`, komplet flag dzialow) i ustawiac 'completed' tylko wtedy; QA11 powinien sprawdzac te warunki niezaleznie, a nie flage ustawiona przez audytowanego agenta.

  [1.25] format_money() uznaje za "brak wyniku" wyłącznie null, a NumberFormatter::format() zgodnie z cytowaną w dokumentacji działu sygnaturą zwraca `string|false`. Wartość false nie uruchamia fallbacku i po konkatenacji znika bez śladu — kwota wypada z dokumentu, a dział raportuje OK.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: Serwer z intl zbudowanym na okrojonych/uszkodzonych danych ICU
                (przypadek opisany wprost w komentarzu tej samej metody) → format()
                zwraca false → warunek `null === $number` jest fałszywy, fallback
                pominięty, `false . ' ' . 'zł'` daje ' zł' → w PDF przy
                {{gross_total}}/{{net_total}} widnieje samo 'zł' bez kwoty, a Agent
                7.2 i tak zwraca MP_OB_Result::ok i dokument idzie do klienta.
        naprawa: Warunek wejścia w fallback zmienić na `if ( ! is_string( $number ) || '' === $number )` (albo dołożyć `false === $number`), żeby każdy nieudany format() schodził na tor zapasowy zamiast produkować pustą kwotę.

  [1.25] Mechanizm znacznika odroczonego nie sprawdza, czy szablon w ogóle zawiera {{offer_number}}. Brak znacznika w szablonie przechodzi bramkę jako sukces, a Dział 9 nie ma czego podstawić — kryterium odbioru "numer NA DOKUMENCIE" cicho nie zostaje spełnione.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: W BD-2 leży szablon (np. wersja 'en' nieaktualizowana razem z 'pl')
                bez znacznika {{offer_number}} → pending_markers() zwraca pustą
                tablicę → Agent 7.2 OK → Dział 9 wywołuje fill_deferred(),
                str_replace nic nie znajduje i też zwraca OK → wygenerowany PDF nie
                ma numeru oferty na stronie, czyli dokładnie ta regresja, którą ten
                mechanizm miał usunąć, przy zielonym wyniku całego pipeline'u.
        naprawa: W Agencie 7.2 po scaleniu wymagać obecności każdego znacznika odroczonego (`false !== strpos( $html, '{{offer_number}}' )`) i failować kodem np. 'missing_deferred_marker'; symetrycznie w Dziale 9 failować, gdy str_replace nie wykonał żadnej podmiany.

  [1.25] build_items_table() nie waliduje zgodności tablic items/products/lines — brakujący element daje cicho 0 gr i pustą nazwę zamiast błędu, więc dokument może być wewnętrznie sprzeczny przy statusie OK.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: Dział 4 zwraca `lines` kluczowane inaczej niż `items` (np. tablica
                po array_filter z zachowanymi kluczami albo indeksowana id produktu)
                → dla każdego $i `isset($lines[$i])` jest fałszem → tabela
                pozycji w PDF pokazuje '0,00 zł' w kolumnach cena/wartość przy
                pełnych nazwach, podczas gdy {{subtotal}}, {{net_total}} i
                {{gross_total}} pokazują prawdziwe kwoty z kontekstu; handlowiec
                wysyła ofertę, w której pozycje nie sumują się do podsumowania, a
                Dział 7 zwrócił OK.
        naprawa: Przed budową wiersza wymagać kompletu danych (`isset( $lines[ $i ]['unit_grosze'], $lines[ $i ]['line_grosze'], $products[ $i ]['name'] )`) i przy pierwszym braku zwracać MP_OB_Result::fail (np. 'line_missing'); dodatkowo porównać sumę line_grosze z subtotal_grosze jako niezależną kontrolę spójności.

  [1.25] Krytyk 11.1 "jednokrotność" mierzy różnicę did_action() wokół własnego, bezwarunkowego wywołania do_action() — a więc wielkość, która z definicji zawsze wynosi 1. Gałąź błędu 'event_not_fired_once' jest nieosiągalna, krytyk nie weryfikuje niczego.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:107
        status: prawdopodobne
        skutek: mp_offer_created zostaje wystawione dla tej samej oferty dwa razy w
                jednym żądaniu (np. ponowne uruchomienie działu 11 / powtórzenie
                przebiegu po wznowieniu): $before=1, $after=2, delta nadal = 1 →
                krytyk zwraca OK, pipeline melduje sukces, a plugin 3 dostaje dwa
                zdarzenia dla jednego offer_id i zakłada dwie sprawy sprzedażowe.
                Deklarowana ochrona ("dokładnie raz") nie zadziała nigdy — ani
                przy pominięciu, ani przy dublu.
        naprawa: Sprawdzać wartość bezwzględną licznika dla tej oferty, a nie deltę lokalną: zapamiętać $before i wymagać 0 === $before (zdarzenie jeszcze nie padło w tym żądaniu) oraz 1 === $after; dodatkowo zabezpieczyć idempotencję po offer_id (np. meta/flaga 'event_emitted' zapisana przy ofercie), żeby powtórny przebieg nie wystawił zdarzenia drugi raz.

  [1.25] Nieudana finalizacja PDF kończy dział błędem, mimo że oferta jest już zapisana i zacommitowana w bazie. Odpowiedź dla klienta mówi "nie udało się", a stan faktyczny to: oferta istnieje, numer zużyty, zdarzenie do pluginu 3 nigdy nie padnie. Brak jakiejkolwiek kompensacji.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:70
        status: prawdopodobne
        skutek: Pełny dysk lub brak uprawnień do katalogu docelowego przy zapisie
                oferty: użytkownik dostaje JSON błędu i klika "generuj ofertę"
                ponownie → w bazie powstaje druga oferta z kolejnym numerem,
                pierwsza zostaje osierocona (istnieje w BD, nieznana pluginowi 3, bez
                PDF), a numeracja ma lukę. Powtarzanie próby mnoży osierocone
                rekordy.
        naprawa: Przy 'pdf_finalize_failed' wykonać krok kompensujący i zwrócić stan zgodny z faktami: oznaczyć zapisaną ofertę statusem błędu/niekompletności (np. 'pdf_failed') i zwrócić w odpowiedzi offer_id + offer_number, żeby ponowienie wznawiało tę samą ofertę zamiast tworzyć nową.

  [1.25] Dział 8 zapisuje wpis 'lead_created' i kończy się sukcesem także wtedy, gdy leada w ogóle nie utworzono (lead_id = 0). Status działu (log_status='success', d8_logged=true) nie odpowiada temu, co naprawdę się stało w procesie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-08.php:37
        status: prawdopodobne
        skutek: Wcześniejszy dział (zapis leada do BD) nie ustawia lead_id w
                kontekście albo ustawia 0 (INSERT leada nie powiódł się). Dział 8
                wykonuje się dalej: do wp_mp_activity_log trafia wiersz z lead_id =
                NULL, action = 'lead_created' i description = 'Utworzono leada #0'.
                QA8 przechodzi (log_id > 0), dział raportuje log_status = 'success'.
                W historii aktywności powstaje wpis stwierdzający utworzenie leada,
                którego nie ma — audyt/raporty liczące zdarzenia 'lead_created'
                pokazują zawyżone liczby i osierocone wpisy, a pipeline nie
                sygnalizuje żadnego problemu. Dodatkowo wiersz jest wewnętrznie
                sprzeczny: lead_id NULL, ale opis mówi o leadzie #0.
        naprawa: W agencie 8.1 przerwać dział, gdy lead_id <= 0 (np. `return MP_Result::fail( 'Brak lead_id — nie ma czego logować', array(), 'no_lead_id' );`) albo zapisywać wtedy inną akcję odpowiadającą faktowi (np. 'lead_create_failed') i nie budować description z 0. QA8 powinien dodatkowo wymagać zgodności zapisanego lead_id z kontekstem.

  [1.25] Warunek ochronny `empty( $data )` w agencie 8.3 nigdy nie będzie prawdziwy w normalnym przebiegu działu, więc gałąź błędu 'no_activity' jest martwa i pusty/niekompletny rekord i tak trafia do INSERT-u.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-08.php:94
        status: prawdopodobne
        skutek: Agent 8.1 nie ustawia activity_data (np. jego wynik został
                odrzucony/nadpisany albo kontekst nie zawiera activity_data). 8.2
                startuje z pustej tablicy, dokłada user_id, ip_address i meta_json i
                przekazuje dalej. 8.3 przechodzi obok kontroli `empty()` i woła
                insert_activity() na rekordzie bez kolumn lead_id, action i
                description. Skutkiem jest albo wiersz logu bez akcji i opisu (jeśli
                kolumny mają wartości domyślne), albo błąd bazy zamiast
                czytelnego wyniku 'no_activity' — kontrola, która miała ten
                przypadek złapać, nie działa.
        naprawa: Sprawdzać w 8.3 obecność pól obowiązkowych, a nie pustość tablicy, np. `if ( empty( $data['action'] ) || ! array_key_exists( 'lead_id', $data ) ) { return MP_Result::fail( ... 'no_activity' ); }`. Analogicznie 8.2 powinien odmówić pracy, gdy nie dostał activity_data z 8.1.

  [1.25] Agent 2.2 zwraca `normalized => true`, a agent 2.3 `form_valid => true` także wtedy, gdy sanitize_email() po cichu ZMIENIŁ adres na inny, istniejący składniowo adres. Nie ma porównania wartości przed/po normalizacji ani żadnego sygnału o modyfikacji.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-02.php:73
        status: prawdopodobne
        skutek: Zgłoszenie z adresem zawierającym znak spoza dozwolonego zbioru, np.
                `krzysztof.użytkownik@example.com` lub `jan!kowalski@example.com` →
                sanitize_email() usuwa niedozwolone znaki i zwraca
                `krzysztof.ytkownik@example.com` / `jankowalski@example.com` →
                is_email() zwraca prawdę → dział kończy się `form_valid=true`,
                `d2_valid=true`, bez jednego wpisu w `errors`. Dalsze działy dostają
                i zapisują adres, którego użytkownik nigdy nie podał;
                korespondencja trafia pod zły albo nieistniejący adres, a pipeline
                raportuje pełny sukces.
        naprawa: W agencie 2.2 porównać wartość przed i po sanitizacji (`$raw !== $clean`) i albo zwrócić `MP_Result::fail(..., 'email_modified')`, albo dołożyć flagę `email_changed` do danych, którą krytyk K2.2 traktuje jako błąd walidacji zamiast przepuszczać cichą podmianę.

  [1.25] Agent 2.3 normalizuje kod kraju (`strtoupper( trim( ... ) )`) wyłącznie do zmiennej lokalnej i nie zwraca go w wyniku; agent 2.2 w ogóle nie przetwarza pola `country`. Walidacja przepuszcza więc wartości, których znormalizowana postać nigdy nie trafia dalej — dokładnie ten skutek, przed którym komentarz w kodzie deklaruje ochronę.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-02.php:145
        status: prawdopodobne
        skutek: Formularz z `country = "pl"` albo `country = " pl "` → 2.3 sprawdza
                `"PL"` i nie zgłasza błędu → `form_valid=true` → w kontekście
                nadal leży `"pl"` / `" pl "`, bo nikt nie zapisał wartości
                znormalizowanej. Dział 3 wywołuje VIES z kodem kraju zawierającym
                spacje lub małe litery i dostaje błąd/odrzucenie, mimo że dział 2
                zaraportował poprawną walidację. Dodatkowo `country` jest jedynym
                polem użytkownika, które nie przechodzi przez żadną funkcję
                sanitizującą WordPressa, więc surowa wartość idzie dalej i do
                zapisu.
        naprawa: Dodać `country` do wyniku agenta 2.2 (np. `sanitize_text_field()` + `strtoupper( trim( ... ) )`), a w 2.3 walidować już znormalizowaną wartość z kontekstu i zwrócić ją w danych wyniku, żeby dział 3 na pewno dostał `PL`, a nie `" pl "`.

  [1.25] Normalizacja NIP usuwa wszystkie znaki niebędące cyframi, a walidacja wymaga dokładnie 10 cyfr — co jest sprzeczne z obsługą zgłoszeń spoza Polski, którą ten sam dział jawnie dopuszcza (walidacja `country` pod kątem wywołania VIES w dziale 3).
        mp-lead-intake/includes/pipeline/departments/class-mp-department-02.php:121
        status: prawdopodobne
        skutek: Zgłoszenie firmy czeskiej: `country = "CZ"`, `nip = "CZ12345678"` →
                2.2 zostawia `"12345678"` → 2.3 zwraca błąd `NIP powinien mieć 10
                cyfr` → QA2 zwraca `validation_failed` i poprawny lead z ważnym
                numerem VAT UE zostaje odrzucony, mimo że dział 3 (VIES) potrafiłby
                go zweryfikować. Odwrotny wariant: numer, który po usunięciu
                prefiksu ma przypadkiem 10 cyfr, przechodzi jako „NIP" i jest dalej
                traktowany jak numer polski.
        naprawa: Uzależnić regułę długości od kodu kraju: dla `country` pustego lub `PL` wymagać 10 cyfr, dla pozostałych krajów dopuścić zakres długości numeru VAT UE (i nie usuwać liter, lecz odciąć wyłącznie prefiks kraju), albo jawnie udokumentować i wymusić, że dział przyjmuje wyłącznie podmioty z `country = PL`.

  [1.25] Zakres 'team' wymaga niepustego pola `team`, a zasieg operacji (in_reach) wymaga tylko flagi `view_team` — te same dane aktora daja dwie sprzeczne odpowiedzi w jednym dziale, a widok jest cicho zawezany do 'own'.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-03.php:276
        status: prawdopodobne
        skutek: Handlowiec ma w snapshocie `view_team = true` i niepusta liste
                `team_members`, ale puste `team` (meta zespolu nieustawiona lub
                usunieta). A3.1 dla procesu kolegi z tej listy wola in_reach() ->
                true, wiec K3.1 przepuszcza np. EVENT_STATUS_CHANGE i zmiana statusu
                przechodzi. Rownoczesnie A3.2 wpada w galaz `else` i zwraca
                scope='own' oraz `scope_members = array()`. Pulpit, ktory wg
                komentarza ma budowac zapytanie 'z TEJ listy', nie pokaze procesu, na
                ktorym aktor przed chwila legalnie wykonal operacje — czyli
                dokladnie ciche zawezenie widoku, ktorego dokumentacja dzialu zakazuje
                ('nigdy ciche zawezenie'), przy statusie operacji 'allowed'.
        naprawa: Oprzec decyzje o zakresie na tym samym predykacie, co in_reach(): `elseif ( $view_team && ! empty( $actor['team_members'] ) )`, a `team` traktowac wylacznie jako etykiete raportowa. Alternatywnie (jesli pole `team` ma byc warunkiem koniecznym) dodac ten sam warunek do in_reach(), zeby oba testy odmawialy razem.

  [1.25] Agent 10.2 dokleja do odpowiedzi status z domyslna wartoscia 'new' rowniez wtedy, gdy odpowiedz oznacza porazke — pole status nie odpowiada temu, co naprawde sie stalo i jest sprzeczne z rownoczesnie ustawianym message.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-10.php:59
        status: prawdopodobne
        skutek: Zapis leada nie powiodl sie, wiec lead_id = 0 i w kontekscie nie ma
                klucza 'status'. Odpowiedz to: success=false, lead_id=0, status='new',
                score=0, message='Blad tworzenia leada'. Konsument rozgalezinajacy sie
                po polu status (np. logowanie lub kolejkowanie leada o statusie 'new')
                potraktuje nieistniejacego leada jak swiezo utworzonego, mimo ze zaden
                rekord nie powstal.
        naprawa: Ustawiac status warunkowo, spojnie z success — np. $r['status'] = ! empty( $r['success'] ) ? (string) $context->get( 'status', 'new' ) : 'failed'; albo w ogole nie dodawac status/score/salesman_id do odpowiedzi, gdy success jest falszem.

  [1.25] Agent 3.2 ustawia flagę kontraktową `items_valid => true` wyłącznie na podstawie własnej kontroli ilości — nie sprawdza `items_matched`. Kod opiera się na niepisanym założeniu ("dotarcie tu = istnienie już potwierdzone"), które jest sprzeczne z tym, co robi bramka QA tego samego działu: QA Agent 3 jawnie weryfikuje `items_matched` ORAZ `quantities_ok`. Dokładnie jedno z tych dwóch miejsc jest błędne: albo runner nie przerywa po nieudanym agencie (wtedy Agent 3.2 emituje nieprawdziwy status sukcesu), albo przerywa (wtedy warunek `empty( $context->get( 'items_matched' ) )` w bramce nigdy nie będzie prawdziwy i jest kodem martwym).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-03.php:121
        status: prawdopodobne
        skutek: Żądanie z pozycją items[0] wskazującą produkt bez pokrycia w
                snapshocie (brak `products[0]` albo `purchasable` = false) i z
                poprawną ilością qty=5. Agent 3.1 zwraca fail `items_not_matched`.
                Jeśli MP_OB_Department::process() wykonuje kolejne pary mimo porażki
                poprzedniej (co zakłada bramka QA, inaczej jej sprawdzenie
                `items_matched` jest nieosiągalne), Agent 3.2 kończy się sukcesem i
                wpisuje do kontekstu `items_valid = true` — czyli status "pozycje
                zwalidowane" mimo nieudanej walidacji istnienia. Dział 4 (kalkulacja)
                czytający `items_valid` z kontraktu JSON dostanie zgodę na liczenie
                koszyka zawierającego pozycję bez produktu w snapshocie.
        naprawa: Uzależnić flagę od faktycznego stanu zamiast od założenia o kolejności: w Agencie 3.2 zwracać `'items_valid' => ! empty( $context->get( 'items_matched' ) )`, albo (lepiej) wyliczać `items_valid` w jednym miejscu z obu flag i zapewnić, że dane bramki są scalane z kontekstem. Jeśli natomiast runner faktycznie przerywa dział po pierwszej porażce — usunąć nieosiągalny warunek z QA Agenta 3 i udokumentować gwarancję przerwania, żeby założenie przestało być niepisane.

  [1.25] Gałąź „kept" (proces ma już właściciela) nie weryfikuje, czy ten właściciel nadal istnieje w snapshocie i czy ma `active`. Przypisanie jest zwracane jako poprawne, a K4.2 przepuszcza je wyłącznie na podstawie `user_id > 0`.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:293
        status: prawdopodobne
        skutek: Handlowiec odchodzi z firmy i Dział 2 zwraca go z `active = 0` (albo
                w ogóle przestaje go zwracać w `salesmen.complete`). Procesy, które
                miały go w `flow.row.assigned_user_id`, przy każdym kolejnym
                zdarzeniu (zmiana statusu, zatwierdzenie oferty) dostają `match =
                'kept'`, `fallback = false` i wynik OK — lead pozostaje na trwałe
                przypisany do nieaktywnego konta, żadna bramka tego nie wychwytuje, a
                uzasadnienie A4.3 raportuje przypisanie jako prawidłowe.
        naprawa: W gałęzi `$current > 0` sprawdzić, czy `$current` występuje w `salesmen.complete` z `active` prawdziwym; gdy nie — nie zwracać `match = 'kept'`, tylko przejść do `MP_SW_D4_Assigner::decide()` (z `reassigned = true` i osobnym powodem, np. `match = 'reassigned_inactive'`), żeby K4.2 i K4.3 miały co sprawdzać.

  [1.25] `decide()` może zwrócić decyzję z `user_id = 0` przy jednocześnie ustawionym `match` (np. `exact`), a rekord bez `user_id` zawsze wygrywa sortowanie w `pick()` i blokuje przypisanie dla całego procesu.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:159
        status: prawdopodobne
        skutek: W `salesmen.complete` pojawia się jeden rekord z pustym/brakującym
                `user_id` (usunięty użytkownik, niekompletna usermeta `mp_*`). Ten
                rekord wygrywa sortowanie w każdym kręgu, `decide()` zwraca `user_id
                = 0` z `match = 'exact'`, `MP_SW_D4_Agent_Rotation` ustawia
                `reassigned = false` (bo `0 > 0`), a K4.2 przerywa proces fatalnie
                komunikatem „Nie wybrano właściciela procesu mimo dostępnych
                handlowców" — mimo że pozostali aktywni handlowcy byli w puli i
                mogli lead przejąć. Jeden zepsuty rekord zatrzymuje wszystkie
                przypisania.
        naprawa: W `candidate_tiers()` pomijać rekordy, dla których `(int) $salesman['user_id'] <= 0` (obok sprawdzenia `active`), a w `decide()` traktować `$id <= 0` jak brak kandydata (`continue` do następnego kręgu).

  [1.25] Nierozpoznany kraj jest po cichu zamieniany na 'PL', mimo że kraj jest częścią klucza wyszukiwania (country, nip) — zapytanie leci na inny klucz niż dane wejściowe, a dział zgłasza pełny sukces.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-01.php:50
        status: prawdopodobne
        skutek: Formularz przychodzi z krajem podanym inaczej niż jako 2-literowy kod
                ISO (np. 'DEU', 'Deutschland', 'Niemcy', 'GB-ENG') albo z VAT-ID w
                polu NIP ('DE123456789') przy pustym polu kraju. Warunek preg_match
                nie przechodzi, $country zostaje ustawione na 'PL', więc
                get_leads_by_nip() pyta o parę ('PL', cyfry) zamiast ('DE', cyfry).
                Istniejący klient zagraniczny nie zostaje znaleziony, agent zwraca
                MP_Result::ok z leads = [], krytyk (tablica) i QA (is_array)
                przepuszczają — dalsza część pipeline'u dostaje fałszywą
                informację „brak historii” i traktuje znanego klienta jak nowego
                (duplikat leada zamiast dopięcia do istniejącego).
        naprawa: Nie podstawiać milcząco 'PL'. Albo zwrócić MP_Result::fail z kodem typu 'invalid_country' dla wejścia, którego nie da się zmapować, albo wywołać tę samą, jedną funkcję normalizacji kraju, której używa dział 4.1 (wydzielić ją do wspólnego helpera i wołać z obu miejsc), tak aby dział 1 i dział 4.1 nie mogły dać różnego wyniku dla tego samego wejścia.

  [1.25] Przy pustym/nienumerycznym NIP dział w ogóle nie odpytuje BD-3, ale raportuje sukces z pustą listą — wynik jest nieodróżnialny od „sprawdzono i nic nie znaleziono”, a bramka QA to zatwierdza.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-01.php:53
        status: prawdopodobne
        skutek: Zgłoszenie bez NIP-u albo z wartością bez cyfr (np. 'brak', 'n/d',
                '---'): preg_replace daje '', gałąź warunkowa pomija odczyt z BD-3,
                agent zwraca ok(leads=[]), krytyk K1.1 i QA1 przechodzą, bramka
                jakości akceptuje dział. Dalsze działy widzą dokładnie ten sam
                sygnał co przy realnie wykonanym zapytaniu bez trafień, więc
                uznają zgłaszającego za nowego klienta i zakładają duplikat, mimo
                że historia w wp_mp_leads nigdy nie została sprawdzona.
        naprawa: Przekazywać dalej informację, czy odczyt faktycznie się odbył, np. MP_Result::ok( array( 'leads' => $leads, 'leads_lookup_done' => ( '' !== $nip ) ) ), i rozszerzyć QA Agent 1 o sprawdzenie tej flagi (lub zwracać fail z kodem 'missing_nip', jeśli NIP jest polem wymaganym) — tak, by pusta lista bez zapytania nie mogła przejść bramki jako komplet danych działu.

  [1.25] Bramka QA dzialu 4 nie moze zwrocic porazki — wszyscy trzej agenci ustawiaja niepuste wartosci domyslne, wiec warunek pustki w QA4 jest nieosiagalny, a kod bledu assignment_incomplete to martwa galaz.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:140
        status: prawdopodobne
        skutek: Zgloszenie z country='XX', segment='asdfgh' (przekazany wprost z
                formularza) i client_category='' przechodzi dzial 4: QA4 zwraca ok(
                d4_assigned => true ), a raport/bramka jakosci pokazuje 'przypisanie
                zweryfikowane', mimo ze zweryfikowana zostala tylko niepustosc
                wartosci, ktore QA sam przed chwila otrzymal jako defaulty. Zadne
                wejscie nie jest w stanie wywolac assignment_incomplete, wiec bramka
                nie chroni przed niczym.
        naprawa: QA4 ma sprawdzac zbiory wartosci, a nie pustke: country wobec listy ISO 3166-1 alpha-2, segment wobec wartosci z MP_D4_Agent_Segment::MAP + 'Inne', client_category wobec dozwolonego zbioru (np. B2B/B2C). Dopiero wtedy assignment_incomplete/'invalid_value' staje sie osiagalny.

  [1.25] Walidacja kraju sprawdza tylko ksztalt 'dwie wielkie litery', a nie przynaleznosc do ISO 3166-1 alpha-2 — nieistniejacy kod kraju przechodzi jako poprawny i trafia dalej (m.in. do weryfikacji VIES).
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:35
        status: prawdopodobne
        skutek: Formularz przesyla country='UK' (potoczny, ale nie-ISO; poprawny kod
                to 'GB') albo country='XX'. Regex przepuszcza wartosc bez zmian, QA4
                tez (niepusta), wiec zgloszenie realnej brytyjskiej firmy idzie do
                VIES/dalszych dzialow z krajem, ktorego nie ma w normie —
                weryfikacja VAT zwraca blad albo brak wyniku, a lead zostaje oznaczony
                jako niezweryfikowany mimo poprawnych danych klienta.
        naprawa: Porownac kod z lista dopuszczalnych kodow (pelna ISO 3166-1 alpha-2 albo minimum lista panstw UE obslugiwanych przez VIES); wartosc spoza listy traktowac jak brak (fallback PL) i odnotowac w wyniku, ze kod zostal odrzucony.

  [1.25] Domyslne 'PL' jest ustawiane bez sprawdzenia, czy NIP faktycznie jest polski, i bez sladu, ze wartosc jest zgadnieta — dzial zglasza kraj jako ustalony fakt.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:36
        status: prawdopodobne
        skutek: Niemiecka firma wypelnia formularz podajac VAT 'DE811907980', ale pole
                country nie jest wyslane (np. ukryte/puste w danym wariancie
                formularza). Dzial 4 ustawia country='PL', QA4 przepuszcza, a
                weryfikacja VAT UE dostaje pare PL + numer niemiecki -> negatywny
                wynik i lead odrzucony/oznaczony jako niepoprawny mimo poprawnych
                danych. Nikt w dalszym procesie nie odrozni 'PL z formularza' od 'PL
                domyslnego'.
        naprawa: Przed fallbackiem odczytac prefiks numeru VAT/NIP z kontekstu i uzyc go jako kraju, gdy jest to dwuliterowy kod ISO; niezaleznie od tego dolozyc do wyniku znacznik (np. 'country_source' => 'form|vat_prefix|default'), zeby dzialy zalezne wiedzialy, ze wartosc jest domyslna.

  [1.25] Off-by-one: agent 5.3 czyta licznik JUŻ zawierający bieżące żądanie, więc pipeline odrzuca piąte zgłoszenie, choć deklarowany limit to 5 na okno. Pre-gate i pipeline stosują dwa różne progi (5 vs 4).
        mp-lead-intake/includes/pipeline/departments/class-mp-department-05.php:133
        status: prawdopodobne
        skutek: Ten sam IP wysyła piąte zgłoszenie w oknie. Pre-gate widzi licznik
                4 (< 5) → przepuszcza, inkrementuje do 5, uruchamia pipeline. Agent
                5.3 czyta 5, `5 >= 5` → rate_ok = false → QA5 zwraca fail
                'security_failed'. Legalny, mieszczący się w limicie lead zostaje
                odrzucony, a użytkownik dostaje komunikat o naruszeniu zabezpieczeń
                zamiast oferty. Efektywny limit to 4, nie 5.
        naprawa: Rozdzielić progi: dla odczytu po inkremencie porównywać `> self::LIMIT` (albo dodać metodę over_limit_after_increment()), zostawiając `>=` wyłącznie dla pre-gate liczącego PRZED inkrementem. Alternatywnie przekazać do kontekstu wynik decyzji pre-gate i w agencie 5.3 tylko go odczytywać.

  [1.25] Okno rate-limitu nie jest stałym oknem 60 s: każdy inkrement odświeża czas życia transientu, więc limit działa jak okno przesuwne bez końca i blokuje ruch, który nigdy nie przekroczył 5 zgłoszeń w żadnej minucie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-05.php:151
        status: prawdopodobne
        skutek: Użytkownik (np. biuro poprawiające formularz) wysyła zgłoszenie co
                55 s. Każdy set_transient przedłuża wygaśnięcie o kolejne 60 s,
                więc licznik nigdy nie spada do zera i po ~3,7 minuty (5.
                zgłoszenie) IP jest zablokowany, mimo że w żadnym 60-sekundowym
                oknie nie było więcej niż jedno zgłoszenie. Odrzucenie leada jest
                niezgodne z zadeklarowaną regułą działu.
        naprawa: Trzymać w transiencie parę (licznik, znacznik startu okna) i przy set_transient przekazywać pozostały czas do końca okna: `set_transient( $key, $data, max( 1, $window_start + MINUTE_IN_SECONDS - time() ) )`, resetując licznik po wygaśnięciu okna.

  [1.25] Zgoda RODO (i marketingowa) jest ustalana wylacznie przez rzutowanie na bool wartosci z kontekstu — kazda niepusta wartosc inna niz "0" jest traktowana jako zgoda udzielona. W dziale 6 nie ma bialej listy dopuszczalnych wartosci.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-06.php:53
        status: prawdopodobne
        skutek: Klient formularza/REST wysyla wartosc pola jako lancuch znakow, np.
                `consent_rodo=false` (typowe dla `formData.append('consent_rodo',
                checkbox.checked)`) lub `consent_rodo=off` przy NIEZAZNACZONEJ
                zgodzie. W PHP `(bool) 'false'` i `(bool) 'off'` to `true`, wiec 6.2
                zwraca `consent_rodo => 1`, `rodo_ok => true`, agent 6.3 wpisuje
                `consent_rodo_at`, a bramka QA6 przepuszcza lead. W BD-3 powstaje
                dowod zgody (art. 7 RODO) dla osoby, ktora zgody nie zaznaczyla.
        naprawa: Znormalizowac wejscie zamiast rzutowac: `$raw = strtolower( trim( (string) $context->get( 'consent_rodo', '' ) ) ); $rodo = in_array( $raw, array( '1', 'true', 'yes', 'on' ), true );` — analogicznie w agencie 6.1 dla `consent_marketing`.

  [1.25] `consent_version` jest stala zaszyta w kodzie ('1.0'), wiec zapisywany dowod zgody nie identyfikuje faktycznej tresci klauzuli, na ktora uzytkownik wyrazil zgode.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-06.php:69
        status: prawdopodobne
        skutek: Administrator zmienia tresc klauzuli zgody na formularzu (np. dodaje
                nowy cel przetwarzania), nie ruszajac kodu wtyczki. Leady zebrane po
                zmianie dostaja `consent_version = '1.0'`, identycznie jak leady
                sprzed zmiany. Przy sporze lub kontroli zapis wskazuje na tresc,
                ktorej dana osoba nigdy nie widziala — status w BD nie odpowiada
                temu, co naprawde sie stalo, a rozroznienie obu grup leadow jest
                niemozliwe.
        naprawa: Pobierac wersje ze zrodla, ktore zmienia sie razem z trescia: `$version = (string) apply_filters( 'mp_consent_version', get_option( 'mp_consent_version', self::CONSENT_VERSION ) );`, a stala zostawic jako fallback; wersjonowac przy kazdej zmianie tresci klauzuli.

  [1.25] QA Agent 4 jest tautologiczny — obie jego kontrole nie moga zawiesc dla danych wyprodukowanych przez agentow 4.1/4.2, wiec brama zawsze zwraca base_prices_ok=true niezaleznie od tego, czy wycena jest prawdziwa.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:137
        status: prawdopodobne
        skutek: Wycena z ustalen 1 i 2 (pozycja za 0 groszy z powodu brakujacej ceny)
                przechodzi przez brame: subtotal >= 0, wszystkie wartosci sa intami,
                suma kontrolna zgadza sie sama ze soba -> QA zwraca
                ok('base_prices_ok' => true). Status dzialu deklaruje udana 'kontrole
                zero-float' i poprawne ceny bazowe, choc dane sa merytorycznie zle.
                Tak samo przejdzie przyszla regresja do float wewnatrz to_grosze(), bo
                rzutowanie (int) zamaskuje ja przed kontrola is_int().
        naprawa: Kontrola musi porownywac wynik z niezaleznym zrodlem, a nie z samym soba: sprawdzic count($lines) === count($items), odrzucic pozycje z unit_grosze <= 0 przy niepustej cenie zrodlowej, i przeliczyc subtotal z surowych prices[] (druga, niezalezna sciezka) zamiast sumowac ponownie te same line_grosze.

  [1.25] Deklarowana odtwarzalność rabatu nie jest osiągalna: w kodzie istnieje tylko JEDEN słownik reguł (`const RULES`) opatrzony etykietą `RULES_VERSION`, nie ma katalogu wersji, a wynik działu zapisuje jedynie `rule_id` + `amount_grosze` (bez `percent`).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-05.php:29
        status: prawdopodobne
        skutek: Oferta #100: wariant `partner`, 60 szt., subtotal 100000 gr → R-02
                10% → 1000... (10000 gr), zapisane `rules_version='v1'`. Później
                reguły zmieniane na v2 (R-02 = 12%) przez edycję tej samej stałej
                `RULES`. Przy reklamacji/audycie ponowne przeliczenie tego samego
                koszyka „wg v1” jest niemożliwe — słownik v1 już nie istnieje
                w kodzie, a z oferty da się odczytać tylko `rule_id='R-02'` i
                kwotę. Wynik: rozbieżność kwoty rabatu na dokumencie i przy
                weryfikacji, bez możliwości rozstrzygnięcia która była poprawna.
        naprawa: Albo trzymać słownik jako mapę wersjonowaną (`RULES = array( 'v1' => array(...), 'v2' => array(...) )`) i dobierać po `RULES_VERSION`, albo (taniej) dopisać do wpisu w `discounts` pola `percent`, `min_qty` i `method` faktycznie użytej reguły, żeby kwota dała się odtworzyć z samej oferty.

  [1.25] Brak kontroli spójności między `total_qty` a `subtotal_grosze`: gdy klucz `items` nie trafi do kontekstu (albo ma inną nazwę/kształt), `total_qty` wychodzi 0, dobierana jest reguła catch-all R-00, a dział kończy się SUKCESEM z rabatem 0 zł mimo niezerowego subtotalu.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-05.php:76
        status: prawdopodobne
        skutek: Kontekst po Dziale 4 zawiera `subtotal_grosze = 100000`, ale pozycje
                pod innym kluczem niż `items` (albo z `quantity`/`ilosc` zamiast
                `qty`). Wariant = `partner`. `total_qty = 0` → jedyna pasująca
                reguła to R-00 → rabat 0 gr. Agent 5.2 (0 > 30000 = false) → OK,
                QA5 widzi komplet kluczy → OK, bramka akceptuje. Oferta dla partnera
                wychodzi bez należnego rabatu 5–10%, a żaden agent, krytyk ani
                bramka tego nie sygnalizuje.
        naprawa: W Agencie 5.1 wykryć stan sprzeczny: `if ( $subtotal_grosze > 0 && 0 === $total_qty )` → `MP_OB_Result::fail( ..., 'missing_cart_qty' )` zamiast cichego R-00. Reguła catch-all ma obsługiwać nieznany wariant, nie brak koszyka.

  [1.25] Agent 8.1 liczy kandydata numeru ZAWSZE, także gdy Dział 8 pracuje w trybie korekty, i twardo przerywa dział przy nieparsowalnym `last_number` — mimo że w korekcie ta wartość nie jest w ogóle używana.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-08.php:57
        status: prawdopodobne
        skutek: W bazie istnieje ostatnia oferta roku z numerem spoza formatu
                OF/RRRR/NNNNNN (numer wpisany ręcznie, zaimportowany ze starego
                systemu, np. 'OF/2026/12' albo 'OF/2026/000123-KOR'). Dział 2 wkłada
                ten napis do `numbering['last_number']`. Użytkownik chce zrobić
                korektę zupełnie innej, poprawnej oferty (`existing_offer_number`
                ustawione). Agent 8.1 zwraca fail 'malformed_last_number', dział 8
                staje, korekta nie powstaje — mimo że numer korekty jest znany i
                niezależny od `last_number`. Blokada dotyczy wszystkich korekt w
                danym roku aż do ręcznej naprawy rekordu w bazie.
        naprawa: Uzależnić parę 8.1 od trybu: albo przenieść rozpoznanie korekty przed Agenta 8.1 (i przy `existing_offer_number` pomijać liczenie kandydata), albo w Agencie 8.1 przy nieparsowalnym `last_number` zwracać ok z `candidate_offer_number => null` i przenieść twardy fail do Agenta 8.2 — tam, gdzie kandydat jest naprawdę potrzebny (gałąź `'' === $candidate` już taki fail ma: 'missing_candidate_number').

  [1.26] Ostrzezenie w adminie stawia jedna, twarda diagnoze ("Twoj motyw nie rejestruje standardowego menu WordPressa"), a flaga OPTION_MENU_OK=0 jest ustawiana takze w sytuacjach, gdy motyw menu rejestruje. add_to_menus() zwraca false rowniez wtedy, gdy get_nav_menu_locations() zwraca niepusta tablice z samymi zerami (lokalizacje zarejestrowane, ale admin nie przypisal do nich zadnego menu) albo gdy kazde wp_update_nav_menu_item() zwrocilo blad/0. Komunikat mowi wowczas nieprawde o przyczynie i kieruje do zlego dzialania naprawczego.
        mp-lead-intake/includes/class-mp-page.php
        status: prawdopodobne
        skutek: Motyw robi register_nav_menu('primary'), ale admin nie przypisal menu
                do tej lokalizacji, wiec get_nav_menu_locations() zwraca
                array('primary' => 0). Petla pomija menu_id 0, add_to_menus() zwraca
                false, OPTION_MENU_OK=0. Admin widzi komunikat, ze jego motyw nie
                obsluguje menu WordPressa, wiec zamiast wejsc w Wyglad > Menu i
                przypisac menu do lokalizacji (co rozwiazuje problem w 10 sekund i
                daje link w prawdziwej nawigacji), dokleja link recznie lub zaczyna
                szukac winy w motywie. Fallback HTML dalej pracuje buforem na kazdej
                stronie frontendu.
        naprawa: Rozroznic przyczyny w add_to_menus() (zwracac np. 'no_locations' / 'unassigned' / 'insert_failed' zamiast bool) i w maybe_admin_notice() dobierac tresc: dla lokalizacji bez przypisanego menu poinformowac o Wyglad > Menu z linkiem do nav-menus.php, dla realnego braku rejestracji menu zostawic obecny tekst.

  [1.26] Galaz porazki tworzenia pod-strony nie generuje zadnego komunikatu. Gdy wp_insert_post() zwroci 0 lub WP_Error, create() nie zapisuje ani OPTION, ani OPTION_MENU_OK, a maybe_admin_notice() czyta OPTION_MENU_OK z domyslna wartoscia '1' (czyli 'sukces') i milczy. Jedyny komunikat, jaki widzi czlowiek, to wygenerowany przez WordPressa 'Wtyczka wlaczona' — twierdzenie o sukcesie w sytuacji, w ktorej kluczowy wymog wtyczki nie zostal spelniony.
        mp-lead-intake/includes/class-mp-page.php
        status: prawdopodobne
        skutek: Podczas aktywacji inna wtyczka lub filtr blokuje wstawienie wpisu
                (wp_insert_post_empty_content / blad zapisu do bazy), wp_insert_post()
                zwraca WP_Error. Wtyczka aktywuje sie 'poprawnie', admin dostaje
                standardowy komunikat o wlaczeniu wtyczki i nie dowiaduje sie o
                niczym; strona /zapytanie-ofertowe nie istnieje, w menu nie ma linku,
                klienci wchodzacy z reklamy trafiaja na 404 i zadne zapytanie ofertowe
                nie wplywa, dopoki ktos przypadkiem tego nie zauwazy.
        naprawa: W galezi porazki create() ustawic osobna flage (np. mp_lead_intake_page_error z komunikatem z WP_Error->get_error_message()) i wyswietlic w adminie notice notice-error z konkretna instrukcja: powod bledu + polecenie recznego utworzenia strony z shortcode [mp_lead_intake_form].

  [1.26] Throttling powiadomień o wyjątkach używa jednego globalnego klucza transientu dla WSZYSTKICH działów, więc wyjątek w innym dziale w ciągu 15 minut nie wygeneruje żadnego maila — admin dostaje jeden komunikat wskazujący konkretny dział i nie dowiaduje się o pozostałych awariach.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:97
        status: prawdopodobne
        skutek: O 10:00 wyjątek w dziale 3 (lead A) wysyła mail „Nieoczekiwany
                wyjątek w pipeline (dział 3)”. O 10:05 wyjątek w dziale 7 dla
                leada B — get_transient() zwraca prawdę, funkcja kończy się przed
                wp_mail(). Administrator widzi wyłącznie komunikat o dziale 3 i w
                dobrej wierze uznaje, że awaria dotyczyła jednego działu i jednego
                leada; lead B ginie bez powiadomienia.
        naprawa: Zbudować klucz throttlingu z numeru działu i klasy wyjątku, np. $throttle_key = 'mp_notify_exception_' . (int) $dept_num . '_' . md5( get_class( $e ) ); alternatywnie zliczać stłumione zdarzenia w transiencie i dopisywać ich liczbę do treści kolejnego maila.

  [1.26] Treść i temat obu maili do administratora nie zawierają lead_id ani request_id, mimo że oba są zapisywane w wierszu logu BD-3. Odbiorca nie ma jak powiązać powiadomienia z konkretnym leadem ani wpisem w logu i nie dostaje żadnej wskazówki, co zrobić dalej.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:138
        status: prawdopodobne
        skutek: Dwa formularze od różnych klientów wpadają w tym samym dziale w
                odstępie kilku minut. Przez throttling administrator dostaje jeden
                mail „Pipeline zatrzymany w dziale 4 (walidacja). Kod: ...”. Nie
                wie, którego klienta dotyczy awaria ani którego wiersza
                wp_mp_activity_log szukać, więc ręczne dokończenie procesu
                (kontakt z klientem) nie następuje — zgłoszenie klienta przepada,
                a klient nie otrzymuje oferty.
        naprawa: Przekazać $context do notify_admin() i dopisać do treści lead_id oraz request_id plus jedno zdanie z instrukcją (np. „Sprawdź wpis w Logu aktywności o tym request_id i dokończ obsługę leada ręcznie”); w log_exception() dodać te same pola do maila.

  [1.26] Do treści maila wstawiany jest wynik wp_json_encode() bez sprawdzenia, czy nie zwrócił false. Przy nieprawidłowym UTF-8 w błędach administrator dostaje komunikat z pustym polem „Błędy:”, a w BD-3 kolumna meta_json zapisuje się jako pusty ciąg — nie zostaje żaden ślad, co się zepsuło.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:143
        status: prawdopodobne
        skutek: Klient wkleja do pola formularza tekst z uszkodzonym kodowaniem
                (skopiowany z pliku w windows-1250); walidator zwraca ten fragment w
                treści błędu. wp_json_encode() zwraca false, więc administrator
                dostaje mail „Pipeline zatrzymany w dziale 4 (walidacja). Kod: ...
                Błędy:” — komunikat urywa się bez treści, a w logu BD-3
                meta_json jest puste, więc nie da się odtworzyć, które pole
                odrzucono i skontaktować się z klientem.
        naprawa: Zapisać wynik do zmiennej i zabezpieczyć fallbackiem, np. $errors_json = wp_json_encode( $result->get_errors() ); if ( false === $errors_json ) { $errors_json = wp_json_encode( array_map( 'sanitize_text_field', ... ) ) ?: '(nie udało się zakodować szczegółów błędu)'; } — i użyć zmiennej zarówno w mailu, jak i w kolumnie meta_json.

  [1.26] Lead z krajem innym niż PL dostaje komunikat 'Niepoprawna suma kontrolna NIP', mimo że jego numer VAT UE nigdy nie miał spełniać polskiego algorytmu; QA3 dodatkowo zatrzymuje proces.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:71
        status: prawdopodobne
        skutek: Klient z Niemiec wpisuje prawidłowy DE123456789 (9 cyfr) i
                country=DE. checksum_valid zwraca false już na preg_match
                '/\A\d{10}\z/', człowiek widzi komunikat twierdzący, że suma
                kontrolna jego NIP jest niepoprawna, choć numer jest poprawny — i
                nie ma jak przejść dalej, bo QA3 blokuje lead. Klient poprawia numer
                w kółko, bo komunikat sugeruje literówkę.
        naprawa: Uzależnić 3.1 od kraju: dla country != 'PL' nie uruchamiać polskiego algorytmu i nie ustawiać błędu 'nip' (albo ustawiać 'nip_valid' => true/nieustalony i zdać się na 3.2/VIES), a komunikat dla PL doprecyzować, np. 'Numer NIP (10 cyfr) wygląda na niepoprawny — sprawdź, czy nie ma literówki'.

  [1.26] Puste pole NIP daje komunikat o niepoprawnej sumie kontrolnej zamiast informacji, że pole jest wymagane; komunikat nie mówi, co użytkownik ma zrobić.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:71
        status: prawdopodobne
        skutek: Klient wysyła formularz nie wypełniając pola NIP (albo pole nie
                zostało przekazane do kontekstu). Zamiast 'Podaj NIP' dostaje
                'Niepoprawna suma kontrolna NIP' — komunikat o wartości, której
                nie wpisał, i bez wskazówki, co poprawić. Pracownik odczytujący
                ten sam błąd w rekordzie leada wnioskuje, że klient podał zły
                numer, zamiast że numeru w ogóle nie ma.
        naprawa: W run() rozdzielić przypadki: '' === trim( $nip ) → 'errors' => array( 'nip' => 'Pole NIP jest wymagane — wpisz 10 cyfr NIP-u firmy' ); pozostałe → komunikat o niepoprawnym numerze z instrukcją, co sprawdzić.

  [1.26] Krytyk 3.2 orzeka kategorycznie 'VIES: numer VAT niepoprawny' także wtedy, gdy podstawą jest tylko wpis z 24-godzinnego cache; komunikat nie podaje żadnej ścieżki wyjścia.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:385
        status: prawdopodobne
        skutek: Firma świeżo zarejestrowana do VAT UE (VIES jeszcze jej nie widzi)
                wysyła formularz — wynik false trafia do transienta na 24 h. Gdy
                następnego dnia rano VIES już potwierdza numer, klient próbuje
                ponownie i przez resztę doby dostaje identyczne, kategoryczne 'VIES:
                numer VAT niepoprawny', mimo że jego numer jest już poprawny.
                Komunikat nie mówi ani kiedy spróbować ponownie, ani z kim się
                skontaktować, więc klient rezygnuje.
        naprawa: Nie cache'ować wyniku negatywnego na dobę (np. krótki TTL rzędu 1 h dla false) oraz złagodzić i uzupełnić komunikat: 'Nie udało się potwierdzić numeru VAT UE w VIES. Sprawdź numer i kraj; jeśli firma zarejestrowała się niedawno, spróbuj ponownie za kilka godzin lub napisz na <adres kontaktowy>.'

  [1.26] Gałąź obsługi nieudanego UPDATE zwraca komunikat twierdzący, że oferta 'była już zatwierdzona — nic się nie zmieniło' (waga 'info'), także wtedy, gdy zapis w ogóle się nie powiódł.
        mp-offer-builder/includes/class-mp-offer-builder-approval.php:147
        status: prawdopodobne
        skutek: Pracownik klika „Zatwierdź” dla poprawnego szkicu z numerem i
                PDF-em, ale UPDATE kończy się błędem bazy (deadlock, zerwane
                połączenie, blokada tabeli, brak kolumny po nieudanej migracji).
                Oferta zostaje w statusie draft, dziennik nie dostaje wpisu,
                `mp_offer_approved` nie wychodzi, plugin 3 nic nie wysyła. Pracownik
                widzi niebieski komunikat informacyjny mówiący, że oferta już
                była zatwierdzona i nic się nie zmieniło, więc uznaje sprawę za
                załatwioną i nie ponawia próby — klient nigdy nie dostaje oferty.
        naprawa: Rozróżnić `false` (błąd zapytania) od `0` (wiersz nie pasował do WHERE): przy `false === $changed` zwrócić osobny kod błędu, np. 'db_error', z komunikatem klasy 'error' mówiącym, że zatwierdzenie się nie powiodło i trzeba spróbować ponownie; kod 'already_approved' zostawić wyłącznie dla `0 === (int) $changed`.

  [1.26] Komunikat sukcesu zapewnia pracownika, że „moduł sprzedażowy przejmuje wysyłkę do klienta”, choć kod tego nie sprawdza — `do_action()` nie zwraca informacji, czy ktokolwiek odebrał zdarzenie.
        mp-offer-builder/includes/class-mp-offer-builder-approval.php:251
        status: prawdopodobne
        skutek: Plugin 3 jest wyłączony (albo oferta powstała ręcznie i ma
                `lead_id = 0`, więc po stronie procesu sprzedażowego nie ma do czego
                jej podpiąć). Pracownik zatwierdza ofertę i dostaje zielony
                komunikat, że moduł sprzedażowy przejmuje wysyłkę. Nikt niczego
                nie wysyła, a pracownik — poinformowany o sukcesie wysyłki — nie
                wysyła oferty sam ani nie sprawdza stanu. Klient nie dostaje oferty i
                nikt tego nie zauważa.
        naprawa: Rozdzielić komunikat na fakt potwierdzony i obietnicę: zawsze potwierdzać samo zatwierdzenie („Oferta zatwierdzona.”), a zdanie o przejęciu wysyłki dodawać tylko po sprawdzeniu `has_action( self::HOOK )` (i, dla ofert bez leada, zastąpić je informacją, że ofertę trzeba wysłać ręcznie).

  [1.26] E-mail do administratora o zatrzymaniu pipeline'u nie zawiera zadnego identyfikatora oferty ani zadania (offer_id / request_id), mimo ze log_failure() ma je w $context i zapisuje do BD-2. notify_admin() dostaje tylko $department i $result, wiec odbiorca nie wie, ktorej oferty/klienta dotyczy awaria ani co ma dalej zrobic.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:135
        status: prawdopodobne
        skutek: Rownolegle generowane sa oferty dla dwoch klientow. Dzial 4 zatrzymuje
                pipeline dla jednej z nich. Administrator dostaje maila "Pipeline
                zatrzymany w dziale 4 (pricing). Kod: ... Bledy: [...]" i nie jest w
                stanie ustalic, ktora oferta utknela ani ktorego klienta obsluzyc —
                musi recznie przeszukiwac wp_mp_ob_offer_activity_log, a e-mail nie
                mowi nawet, ze tam nalezy zajrzec.
        naprawa: Przekazac $context do notify_admin() i dopisac w tresci offer_id oraz request_id plus jedno zdanie z instrukcja (np. link do ekranu oferty / wpisu w logu aktywnosci).

  [1.26] W log_exception() klucz throttlingu jest jeden globalny ('mp_ob_notify_exception'), a nie per dzial/oferta — przez 15 minut po pierwszym wyjatku administrator nie dostanie zadnego maila o kolejnych wyjatkach, nawet z innych dzialow i innych ofert.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:94
        status: prawdopodobne
        skutek: O 10:00 wyjatek w dziale 3 (jednorazowa awaria zewnetrznego API)
                wysyla maila. O 10:02 inny wyjatek w dziale 7 wywraca generowanie
                ofert dla wszystkich kolejnych zgloszen. Administrator dostal tylko
                pierwszego maila, o dziale 3, i przez 15 minut ma falszywy obraz:
                mysli, ze problem dotyczy jednego dzialu, podczas gdy caly pipeline
                jest zepsuty.
        naprawa: Wlaczyc numer dzialu (i ewentualnie klase wyjatku) do klucza transienta, np. 'mp_ob_notify_exception_' . (int) $dept_num, albo zliczac stlumione zdarzenia i dopisywac ich liczbe do nastepnego maila.

  [1.26] Throttling per dzial w notify_admin() tlumi powiadomienia o awariach ROZNYCH ofert — druga i kolejne oferty, ktore w ciagu 15 minut poleglyby w tym samym dziale, nie generuja zadnego maila, a wyslany wczesniej mail nie sygnalizuje, ze cos zostalo pominiete.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:123
        status: prawdopodobne
        skutek: W ciagu jednej minuty formularz wypelnia 10 klientow i dla wszystkich
                dzial 5 zwraca STOP (np. brak cennika). Administrator dostaje jednego
                maila o jednym bledzie w dziale 5 i uznaje sprawe za pojedynczy
                incydent; pozostalych 9 ofert nigdy nie powstaje, a e-mail nie daje
                zadnego sygnalu, ze dotyczy wiecej niz jednego przypadku.
        naprawa: Zliczac w transiencie liczbe wystapien w oknie throttlingu i dopisywac ja do tresci nastepnego maila (np. "w ciagu ostatnich 15 min pominieto N podobnych bledow") albo throttlowac per (dzial + offer_id).

  [1.26] Ostrzeżenie o nginx sprawdza meta `mp_ob_nginx_notice_dismissed`, ale wypisany notice nie ma klasy `is-dismissible` ani żadnego mechanizmu (przycisk, AJAX, JS) ustawiającego tę metę — komunikat jest nieusuwalny i wraca na każdym ekranie wp-admin, także po poprawnym skonfigurowaniu nginx.
        mp-offer-builder/mp-offer-builder.php:129
        status: prawdopodobne
        skutek: Administrator serwisu na nginx wkleja podany blok `location` do
                konfiguracji i przeładowuje serwer. Przy kolejnym wejściu do
                wp-admin (i każdym następnym, bez możliwości zamknięcia) nadal
                widzi ostrzeżenie 'Pliki .htaccess NIE chronią katalogu ofert PDF'.
                Wniosek, jaki wyciąga: konfiguracja nie zadziałała, oferty PDF
                wciąż są niezabezpieczone — albo zaczyna ignorować wszystkie
                ostrzeżenia tej wtyczki, w tym te istotne.
        naprawa: Dodać klasę `is-dismissible` do diva oraz obsługę zapisu `mp_ob_nginx_notice_dismissed` (np. link/nonce `?mp_ob_dismiss_nginx=1` obsłużony w admin_init lub wp_ajax_ + skrypt na zdarzenie click.wp-dismiss-notice), albo usunąć martwy warunek get_user_meta(), żeby kod nie sugerował istnienia zamykania, którego nie ma.

  [1.26] Okno licznika odnawia TTL przy kazdej wysylce, wiec tresc alarmu do administratora („po %d wiadomosciach w ciagu godziny”) potrafi byc nieprawdziwa.
        mp-sales-workflow/includes/class-mp-sw-mailer.php:168
        status: prawdopodobne
        skutek: Sklep pracuje normalnie i wysyla ok. 60 powiadomien na godzine bez
                zadnej godzinnej przerwy. Po ~3,5 godzinach licznik dobija do 200,
                bezpiecznik zatrzymuje kolejke, a administrator dostaje maila
                twierdzacego, ze bylo „200 wiadomości w ciągu godziny”, i
                polecenie szukania zapetlonego zrodla zdarzen. Administrator szuka
                petli, ktorej nie ma (w zadnej godzinie nie bylo wiecej niz 60
                wiadomosci), a w tym czasie kolejka stoi i klienci nie dostaja
                powiadomien o ofertach.
        naprawa: Albo trzymac w transiencie znacznik startu okna i przy odnawianiu przekazywac pozostaly czas zycia (okno staloczasowe), albo zmienic tresc komunikatu na zgodna z faktycznym dzialaniem, np. „po %d wiadomościach w bieżącym oknie (licznik zeruje się po godzinie bez wysyłek)” i podac czas rozpoczecia okna.

  [1.26] Alarm o zatrzymaniu kolejki jest jednorazowy i wysylany „na slepo” — jesli `wp_mail()` zawiedzie, zaden czlowiek nigdy nie zobaczy informacji, ze kolejka stoi.
        mp-sales-workflow/includes/class-mp-sw-mailer.php:202
        status: prawdopodobne
        skutek: Bezpiecznik zadziala w momencie, gdy transport poczty jest chwilowo
                niedostepny (SMTP odrzuca polaczenie) albo gdy `admin_email` jest
                nieprawidlowy — `wp_mail()` zwraca false lub funkcja konczy sie
                wczesniej na `if ( ! is_email( $admin ) ) { return; }`. Kolejka
                pozostaje zatrzymana bezterminowo (`OPTION_HALTED` znika tylko przez
                `resume()`), zaden mail juz nie wyjdzie, a pracownik nie dostaje
                zadnego komunikatu — klienci przestaja dostawac powiadomienia o
                ofertach i nikt sie o tym nie dowiaduje poza wpisem w logu.
        naprawa: Ustawiac ALERT_KEY dopiero po udanej wysylce (`if ( wp_mail( ... ) ) { set_transient( ... ); }`), a nieudana probe zapisywac do logu; dodatkowo pokazac stan zatrzymania jako trwaly komunikat w panelu (admin notice na stronie „Procesy sprzedażowe”), zeby informacja nie zalezala wylacznie od dostarczenia jednego maila.

  [1.26] Kasownik danych osobowych rdzenia raportuje wynik wylacznie na podstawie liczby zmienionych wierszy; blad zapytania (`false`) po rzutowaniu `(int)` staje sie 0 i jest nieodroznialny od „nic nie bylo do zmiany", a `messages` jest zawsze puste — administrator dostaje komunikat „nie znaleziono danych osobowych" mimo nieudanego usuniecia.
        mp-sales-workflow/includes/class-mp-sw-privacy.php:213
        status: prawdopodobne
        skutek: Na instalacji, gdzie tabela powiadomien nie zostala
                utworzona/zmigrowana (dokladnie ta klasa awarii, ktora komentarz
                dokumentuje dla kolumny `audience`), oba zapytania zwracaja `false` ->
                `$changed = 0` -> `$removed = false`. Ekran Narzedzia -> Usun dane
                osobowe pokazuje zadanie jako zakonczone, bez zadnego komunikatu i bez
                informacji o bledzie. Administrator zamyka zgloszenie RODO jako
                zrealizowane, a adres klienta nadal stoi w `client_email` i w kolejce
                powiadomien.
        naprawa: Po kazdym zapisie sprawdzac `false === $result || '' !== $wpdb->last_error`; w takim przypadku zwrocic z `erase_by_email()` `'items_retained' => true` oraz `messages` z konkretna instrukcja dla administratora (np. „Nie udalo sie usunac danych z procesow sprzedazowych — sprawdz stan tabel wtyczki i powtorz zadanie"), zamiast pustej tablicy.

  [1.26] `items_retained` jest zapisane na sztywno jako `false`, a petla po leadach ustawia `items_removed` na `true`, gdy powiedzie sie CHOCBY JEDEN proces — przy czesciowej porazce czlowiek dostaje komunikat, ze usunieto wszystko i nic nie zostalo zachowane.
        mp-sales-workflow/includes/class-mp-sw-privacy.php:205
        status: prawdopodobne
        skutek: Klient ma dwa procesy z tym samym adresem. Pierwszy anonimizuje sie
                poprawnie, przy drugim `SELECT id ... WHERE lead_id = %d` nie znajduje
                wiersza (np. `lead_id` rozjechany po imporcie) albo UPDATE konczy sie
                bledem -> `anonymize_lead()` zwraca 0. `$removed` jest juz `true`,
                wiec raport rdzenia dla zadania usuniecia mowi „dane usuniete, nic
                nie zachowano", a w drugim procesie adres i nazwa klienta zostaja.
                Administrator (i klient, ktory dostaje potwierdzenie realizacji
                zadania) sa wprowadzeni w blad.
        naprawa: Zliczac osobno leady udane i nieudane; gdy `anonymize_lead()` zwroci 0 dla leada znalezionego po adresie, ustawiac `'items_retained' => true` i dopisac do `messages` liste identyfikatorow procesow, ktorych nie udalo sie zanonimizowac.

  [1.26] Anonimizacja czysci `subject` i `body` w kolejce, ale zostawia status i licznik prob, wiec zaplanowana/ponawiana wysylka do klienta pojdzie dalej — jako wiadomosc z pustym tematem i pusta trescia.
        mp-sales-workflow/includes/class-mp-sw-privacy.php:139
        status: prawdopodobne
        skutek: Powiadomienie do klienta czeka w kolejce ze statusem
                oczekujacym/nieudanym i pozostalymi probami. Administrator wykonuje
                zadanie usuniecia danych. Przy najblizszym przebiegu kolejki mechanizm
                ponowienia bierze ten wiersz i wysyla wiadomosc z pustym tematem i
                pusta trescia na `deleted+<id>@invalid`, co konczy sie bledem
                doreczenia. Pracownik widzi na liscie powiadomien pozycje z pustym
                tematem i bledem wysylki, bez informacji, ze to skutek anonimizacji
                — moze uznac to za awarie systemu i probowac wysylac recznie.
        naprawa: W tym samym UPDATE ustawic wiersze klienckie na status koncowy (np. `cancelled`/`anonymized`) i wyzerowac pozostale proby, a w widoku kolejki pokazywac dla nich jawna etykiete „dane usuniete" zamiast pustego tematu.

  [1.26] Temat i tresc wiadomosci trafiaja do wp_mail() bez sprawdzenia, czy po czyszczeniu nie sa puste — klient moze dostac e-mail bez tematu i/lub bez tresci.
        mp-sales-workflow/includes/class-mp-sw-queue.php:148
        status: prawdopodobne
        skutek: Wiersz kolejki, ktorego kolumna subject jest pusta lub po
                header_safe() zostaje pusta (same znaki sterujace / nowe linie, wpis
                ze starszej wersji kodu, reczna zmiana w bazie). Kod nie ma zadnej
                galezi na ten przypadek, wiec wysyla wiadomosc dalej: klient dostaje
                maila z pustym tematem (w wielu klientach pocztowych pokazywanym jako
                '(brak tematu)'), przy pustym body — maila calkowicie bez tresci.
                Wiersz zostaje oznaczony jako 'sent', wiec nikt po stronie sklepu nie
                dowie sie, ze klient dostal pusty komunikat.
        naprawa: Przed wp_mail() sprawdzic '' === trim( $subject ) oraz '' === trim( (string) $row['body'] ). Przy pustym temacie podstawic staly tekst zapasowy (np. nazwa sklepu + rodzaj powiadomienia); przy pustej tresci nie wysylac wcale — ustawic status FAILED z komunikatem mowiacym, ktorego wiersza dotyczy i ze trzeba go zakolejkowac ponownie.

  [1.26] Komunikat bledu pobierany jest z globalnego $phpmailer->ErrorInfo, ktorego ten kod nigdy nie zeruje — nieudany wiersz moze dostac tresc bledu od POPRZEDNIEGO wiersza, razem z adresem e-mail innego klienta.
        mp-sales-workflow/includes/class-mp-sw-queue.php:225
        status: prawdopodobne
        skutek: W jednym przebiegu crona wiersz A do klienta-a@example.com odpada na
                SMTP i PHPMailer zostawia w ErrorInfo tekst z adresem klienta A.
                Kolejny wiersz B (inny lead, inny odbiorca) tez zwraca false, ale bez
                ustawienia ErrorInfo — tak dzieje sie, gdy wysylke ucina filtr
                'pre_wp_mail'/'wp_mail' (typowe przy wtyczkach SMTP). last_error()
                zwraca wtedy stary tekst od wiersza A i zapisuje go w kolumnie
                last_error wiersza B oraz przekazuje do do_action(
                'mp_sw_notification_failed' ). Pracownik otwierajacy powiadomienie
                klienta B widzi komunikat z adresem e-mail klienta A i diagnozuje zla
                przyczyne — poprawia dzialajacy adres zamiast rzeczywistej blokady
                wysylki.
        naprawa: Przed kazdym wp_mail() wyzerowac globalny $phpmailer->ErrorInfo (jesli obiekt istnieje), albo — lepiej — przechwytywac blad wylacznie przez hook 'wp_mail_failed' zarejestrowany na czas jednej wysylki i uzywac tresci z WP_Error tego konkretnego wywolania, a przy braku danych podstawiac komunikat zapasowy.

  [1.26] Galaz nieprawidlowego odbiorcy oznacza wiersz jako FAILED, ale NIE odpala do_action( 'mp_sw_notification_failed' ) — o niewyslanym powiadomieniu nikt sie nie dowiaduje.
        mp-sales-workflow/includes/class-mp-sw-queue.php:150
        status: prawdopodobne
        skutek: Klient wpisuje w formularzu adres z literowka (np.
                'jan.kowalski@wp,pl'). Wiersz kolejki od razu wpada w ta galaz: status
                FAILED, koniec. Zaden odbiorca hooka 'mp_sw_notification_failed'
                (powiadomienie w panelu, mail do handlowca, wpis w logu) nie zostaje
                wywolany, wiec handlowiec nie dostaje zadnego sygnalu i pozostaje w
                przekonaniu, ze klient dostal potwierdzenie — sprawa cichnie do
                momentu, az klient sam zadzwoni. Ta sama porazka przy blednym SMTP
                komunikat by wygenerowala, wiec zachowanie jest niespojne wlasnie w
                przypadku najczestszym (zly adres).
        naprawa: W galezi ! is_email( $to ) odpalic ten sam do_action( 'mp_sw_notification_failed', $id, $error ) z komunikatem o blednym adresie, zeby oba sposoby porzucenia wiersza dawaly czlowiekowi identyczny sygnal.

  [1.26] Szablon wysyłany do KLIENTA (TPL_OFFER_SENT) wstawia {{offer_number}}, {{link}} i {{salesman_name}} bez jakiegokolwiek kontraktu wymagalności — plik nie definiuje wartości domyślnych ani listy pól obowiązkowych, a jedyny walidator (find_gaps) sprawdza wyłącznie obecność klucza 'version' oraz niepustych 'subject'/'body', nigdy zawartości podstawień.
        mp-sales-workflow/includes/class-mp-sw-templates.php:66
        status: prawdopodobne
        skutek: Kontekst wysyłki trafia do renderera z pustym offer_number (oferta
                jeszcze bez nadanego numeru / draft) albo pustym link. Klient
                zewnętrzny dostaje maila o temacie 'Oferta ' i treści 'w
                załączeniu przesyłamy ofertę nr .' oraz 'Dokument:' bez adresu i
                'Pozdrawiamy' bez podpisu — komunikat niekompletny i mylący, bo
                sugeruje załącznik/dokument, którego nie da się wskazać.
        naprawa: Dodać do definicji każdego szablonu klucz np. 'required' => array( 'offer_number', 'link' ) i metodę walidującą kontekst przed kolejkowaniem; przy braku wartości nie wysyłać do klienta, tylko odłożyć wysyłkę i zgłosić brak handlowcowi (wpis w dzienniku wysyłek z powodem).

  [1.26] all() przyjmuje wynik filtra 'mp_sw_templates' po sprawdzeniu wyłącznie typu tablicowego najwyższego poziomu — brak scalenia z defaults(), więc nadpisanie częściowe (np. samego 'subject' bez 'body' albo bez wersji językowej) przechodzi w całości do wysyłki.
        mp-sales-workflow/includes/class-mp-sw-templates.php:107
        status: prawdopodobne
        skutek: Witryna rejestruje filtr, który zwraca array( 'offer_sent' => array(
                'version' => '2.0', 'pl' => array( 'subject' => 'Oferta
                {{offer_number}}' ) ) ) — chcąc zmienić tylko temat. all() zwraca
                ten zestaw dosłownie: klient dostaje maila z pustą treścią (brak
                'body'), a powiadomienia lead_assigned / status_changed / followup_due
                w ogóle nie mają szablonu, więc handlowiec nie dostaje informacji o
                nowym leadzie ani o terminie SLA.
        naprawa: W all() scalać wynik filtra z defaults() rekurencyjnie po kluczach szablonu i języka (odpowiednik wp_parse_args na dwóch poziomach), tak aby nadpisanie częściowe uzupełniało się brakującymi polami wbudowanymi zamiast je kasować.

  [1.26] render() podstawia wartosc pod znacznik bez sprawdzenia, czy nie jest pusta; pusta wartosc kasuje znacznik, wiec siatka bezpieczenstwa unresolved_markers() strukturalnie nie moze jej wykryc (szuka tylko znacznikow, ktore ZOSTALY).
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:137
        status: prawdopodobne
        skutek: Wiersz procesu bez numeru oferty (offer_no = '' lub NULL po stronie
                wtyczki 2). Szablon TPL_OFFER_SENT z tematem 'Twoja oferta
                {{offer_no}} jest gotowa' renderuje sie do 'Twoja oferta  jest
                gotowa'. unresolved_markers() zwraca pusta tablice, bramka
                przepuszcza, klient dostaje maila z dziura w temacie i nie ma jak
                zidentyfikowac oferty.
        naprawa: W render() (albo w bramce przed kolejkowaniem) odrzucac wartosci puste po trim(): zbierac liste kluczy o pustej wartosci i zwracac ja obok tekstu, a Dzial 7 ma na tej liscie konczyc FAIL tak samo, jak na nierozwiazanych znacznikach.

  [1.26] Renderer i detektor znacznikow uzywaja niezgodnych regul dopasowania: render() dopasowuje doslownie `{{klucz}}` (bez spacji, z rozroznianiem wielkosci liter), a MARKER akceptuje `{{ klucz }}` i dowolna wielkosc liter. Znacznik zapisany ze spacjami nigdy nie zostanie podstawiony.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:52
        status: prawdopodobne
        skutek: Redaktor szablonu w rejestrze wpisuje `Dzien dobry {{ client_name }},`
                (ze spacjami, naturalny zapis). render() nie podstawia nic. Albo
                bramka na unresolved_markers() zatrzymuje caly przebieg zmiany statusu
                (powiadomienie o wyslanej ofercie nie wychodzi wcale, a handlowiec
                widzi tylko blad o nierozwiazanym znaczniku), albo — jesli ktoras
                sciezka pomija te bramke — klient dostaje maila z surowym `{{
                client_name }}` w powitaniu.
        naprawa: Podstawiac tym samym wyrazeniem, ktorym sie weryfikuje: `preg_replace_callback( self::MARKER, ... )` z lookupem po `strtolower($m[1])` w znormalizowanej tablicy `$vars`, zamiast str_replace po surowych kluczach.

  [1.26] Jeden globalny status ($status_po_terminie) jest przypisywany WSZYSTKIM zadaniom z $tasks['fire'], mimo ze wyliczono go na podstawie obecnosci pojedynczego szablonu TPL_FOLLOWUP_DUE. Zadania innego typu domykane w tym samym przebiegu dostaja status niezwiazany z ich wlasna wysylka.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:315
        status: prawdopodobne
        skutek: W jednym przebiegu wypadaja dwa zadania roznych typow, np.
                przypomnienie follow-up i zadanie innego rodzaju. Powiadomienie
                follow-up zostaje pominiete przez Agenta 7.2 (niedostarczalny adres
                wewnetrzny), wiec $status_po_terminie = STATUS_UNDELIVERED. Drugie
                zadanie, ktorego wiadomosc poszla poprawnie do kolejki, zostaje w
                interfejsie i dzienniku oznaczone jako „niedostarczone".
                Symetrycznie: gdy follow-up poszedl, a wiadomosc drugiego zadania nie
                powstala, to drugie zadanie pokazuje sie czlowiekowi jako
                „wykonane". W obu kierunkach pracownik widzi status sprzeczny z
                faktyczna wysylka.
        naprawa: Liczyc status wewnatrz petli, per zadanie: dopasowywac wiadomosc z 'notifications'/'skipped_notifications' do konkretnego task_id lub do szablonu odpowiadajacego typowi zadania, zamiast jednej wartosci dla calej listy.

  [1.26] Zabezpieczenie przed pustym numerem oferty dziala tylko jednostronnie — blokuje nadpisanie niepustej wartosci pusta, ale nie gwarantuje, ze numer w wierszu procesu w ogole jest, zanim powiadomienie go uzyje.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:232
        status: prawdopodobne
        skutek: Zdarzenie niesie offer_id wskazujacy na oferte, ktorej Dzial 2 nie
                zdolal odczytac ze snapshotu (wiersz wtyczki 2 usuniety, kolumna
                offer_number pusta albo zawierajaca same biale znaki po trim).
                Powstaje NOWY wiersz procesu z offer_id, ale bez offer_number.
                Powiadomienie budowane z wiersza procesu idzie do klienta z pustym
                numerem — temat „Oferta" i zdanie „przesylamy oferte nr ." —
                czyli dokladnie ten sam obraz, ktory ten komentarz opisuje jako
                naprawiony.
        naprawa: Gdy $entity['offer_id'] > 0, a $offer_number jest pusty, potraktowac to jako blad danych: nie kolejkowac powiadomien do klienta odwolujacych sie do numeru oferty (albo wymusic ponowny odczyt / zapisac wpis LOG_NOTIFICATION_SKIPPED z powodem), zamiast dopuszczac wysylke z pusta wartoscia.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Agent 3.1 nie normalizuje NIP-u, choć agenci 3.2 i 3.3 czytają to samo pole kontekstu z  […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: resolve_wl() traktuje odpowiedź 200 bez pola result.subject.statusVat jako udane sprawdze […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Rabat jest rozdzielany na klasy podatkowe względem `subtotal_grosze` z kontekstu, a nie w […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: QA Agent 6 sprawdza tożsamość, którą sam Agent 6.2 przed chwilą wyliczył (`gross =  […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Zabezpieczenie S4-02 przed nieznanym kodem kraju działa tylko wtedy, gdy WooCommerce jest […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Ta sama wartość `$vat_valid` jest interpretowana w jednej funkcji dwiema różnymi regu� […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Dedup 7.1 porzucił zapytanie do BD i reużywa `leads` z działu 1, ale klucz unikalności […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

  [2.8] REGRESJA wzgledem poprzedniego przebiegu: Gałąź `else` ustawia `vat_status = 'checked'` i wypełnia `vat_checked_at` także wtedy […]
        status: potwierdzone
        skutek: To jest problem WPROWADZONY miedzy dwoma przebiegami. Wiadomo, ze
                powstal w zmianach z tego okresu — to najtansza w calym projekcie
                wskazowka, gdzie szukac.

DROBNE (84):
  [1.23] Status „draft" wpisany literalem, choc istnieje dla niego stala.
        mp-offer-builder/includes/class-mp-offer-builder-lead-listener.php:109
        status: prawdopodobne
        skutek: Zmiana wartosci stalej ominie to miejsce. Rekord zostanie w stanie,
                ktorego reszta kodu juz nie rozpoznaje.
        naprawa: Uzyc stalej STATUS_* zamiast napisu.

  [1.23] Status „draft" wpisany literalem, choc istnieje dla niego stala.
        mp-offer-builder/includes/class-mp-offer-builder-lead-listener.php:250
        status: prawdopodobne
        skutek: Zmiana wartosci stalej ominie to miejsce. Rekord zostanie w stanie,
                ktorego reszta kodu juz nie rozpoznaje.
        naprawa: Uzyc stalej STATUS_* zamiast napisu.

  [1.23] Status „draft" wpisany literalem, choc istnieje dla niego stala.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:106
        status: prawdopodobne
        skutek: Zmiana wartosci stalej ominie to miejsce. Rekord zostanie w stanie,
                ktorego reszta kodu juz nie rozpoznaje.
        naprawa: Uzyc stalej STATUS_* zamiast napisu.

  [1.23] Status „draft" wpisany literalem, choc istnieje dla niego stala.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:84
        status: prawdopodobne
        skutek: Zmiana wartosci stalej ominie to miejsce. Rekord zostanie w stanie,
                ktorego reszta kodu juz nie rozpoznaje.
        naprawa: Uzyc stalej STATUS_* zamiast napisu.

  [1.14] Licencja w naglowku i w composer.json brzmia inaczej.
        mp-offer-builder/composer.json
        status: prawdopodobne
        skutek: Dwa zrodla prawdy o licencji. Przy sporze nie wiadomo, ktore
                obowiazuje.

  [1.17] Dokumentacja cytuje zrodlo z adresem, ale bez daty pobrania.
        mp-lead-intake/docs/dzial-04/iso-3166-1-kody-krajow.md
        status: prawdopodobne
        skutek: Strona zrodlowa zmieni sie i nie da sie ustalic, czy cytat byl wierny
                w momencie pisania. Cytat bez daty jest niesprawdzalny.
        naprawa: Dopisac „Pobrano: RRRR-MM-DD" przy kazdym adresie.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-offer-builder/includes/admin/class-mp-offer-builder-admin.php:248
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:64
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:142
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:377
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.25] Data zapytania do Białej listy i klucz cache liczone są w UTC (gmdate), a nie w strefie sklepu — w nocy polskiego czasu odpytywany jest poprzedni dzień i pod nim zapisywany wynik.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:286
        status: potwierdzone
        skutek: Lead złożony o 01:30 czasu polskiego (CEST, UTC+2) w dniu, w którym
                podmiot został wpisany do wykazu: zapytanie idzie z datą wczorajszą
                i zwraca 'Niezarejestrowany', a wynik jest podawany dalej jako
                aktualny status firmy (i cache'owany na 12 h pod kluczem wczorajszej
                daty).
        naprawa: Użyć daty w strefie WordPressa, np. `current_time( 'Y-m-d' )` (lub `wp_date( 'Y-m-d' )`), spójnie w resolve_wl() i wl_cache_key(), żeby klucz cache i parametr `date` opisywały ten sam, lokalny dzień.

  [1.25] Gałąź cache w resolve_vies() (i cache-hit w trybie async) nie odtwarza pola vat_name, które gałąź sieciowa dodaje do wyniku — ten sam NIP przekazuje dalej różny zestaw danych zależnie od stanu cache.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:133
        status: potwierdzone
        skutek: Pierwszy lead z NIP-em X (cache miss) dostaje w kontekście vat_name z
                VIES; drugi lead z tym samym NIP-em w ciągu 24 h (cache hit) nie
                dostaje vat_name w ogóle. Każdy odbiorca korzystający z tego pola
                (np. uzupełnienie nazwy firmy) działa dla jednego leada, a dla
                identycznego wejścia w oknie cache — nie, w sposób nieodtwarzalny
                w testach.
        naprawa: Cache'ować tablicę wyniku (np. `array( 'valid' => bool, 'name' => string )`) zamiast samego 1/0 i odtwarzać z niej vat_name w gałęzi cache; alternatywnie w ogóle nie zwracać vat_name, jeśli pole nie jest odbierane.

  [1.25] `WC()->countries->get_countries()` jest listą filtrowalną (filtr `woocommerce_countries`), a nie kanonicznym rejestrem ISO — sklep, który ją zawęża, spowoduje twardy FAIL dla poprawnego kraju klienta.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-06.php:97
        status: potwierdzone
        skutek: Sklep (lub inna wtyczka) ogranicza listę krajów filtrem
                `woocommerce_countries` do kilku rynków. Klient z ważnym VAT UE z
                kraju usuniętego z listy (np. 'PT') dostaje FAIL `unknown_country`
                — dział 6 przerywa generowanie oferty dla całkowicie poprawnego,
                istniejącego kraju UE.
        naprawa: Walidować przynależność do statycznej listy ISO 3166-1 alpha-2 (uzupełnionej o `EU_COUNTRIES`), a listę WooCommerce traktować co najwyżej jako informację pomocniczą/ostrzeżenie, nie jako warunek FAIL.

  [1.25] Agent 7.3 zwraca `created_at` wyliczone lokalnie w momencie zwracania wyniku, a nie odczytane z zapisanego wiersza. Przy reaktywacji wartość jest wręcz nieprawdziwa — wiersz w BD zachowuje pierwotną datę utworzenia.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:232
        status: potwierdzone
        skutek: Powracająca firma (zarchiwizowany NIP) przechodzi ścieżką
                reaktywacji. Wiersz w `wp_mp_leads` ma `created_at` z pierwotnego
                zgłoszenia, a kontekst przekazany do działów 8/9 (log) oraz do
                zdarzenia dla wtyczki 2 niesie datę bieżącą. Log i BD pokazują
                dwie różne daty utworzenia tego samego leada — porównanie/raport
                na tej podstawie daje sprzeczne wyniki.
        naprawa: Zwracać `created_at` odczytane z zapisanego wiersza (lub kazać `insert_lead()`/`reactivate_lead()` zwracać komplet id + created_at), a nie generować nowy znacznik czasu w agencie.

  [1.25] Brak klucza `leads` w kontekście jest nieodróżnialny od „brak duplikatów" — domyślna pusta tablica daje `unique_ok = true`, czyli twierdzenie o unikalności bez żadnego dowodu.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:41
        status: potwierdzone
        skutek: Dział 1 nie uruchomi się lub zakończy bez ustawienia `leads`
                (błąd zapytania, zmiana nazwy klucza, ścieżka pomijająca dział
                1). Dział 7.1 raportuje `is_duplicate = false`, `unique_ok = true`,
                K7.1 przepuszcza duplikat, a INSERT rozbija się o UNIQUE(country,
                nip) → `insert_failed` zamiast poprawnego zatrzymania na duplikacie;
                przy braku ograniczenia w BD powstaje drugi aktywny lead na ten sam
                NIP.
        naprawa: Wymagać jawnego potwierdzenia, że dedup został wykonany (np. flaga `leads_fetched` z działu 1) i przy jej braku zwracać `MP_Result::fail` z kodem typu `dedup_unavailable` zamiast `unique_ok = true`.

  [1.25] `$vat_pending` łączy alternatywą dwie niezależne weryfikacje (VAT i status z Białej listy), więc oczekiwanie na status firmy kasuje datę sprawdzenia VAT-u, który był już rozstrzygnięty.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:136
        status: potwierdzone
        skutek: VIES trafia w cache (`vat_valid = true`), a odpytanie Białej listy
                idzie w tło (`company_status_pending = true`). Wiersz zapisuje się z
                `vat_valid = 1`, ale `vat_status = 'pending'` i `vat_checked_at =
                NULL` — kolumna daty twierdzi, że VAT-u nigdy nie sprawdzono, mimo
                że kolumna wyniku mówi, że jest ważny; wtyczka 2 do czasu
                rozstrzygnięcia statusu firmy nie widzi ważnego VAT UE.
        naprawa: Prowadzić dwa niezależne stany: `vat_status`/`vat_checked_at` wyłącznie na podstawie `vat_pending`, a oczekiwanie na Białą listę sygnalizować osobnym polem (np. `company_status_pending`).

  [1.25] Zabezpieczenie przed cena ujemna sprawdza tylko cene efektywna; ujemna cena regularna przechodzi do snapshotu.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:196
        status: prawdopodobne
        skutek: Produkt z bledna konfiguracja: regular_price = -100, aktywna promocja
                z cena 0. $effective = 0 przechodzi kontrole, a do snapshotu trafia
                regular_price = -100. Dzialy prezentacyjne/rabatowe pokazuja na
                ofercie ujemna cene przed promocja albo licza z niej ujemna kwote
                rabatu.
        naprawa: Rozszerzyc warunek o cene regularna: if ( $effective < 0.0 || (float) $regular < 0.0 ) — z tym samym komunikatem bledu.

  [1.25] Sekcja "Zastosowanie w tym dziale" dokumentacji WC_Tax opisuje wywolanie WC_Tax::get_rates( $tax_class ), a kod celowo wola get_base_tax_rates( $tax_class ) — dokumentacja opisuje zachowanie, ktore wedlug komentarza w kodzie nie dziala.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:267
        status: prawdopodobne
        skutek: Osoba utrzymujaca kod (albo kolejny audyt zgodnosci z Golden Rule #2)
                porownuje kod z dokumentacja dzialu, uznaje get_base_tax_rates() za
                odstepstwo i przywraca get_rates(). Wedlug udokumentowanego w kodzie
                testu na zywym WooCommerce get_rates() w kontekscie AJAX wp-admin (bez
                klienta/sesji) zwraca pustke, wiec KAZDA oferta konczy sie FAIL
                'missing_tax_rate'.
        naprawa: Zaktualizowac docs/dzial-02/woocommerce-wc_tax.md — sekcja "Zastosowanie" ma opisywac get_base_tax_rates() wraz z uzasadnieniem (brak klienta/sesji w AJAX wp-admin); cytaty z API zostawic bez zmian.

  [1.25] Dokumentacja WC_Product opisuje wyliczanie on_sale przez porownanie sale_price < regular_price i jawnie odradza get_price(); kod robi odwrotnie (is_on_sale() + get_price()) z uzasadnieniem S3-01.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:169
        status: prawdopodobne
        skutek: Deweloper przywracajacy zgodnosc kodu z dokumentacja dzialu wraca do
                porownania surowych meta sale_price/regular_price. Produkt z promocja
                zaplanowana na przyszlosc lub juz wygasla ma niepuste meta sale_price,
                wiec oferta zostaje wyceniona po cenie promocyjnej, ktora w sklepie
                nie obowiazuje — regresja bledu S3-01 na dokumencie handlowym.
        naprawa: Zaktualizowac docs/dzial-02/woocommerce-wc_product.md: opisac is_on_sale()/get_price() jako obowiazujace zrodlo ceny aktywnej (z powodem: okno dat promocji) i wskazac flage 'on_sale' w snapshocie jako zapis ZRODLA ceny wymagany przez kryt. 5.3.

  [1.25] Agent 2.2 eksportuje do kontekstu klucz 'prices_include_tax' => true w sytuacji, gdy wszystkie ceny w snapshocie sa juz przeliczone na NETTO — nazwa klucza sugeruje odwrotny stan danych.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-02.php:222
        status: potwierdzone
        skutek: Sklep ma wlaczone "Ceny wprowadzone z podatkiem". Dzial 6 czyta z
                kontekstu 'prices_include_tax' === true i — zgodnie z nazwa klucza
                — zdejmuje VAT z liczby, ktora jest juz netto. Netto i VAT na
                ofercie sa zanizone o cala stawke (100,00 netto -> 81,30 netto + 18,70
                VAT), a bledu nie widac na bramkach, bo arytmetyka pozostaje
                wewnetrznie spojna — dokladnie ten sam typ cichego rozjazdu, przed
                ktorym ostrzega komentarz w tym agencie.
        naprawa: Zmienic nazwe eksportowanego klucza na jednoznaczna wobec stanu danych (np. 'prices_converted_from_gross' / 'prices_are_net' => true) albo usunac go i zostawic wylacznie slad per pozycja ('from_gross'); dodatkowo potwierdzic w Dziale 6, ze zaden kod nie odejmuje VAT-u na podstawie tej flagi.

  [1.25] Petla follow-upow siega po `$plan['fire']` i `$task['type']` bez sprawdzenia kluczy, mimo ze reszta metody konsekwentnie stosuje isset().
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:202
        status: potwierdzone
        skutek: Dzial 6 zapisuje do kontekstu plan niepelny (np. sam klucz `schedule`
                po czesciowej awarii) → `$plan['fire']` to undefined index: PHP
                notice, `(array) null` daje pusta tablice, petla nie wykonuje sie ani
                razu. Powiadomienia follow-up dla wymagalnych zadan cicho nie
                powstaja, a A7.1 zwraca ok().
        naprawa: `$fire = isset( $plan['fire'] ) ? (array) $plan['fire'] : array();` oraz `isset( $task['type'] ) ? (string) $task['type'] : ''`; brak klucza `fire` w niepustym planie traktowac jako blad wejscia dzialu, nie jako pusta liste.

  [1.25] `lang` i `country` są ustawiane wyłącznie przy tworzeniu wiersza (`! $exists`), więc pusta wartość w istniejącym procesie nigdy się nie uzupełni — inaczej niż dane klienta, gdzie warunkiem jest pustka w wierszu, a nie brak wiersza.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Proces zakłada zdarzenie bez języka (np. wywołanie haka z
                integracji, która `lang` nie niesie) — wiersz powstaje z pustym
                `lang`. Kolejne zdarzenie dla tego samego procesu przynosi `lang =
                'en'`, ale warunek `! $exists` jest już fałszywy, więc kolumna
                zostaje pusta na stałe i wszystkie powiadomienia do klienta idą w
                języku domyślnym.
        naprawa: Zamienić warunek na test pustki w wierszu procesu (jak przy `client_name`/`client_email`): uzupełniać `lang`/`country`, gdy `'' === self::pick( $row, 'lang' )` / `'' === self::pick( $row, 'country' )`, nadal nie nadpisując wartości niepustej.

  [1.25] Wartownik TOCTOU jest brany tylko z PIERWSZEGO wymagalnego zadania, mimo że w jednym zdarzeniu może wypalić kilka zadań o różnych `guard_status`.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Cron wypala jednocześnie dwa zadania z różnymi wartownikami (np.
                follow-up wymagający statusu „oferta wysłana” i przypomnienie
                wymagające statusu „w negocjacji”). Sprawdzany jest wyłącznie
                wartownik pierwszego z listy; drugie zadanie zostaje domknięte jako
                DONE, a jego powiadomienie w kolejce, choć warunek statusu, którego
                miało pilnować, już nie obowiązuje — handlowiec dostaje
                przypomnienie o sprzedaży zamkniętej między odczytem a zapisem,
                czyli dokładnie ten przypadek, przed którym broni komentarz
                „WARTOWNIK SPRAWDZANY PONOWNIE W TRANSAKCJI”.
        naprawa: Przenieść `guard_status` na poziom pojedynczego wpisu (`tasks_close` / powiązanego powiadomienia) i sprawdzać go w transakcji osobno dla każdego wypalonego zadania, zamiast jednego pola `flow.guard` z `fire[0]`.

  [1.25] Termin SLA liczony jest przez `strtotime()` na tekstowej dacie GMT sklejonej z ' +1 day' — wynik zalezy od domyslnej strefy PHP, a nieudany parse (`false`) daje date z 1970 roku, ktorej krytyk K5.2 nie wychwyci, bo sprawdza tylko niepustosc.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:283
        status: prawdopodobne
        skutek: (a) Dowolna wtyczka/motyw w instalacji wola
                `date_default_timezone_set()` na strefe inna niz UTC — `strtotime()`
                interpretuje wtedy lancuch GMT jako czas lokalny, a `gmdate()`
                formatuje w UTC: termin SLA jest przesuniety o offset strefy (np. o 2
                godziny dla Europe/Warsaw w lecie), wiec Dzial 6 uzna zadanie za
                przeterminowane wczesniej/pozniej niz powinien. (b) Gdyby
                `strtotime()` zwrocil false, `gmdate('Y-m-d H:i:s', false)` da
                '1970-01-01 00:00:00' — wartosc niepusta, wiec K5.2 przepusci ja
                jako poprawna, a SLA bedzie od razu przeterminowane.
        naprawa: Liczyc na znacznikach: `$sla_due_at = gmdate( 'Y-m-d H:i:s', current_time( 'timestamp', true ) + DAY_IN_SECONDS );` — bez parsowania tekstu i bez zaleznosci od domyslnej strefy PHP. Ewentualnie dodac w K5.2 kontrole, ze `sla_due_at` jest pozniejsze od chwili biezacej.

  [1.25] Bramka QA5 jest opisana jako "legalnosc-przejscia + komplet skutkow", ale krytyk bramki sprawdza wylacznie `qa_allowed` i `qa_version`. Pole `qa_effects` jest wyliczane i przekazywane, po czym nigdy nie jest z niczym porownywane — druga polowa zadeklarowanej kontroli nie istnieje.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:400
        status: prawdopodobne
        skutek: Przejscie legalne (np. do `offer_sent`), ale lista skutkow trafiajaca
                do kontekstu jest pusta lub niekompletna (np. Dzial 6/7 czyta
                `effects` po tym, jak inny fragment nadpisal klucz w kontekscie).
                Bramka QA5 przepuszcza taki stan, bo `qa_allowed` = true i
                `qa_version` = 'v1'. Powiadomienie do klienta i follow-upy d+3/d+7 nie
                powstaja, a bramka jakosci raportuje PASS — dziennik pokaze
                przejscie zaliczone mimo braku skutkow.
        naprawa: W MP_SW_D5_QA_Critic porownac `qa_effects` z `MP_SW_D5_Machine::effects_for( $transition['to'] )` (gdy `changes_status` jest prawda) i zwrocic fail przy roznicy — tak jak robi to K5.2 — albo poprawic opis bramki, zeby nie deklarowal kontroli, ktorej nie wykonuje.

  [1.25] Brak wiersza procesu jest nieodroznialny od wiersza w statusie `new` — `from` domyslnie przyjmuje STATUS_NEW, wiec maszyna orzeka legalne przejscie dla procesu, ktorego nie ma.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:152
        status: prawdopodobne
        skutek: Zdarzenie `lead.created` (albo `status.change` na `assigned`) dla
                identyfikatora procesu, ktorego wiersza nie ma w bazie (usuniety
                proces, blad wczesniejszego dzialu, ktory nie zaladowal
                `flow['row']`): D5 przyjmuje from=`new`, uznaje przejscie
                new→assigned za legalne, K5.1 i QA5 przepuszczaja, decyzja trafia do
                dziennika jako udana. Dzial 8 wykonuje `wpdb::update()`, ktory
                aktualizuje zero wierszy, a dziennik pokazuje zmiane statusu, ktora
                nigdy nie nastapila.
        naprawa: Rozroznic brak wiersza od statusu poczatkowego: gdy `$flow['row']` nie istnieje, zwracac fail (np. `flow_row_missing`) zamiast domyslac sie STATUS_NEW; domyslke STATUS_NEW stosowac tylko dla istniejacego wiersza z pustym statusem.

  [1.25] Pusta wartość meta `mp_sw_active` jest zawsze interpretowana jako 'aktywny' — odczyt nie odróżnia braku klucza od świadomego wyłączenia handlowca.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Formularz profilu zapisuje niezaznaczony checkbox przez
                `update_user_meta( $id, 'mp_sw_active', false )` — WordPress trzyma
                to w `meta_value` jako pusty string. Przy kolejnym odczycie handlowiec
                wyłączony z rotacji wraca na listę `complete` z `active = true` i
                dostaje przydział nowych procesów, mimo że w profilu ma odznaczone
                'przyjmuje nowe procesy'.
        naprawa: Zapisywać jawne '1'/'0' i traktować jako domyślnie aktywnego wyłącznie brak klucza: `metadata_exists( 'user', $id, self::META_ACTIVE ) ? (bool) $active : true`.

  [1.25] Wynik filtra `mp_sw_read_offer` scalany jest z szablonem, w którym `table_present` to `false` — override potwierdzający ofertę zwraca stan wewnętrznie sprzeczny (`exists = true` przy `table_present = false`, pustym `status` i pustym `offer_number`).
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Wtyczka 2 podpina się pod filtr i zwraca `array( 'exists' => true,
                'belongs' => true, 'handle' => 'uuid' )` — czyli dokładnie to, po
                co zaczep powstał. Snapshot niesie jednocześnie 'oferta istnieje i
                należy do leada' oraz 'tabela ofert nieobecna', a `status` i
                `offer_number` są puste. Konsument, który warunkuje wysyłkę
                statusem oferty albo raportuje brak wtyczki 2 na podstawie
                `table_present`, zablokuje poprawnie potwierdzoną ofertę lub zgłosi
                nieprawdziwy komunikat o nieaktywnej wtyczce 2.
        naprawa: Dla gałęzi override użyć osobnej bazy scalania z `'table_present' => true` (lub trzystanowego pola `source` = event|table|filter) i odrzucać override, który deklaruje `exists = true` bez `status`.

  [1.25] Brak rozszerzenia mbstring daje komunikat o niezgodności treści PDF zamiast o brakującej zależności — status nie odpowiada temu, co naprawdę się stało.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:192
        status: prawdopodobne
        skutek: Wdrożenie na hostingu bez mbstring: KAŻDA generacja oferty pada z
                kodem pdf_content_mismatch i komunikatem sugerującym
                uszkodzony/niezgodny PDF. Operator szuka błędu w renderze, danych i
                numeracji, podczas gdy przyczyną jest brak rozszerzenia PHP —
                inaczej niż przy Dompdf, gdzie brak zależności ma własny kod
                dompdf_unavailable.
        naprawa: Sprawdzić dostępność mb_convert_encoding na wejściu run() Agenta 9.2 i zwrócić osobny kod (np. 'mbstring_unavailable') z jednoznacznym komunikatem, tak jak robi to class_exists dla Dompdf.

  [1.25] Krytyk 9.2 ("zawartość-pdf") nie ma ścieżki, którą mógłby zawieść — sprawdza format wartości, którą sam agent zawsze produkuje funkcją hash().
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:262
        status: prawdopodobne
        skutek: Regresja w Agencie 9.2 (np. contains_pdf_info_string zaczyna zwracać
                true trywialnie po zmianie kodowania metadanych) nie zostaje wyłapana
                przez nikogo: krytyk nazwany "zawartość-pdf" przepuszcza wynik
                bezwarunkowo, a QA 9 sprawdza wyłącznie zgodność skrótu z
                plikiem. Dział raportuje zweryfikowaną zawartość, której nikt
                niezależnie nie zweryfikował.
        naprawa: Przenieść do krytyka realną, niezależną kontrolę (np. samodzielne wyszukanie obu wartości metadanych w bajtach pliku odczytanych ponownie z dysku), zamiast walidacji formatu skrótu, która nie może zawieść.

  [1.25] Wyszukiwanie metadanych nie wiąże wartości z kluczem — dopasowanie w dowolnym polu /Info zalicza kontrolę kwoty.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:196
        status: prawdopodobne
        skutek: Numer oferty zaczyna się od tego samego ciągu cyfr, co suma brutto w
                groszach (np. numer "202607/0001" i kwota 202607 gr = 2026,07 zł).
                Wtedy warunek na kwotę spełnia się na wartości /Title, nawet gdyby
                klucz MPOfferGross w pliku był pusty lub zawierał inną kwotę —
                kontrola kwoty przechodzi, nie sprawdziwszy jej faktycznie.
        naprawa: Szukać pary klucz+wartość, np. dopasowywać wzorzec `/MPOfferGross` a po nim ciąg BOM+UTF-16BE(kwota) zakończony nawiasem, zamiast gołego strpos po całym pliku.

  [1.25] Limit rozmiaru i wartość 'bytes' przekazywana dalej pochodzą z bufora w pamięci, a nie z pliku, który realnie leży na dysku i będzie wydany klientowi.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:212
        status: prawdopodobne
        skutek: Zapis tymczasowy kończy się częściowo (wyczerpany limit
                dyskowy/quota), a write_tmp_pdf zwraca ścieżkę. Rozmiar zapisany w
                kontekście i przekazany do Działów 10/11 opisuje bufor z pamięci,
                nie plik — metadane oferty (rozmiar w BD-2, nagłówek przy
                pobraniu) rozjeżdżają się z rzeczywistym plikiem, a kontrola
                limitu nigdy nie dotyka faktycznej zawartości dysku.
        naprawa: W Agencie 9.2 wyliczyć rozmiar z odczytanych bajtów (strlen( $file_bytes )), sprawdzić limit na tej wartości i tę wartość wpisać do wyniku, ewentualnie porównując ją z deklarowaną przez 9.1.

  [1.25] Żadna ścieżka FAIL po zapisie pliku tymczasowego go nie usuwa — nieudane generacje zostawiają na dysku sieroty w katalogu prywatnym-tymczasowym.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:209
        status: prawdopodobne
        skutek: Kontrola rozmiaru odrzuca PDF przekraczający 5 MB — ale plik już
                leży na dysku i tam zostaje. Powtarzalny błąd (np. źle
                skonfigurowany szablon generujący ogromne dokumenty albo brak
                mbstring, patrz wyżej) przy każdej próbie dokłada kolejny plik;
                katalog tymczasowy rośnie bez ograniczenia aż do zapełnienia dysku.
        naprawa: Usuwać plik tymczasowy na każdej ścieżce zakończonej niepowodzeniem po jego zapisie (albo dodać w Dziale 9/10 jawne sprzątanie tmp starszego niż X), tak by tylko ścieżka COMMIT pozostawiała plik.

  [1.25] K9.1 zgłasza `duplicate_emit` i wywraca cały przebieg w sytuacji, w której emiter zadziałał poprawnie i duplikat stłumił — licznik `times()` zlicza PRÓBY, nie faktyczne wystawienia.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:221
        status: prawdopodobne
        skutek: Jedno uruchomienie crona przetwarza kolejkę, w której ten sam
                `event_id` znalazł się dwa razy (ponowne dodanie po nieudanej
                wysyłce), i oba przebiegi wykonują zapis. Drugi przebieg: emiter
                poprawnie nie wystawia niczego (`dispatched = []`), zapis zostaje
                zatwierdzony (COMMIT), ale K9.1 widzi `emit_times = 2` i kończy
                przebieg błędem `duplicate_emit`. Wynik działu mówi „porażka"
                mimo że zapis się udał i żadne zdarzenie nie poszło dwa razy —
                zdarzenie zostanie oznaczone jako błędne i może być ponawiane w
                kółko.
        naprawa: Rozdzielić licznik prób od licznika faktycznych wystawień (np. `times()` zwraca liczbę wywołań `do_action`, a `attempts()` liczbę prób). K9.1 powinien wywracać przebieg tylko wtedy, gdy faktycznych wystawień jest > 1; powtórzoną próbę raportować jako informację, nie jako `fail`.

  [1.25] K9.2 nie sprawdza, czy w wyniku agenta w ogóle jest klucz `response`; przy jego braku krytyk przepuszcza pustą odpowiedź jako zgodną z procesem i odczytuje nieistniejące klucze tablicy.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:328
        status: prawdopodobne
        skutek: A9.2 kończy się wynikiem bez klucza `response` (np. po zmianie
                kształtu wyniku), a zdarzenie nie ma `entity.lead_id` (`$lead_id =
                0`). Wtedy `0 !== 0` jest fałszem, `assigned_user_id` wychodzi 0,
                więc kontrola przecieku zakresu też nie zadziała — K9.2 zwraca
                `ok` dla PUSTEJ odpowiedzi i deklaruje, że „odpowiedź dotyczy tego
                procesu". Gwarancja K9.2 staje się pusta; do wywołującego trafia
                odpowiedź bez danych, a w logu PHP lądują ostrzeżenia o
                nieistniejących kluczach.
        naprawa: Na wejściu K9.2 dodać twardy warunek: `if ( ! isset( $data['response'] ) || ! is_array( $data['response'] ) || empty( $data['response'] ) ) { return MP_SW_Result::fail( ..., 'response_missing' ); }`, oraz sprawdzić obecność kluczy wymaganych (`ok`, `lead_id`, `trace_id`, `http_status`) zanim się je odczyta.

  [1.25] `do_action( 'mp_lead_created' )` jest wywolywane wewnatrz agenta 11.3, przed zwroceniem wyniku i przed ocena krytyka K11.3 oraz bramki QA11, bez zadnego zabezpieczenia przed powtorna emisja.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:109
        status: prawdopodobne
        skutek: Ten sam `lead_id` przechodzi przez dzial 11 dwa razy (ponowne
                uruchomienie agenta po odrzuceniu przez krytyka, wznowienie pipeline'u
                od dzialu 11, recznie powtorzone przetworzenie leada). Za kazdym razem
                leci `mp_lead_created` z tym samym `lead_id`, wiec subskrybent w
                pluginie 2 tworzy drugi szkic oferty dla jednego leada — handlowiec
                dostaje zdublowana oferte, a plugin 1 nie ma jak tego cofnac, bo
                skutek uboczny wyszedl poza jego granice zanim wynik dzialu zostal
                zaakceptowany.
        naprawa: Emitowac hook dopiero po zaakceptowaniu wyniku dzialu (albo na koncu pipeline'u), a przed emisja sprawdzac znacznik idempotencji dla danego `lead_id` (np. meta/kolumna „hook wyslany”), tak zeby powtorzenie dzialu nie tworzylo drugiego szkicu.

  [1.25] Przy braku `started_ts` raport koncowy zwraca `duration_ms = null`, ale jednoczesnie `report_ready = true` i `pipeline_status = 'completed'` — brak zadeklarowanej w opisie agenta wartosci nie obniza statusu raportu.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:76
        status: prawdopodobne
        skutek: Pipeline uruchomiony bez dzialu 9 (wznowienie od pozniejszego dzialu,
                sciezka skrocona) nie ma `started_ts` w kontekscie. Raport koncowy
                przechodzi wszystkie kontrole jako kompletny, ale w BD-3 / w
                podgladzie admina czas trwania jest pusty albo — po rzutowaniu nulla
                na liczbe po stronie odbiorcy — pokazuje 0 ms, czyli falszywa
                informacje, ze pipeline wykonal sie natychmiast.
        naprawa: Gdy `started_ts` jest niedostepny, oznaczyc raport jako niepelny (np. `report_ready = false` albo osobna flaga `duration_unavailable`), zeby krytyk/bramka mogly to wychwycic, zamiast zwracac cichego nulla przy statusie „gotowe”.

  [1.25] Bramka realizuje tylko połowę cytowanego w niej kryterium: wykrywa znaczniki pozostawione w nawiasach, ale nie wykrywa znaczników podstawionych wartością pustą.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: Klient bez NIP (`$client['nip'] === ''`) i szablon z wierszem 'NIP:
                {{client_nip}}' → dict podstawia pusty string → w HTML nie ma już
                żadnych nawiasów → pending_markers() zwraca pustą tablicę →
                wynik OK → do klienta idzie oferta z pustą etykietą 'NIP:', mimo
                że przytoczone kryterium odbioru tego zabrania.
        naprawa: Po scaleniu sprawdzać także puste wartości w $dict dla znaczników faktycznie obecnych w szablonie, z jawną białą listą pól celowo pustych (tax_mechanism_note), i failować osobnym kodem np. 'empty_placeholder'.

  [1.25] Gałąź fail Krytyka 7.1 jest nieosiągalna — jej warunek nigdy nie może być prawdziwy, więc deklarowana "niezależna, powtórna weryfikacja" nie daje żadnej dodatkowej ochrony.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: Dla każdego wejścia, przy którym agent zwraca OK, zachodzi
                template_lang === lang, więc kod 'template_language_mismatch' nie
                powstanie nigdy. Realny przypadek, który krytyk miałby złapać —
                wiersz w BD-2 otagowany lang='pl', ale z treścią angielską w polu
                content — przechodzi obie bramki i wychodzi do klienta jako polska
                oferta po angielsku (kod QA Agenta 7 deklarującego jednojęzyczność
                nie znajduje się w dossier, więc nie da się stwierdzić, czy to
                domyka).
        naprawa: Albo weryfikować niezależne źródło (`$context->get('templates')[$lang]['lang']` oraz zgodność template_content z wybranym wpisem), albo usunąć martwą gałąź, żeby nie sugerowała zabezpieczenia, którego nie ma.

  [1.25] trace_id powstaje dopiero w Agencie 11.1, czyli po sukcesie działów 1–10. Wszystkie odpowiedzi błędne (awaria w dowolnym wcześniejszym dziale) nie mają trace_id, mimo że kontrakt odpowiedzi go wymaga.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:108
        status: prawdopodobne
        skutek: Pipeline przerywa się w dziale 5 (walidacja/wycena): klient dostaje
                wp_send_json_error bez trace_id, więc zgłoszenia "nie działa
                generowanie oferty" nie da się powiązać z żadnym wpisem w logu —
                dokładnie w przypadkach, w których korelacja jest potrzebna
                najbardziej.
        naprawa: Generować trace_id na starcie pipeline'u (dział 1) i wkładać do kontekstu; dział 11 ma go tylko odczytywać, a handler AJAX dołączać go również do odpowiedzi błędnych.

  [1.25] Odpowiedź sukcesu zawsze zwraca pdf_url do akcji, dla której — zgodnie z dokumentacją tego samego działu — handler jeszcze nie istnieje; dodatkowo URL nosi nonce, więc ma ograniczoną ważność i jest związany z bieżącym użytkownikiem.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:176
        status: prawdopodobne
        skutek: Użytkownik kończy generowanie oferty dziś i klika pdf_url:
                admin-ajax.php nie ma zarejestrowanej akcji
                mp_offer_builder_download_pdf i zwraca '0' — odpowiedź success:true
                obiecuje zasób, którego nie da się pobrać. Ten sam URL zapisany
                przy ofercie lub wysłany innemu handlowcowi przestaje działać
                także po wygaśnięciu nonce / przy innym użytkowniku.
        naprawa: Do czasu istnienia handlera nie zwracać pdf_url jako gotowego linku (albo zwracać go razem z informacją o niedostępności); docelowo generować URL bez wbudowanego nonce'u (nonce wystawiać w chwili kliknięcia po stronie UI), żeby adres nie tracił ważności i nie był związany z jednym użytkownikiem.

  [1.25] Nieudane kodowanie meta_json nie jest wykrywane — do bazy trafia pusty łańcuch zamiast danych, a dział nadal raportuje log_status = 'success'.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-08.php:64
        status: prawdopodobne
        skutek: Do kontekstu trafia pole tekstowe z niepoprawnym UTF-8 (np. segment
                lub company_status pochodzące z formularza w innym kodowaniu).
                wp_json_encode() zwraca false, kolumna meta_json dostaje '' zamiast
                JSON-a. Wpis logu istnieje, log_id > 0, QA8 przechodzi, dział zwraca
                log_status = 'success' — mimo że score, segment, kraj, status VAT i
                przypisany handlowiec zniknęły bezpowrotnie z historii. Późniejsze
                odczyty meta_json dostają pusty łańcuch i nie odróżnią go od
                poprawnego zapisu bez danych.
        naprawa: Sprawdzić wynik: `$meta = wp_json_encode( ... ); $data['meta_json'] = ( false === $meta ) ? null : $meta;` i przy false zwrócić z 8.2 wynik ostrzegawczy/fail zamiast cichego przejścia dalej.

  [1.25] Wyrażenie walidujące telefon sprawdza tylko zbiór dopuszczonych znaków i minimalną długość, nie wymaga ani jednej cyfry — łańcuch złożony z samych separatorów przechodzi jako poprawny numer.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-02.php:137
        status: prawdopodobne
        skutek: Użytkownik wpisuje `++++++` albo `()()()` (6 znaków) → preg_match
                zwraca 1 → brak wpisu w `errors` → `form_valid=true`, dział
                raportuje poprawny telefon i zapisuje go do bazy. Handlowiec w dalszym
                procesie dostaje rekord z „numerem", pod który nie da się
                zadzwonić, a walidacja nigdy nie zasygnalizowała problemu.
        naprawa: Liczyć cyfry po odfiltrowaniu separatorów, np. `$digits = preg_replace( '/\D+/', '', $phone );` i wymagać `strlen( $digits ) >= 9` (oraz sensownego maksimum), zamiast opierać się wyłącznie na dopuszczonym zbiorze znaków.

  [1.25] Fallback w `str_len()` mierzy limity długości w bajtach zamiast w znakach, gdy brak rozszerzenia mbstring, przez co limity 190/255/30 stają się ostrzejsze niż deklarowane i odrzucają poprawne dane.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-02.php:99
        status: prawdopodobne
        skutek: Instalacja bez mbstring, nazwa firmy z polskimi znakami diakrytycznymi
                o 200 znakach (ok. 260 bajtów w UTF-8) → `strlen()` zwraca 260 >
                255 → `errors['company_name']` → QA2 odrzuca zgłoszenie
                komunikatem „maks. 255 znaków", mimo że nazwa ma 200 znaków i
                zmieściłaby się w kolumnie VARCHAR(255) utf8mb4 (MySQL liczy znaki,
                nie bajty). Poprawny lead przepada, a komunikat błędu jest dla
                użytkownika nieprawdziwy.
        naprawa: W fallbacku liczyć znaki bez mbstring, np. `count( preg_split( '//u', $s, -1, PREG_SPLIT_NO_EMPTY ) )` lub `strlen( utf8_decode( $s ) )`, albo ujednolicić limity i komunikaty na bajty w obu gałęziach.

  [1.25] Optional entity field z niepoprawną wartością jest cicho pomijany — bez błędu kontraktu i bez śladu. Odbiorca dostaje status „przyjęto”, choć dane, dla których ta lista w ogóle powstała, wypadły z koperty.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-01.php
        status: prawdopodobne
        skutek: Wtyczka 2 emituje `mp_offer_created` z `entity.offer_id` w postaci
                nieliczbowej lub równej 0 (np. pusty string po nieudanym zapisie
                oferty). A1.1 pomija pole bez błędu, K1.1 zwraca sukces, proces
                przechodzi do statusu „oferta w przygotowaniu” bez identyfikatora
                oferty — i ponieważ `offer.approved` nikt nie emituje, brak nigdy
                się nie uzupełni. Wywołujący dostaje odpowiedź sukcesu i nie ma
                jak wykryć, że dane przeszły niekompletnie.
        naprawa: W pętli pól opcjonalnych dopisać gałąź `else { $errors[] = 'entity.' . $field; }` — pole nieobecne dalej pozostaje dopuszczalne, ale pole OBECNE i niepoprawne jest błędem kontraktu, tak samo jak niepoprawny `client.email`.

  [1.25] A1.2 i A1.3 porównują `type` w postaci surowej, podczas gdy A1.1 przed walidacją go przycina. Ten sam ładunek jest więc klasyfikowany przez trzech agentów niespójnie.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-01.php
        status: prawdopodobne
        skutek: Wywołujący wysyła `type` z końcową spacją (`"task.due "`) i bez
                `event_id`. K1.1 przepuszcza żądanie, bo A1.1 obcięło spację i
                typ jest w słowniku. W A1.3 porównanie ścisłe nie trafia, więc
                strażnik „zdarzenie z timera MUSI przyjść z gotowym kluczem”
                nie zadziała i zdarzenie idzie ścieżką dla zdarzeń bez klucza.
                Wymóg „timer d+3 wykonany dokładnie raz” przestaje obowiązywać
                dla takiego ładunku — kolejne wywołania nie są rozpoznawane jako
                to samo zdarzenie.
        naprawa: Znormalizować typ raz (w A1.1) i w A1.2/A1.3 czytać wartość znormalizowaną z wyniku poprzedniego agenta, albo powtórzyć `is_string() + trim()` przed każdym porównaniem ze stałą słownika.

  [1.25] `client.name` jest rzutowane na string bez sprawdzenia typu, więc struktura zamiast łańcucha zamienia się w dosłowny napis „Array” zapisany jako nazwa klienta.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-01.php
        status: prawdopodobne
        skutek: Wtyczka 1 (albo ręczne żądanie JSON) przysyła
                `{"client":{"name":{"first":"Jan","last":"Kowalski"}}}`. Rzutowanie
                tablicy na string daje `"Array"` (plus ostrzeżenie PHP), pole
                przechodzi walidację bez błędu, a Dział 7 — dla którego, wg
                komentarza w kodzie, jest to jedyne wejście danych klienta —
                wysyła klientowi powiadomienie zaadresowane do „Array”.
        naprawa: Przyjmować nazwę tylko gdy `is_string( $raw_client['name'] )` (ewentualnie `is_scalar()`), w przeciwnym razie dopisać `client.name` do `$errors`, analogicznie do obsługi `client.email`.

  [1.25] Galaz krytyka K3.2 wykrywajaca rozszerzenie zakresu przez parametr zadania jest nieosiagalna — wczesniejszy `return` przechwytuje kazdy stan, ktory moglby ja spelnic.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-03.php:351
        status: prawdopodobne
        skutek: Handlowiec (manage_all=false, automated=false) wysyla zadanie z
                parametrem `scope=all`. Sterowanie nigdy nie dochodzi do drugiego
                warunku: albo scope wyliczony przez A3.2 to 'own'/'team' (wtedy
                `SCOPE_ALL === $scope` jest falszem), albo to 'all' — ale wtedy
                pierwszy warunek juz zwrocil bledy. W efekcie kod bledu
                'scope_from_request' nie pojawi sie nigdy w odpowiedzi ani w sladzie
                audytowym, a zadeklarowana w dokumentacji kontrola K3.2
                ('ciecie-zakresu' — parametr zadania nie moze rozszerzyc widoku)
                jest martwa: jedynym realnym zabezpieczeniem pozostaje to, ze A3.2
                ignoruje `$requested`. Regresja, ktora kiedys pozwoli parametrowi
                wplynac na wynik dla wartosci 'team', nie zostanie przez tego krytyka
                zauwazona (warunek porownuje wylacznie SCOPE_ALL).
        naprawa: Porownywac zadany zakres z zakresem naleznym niezaleznie od wyniku eskalacji, np. przed sprawdzeniem eskalacji: `if ( '' !== $requested && $requested !== $scope_allowed_for_actor )` albo `if ( '' !== $requested && $requested === $scope && ! $manage_all && MP_SW_D3::SCOPE_OWN !== $requested )`, tak by galaz 'scope_from_request' byla osiagalna dla kazdej proby poszerzenia (takze 'team').

  [1.25] Bramka uprawnienia szczegolowego indeksuje zamknieta, trzyelementowa mape kluczem zwracanym przez MP_SW_Roles::cap_for_event(); trafienie poza mape daje cicha odmowe, a flaga manage_all nie jest w tej bramce w ogole brana pod uwage.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-03.php:149
        status: prawdopodobne
        skutek: Dla typu zdarzenia, dla ktorego cap_for_event() zwraca uprawnienie
                spoza tej trojki (np. odrebny cap tworzenia procesu dla
                EVENT_LEAD_CREATED wywolanego recznie ze zrodla uzytkownika albo cap
                pulpitu dla EVENT_DASHBOARD_VIEW), `$granted[ $needed ]` jest kluczem
                nieistniejacym, `empty()` zwraca true i KAZDY nieautomatyczny aktor
                — lacznie z rola z manage_all — dostaje 403 z powodem
                'capability_missing'. Odmowa jest nieodrozninalna od realnego braku
                uprawnienia, wiec bledna konfiguracja mapy nie zostanie zdiagnozowana
                z odpowiedzi; kryterium odbioru 'dzialajace role: administrator,
                manager sprzedazy, handlowiec' przestaje byc spelnione dla takiego
                typu zdarzenia.
        naprawa: Budowac `$granted` z tego samego zrodla stalych, ktorego uzywa cap_for_event() (lub odpytywac snapshot po nazwie capa), a trafienie klucza nieznanego traktowac jako blad konfiguracji z odrebnym powodem (np. 'capability_unmapped') zamiast 'capability_missing'; dodatkowo przepuszczac aktora z `manage_all` przed testem capa szczegolowego.

  [1.25] Bramka jakosci dzialu 10 (QA10 + K10.3) weryfikuje ta sama wielkosc policzona dwa razy — result_ready jest wyprowadzone z success, a success z lead_id > 0 — wiec warunek 'result_ready AND lead_id > 0' jest tautologia i nie moze wykryc zadnej wady powstalej w 10.2 lub 10.3.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-10.php:108
        status: prawdopodobne
        skutek: Przy lead_id = 41 wynik bramki jest przesadzony jeszcze przed
                uruchomieniem 10.2 i 10.3. Dowolna awaria w tych agentach —
                response_json rowne false, brak pola message, sprzeczny status —
                przechodzi bramke jako 'wynik gotowy', bo QA10 nie oglada zadnego z
                tych pol. Dzial raportuje prawdziwy wynik tylko przez przypadek.
        naprawa: Rozszerzyc QA10 o kontrole faktycznego produktu dzialu: is_string( $context->get( 'response_json' ) ) oraz obecnosc wymaganych kluczy odpowiedzi (success, lead_id, message, status), zamiast powtornego sprawdzania lead_id.

  [1.25] Dział przepuszcza pusty koszyk jako w pełni zwalidowany. Obie pętle walidacyjne (`foreach ( $items ... )`) na pustej tablicy nie wykonują ani jednej iteracji, a jedyna kontrola liczby pozycji sprawdza wyłącznie górny limit (`count( $items ) > self::MAX_ITEMS`). Dokumentacja działu definiuje MAX_ITEMS = 50, ale nie ma odpowiednika MIN_ITEMS ani w dokumentacji, ani w kodzie.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-03.php:90
        status: prawdopodobne
        skutek: Żądanie z pustą listą pozycji (products: []) — snapshot Działu
                2 też jest pusty. Agent 3.1 → ok, Agent 3.2 → ok, bramka QA →
                accept, dział zwraca `items_valid = true`. Kalkulacja w kolejnym
                dziale daje ofertę z zerową liczbą pozycji i wartością 0, która
                trafia do renderu PDF/e-maila jako poprawna oferta, bez jawnej
                informacji dla handlowca. Ustalenie jest aktualne, o ile żaden
                wcześniejszy dział nie wymusza co najmniej jednej pozycji — w
                dossier nie ma dowodu na taką kontrolę, a Dział 3 jest miejscem,
                gdzie limity liczby pozycji są zdefiniowane.
        naprawa: Dodać stałą `MIN_ITEMS = 1` obok `MAX_ITEMS` i warunek `if ( count( $items ) < self::MIN_ITEMS )` w Agencie 3.2 (błąd `field => 'items'`), oraz dopisać dolny limit do tabeli limitów w docs/dzial-03/limity-koszyka.md, żeby kod i dokumentacja źródłowa nadal się zgadzały.

  [1.25] Krytyk K4.1 sprawdza warunek, który nigdy nie może być prawdziwy — gałąź `fail` jest nieosiągalna, więc bramka zawsze raportuje PASS niezależnie od danych.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:253
        status: prawdopodobne
        skutek: Dział 2 zwraca pusty lub obcięty `salesmen.complete` (np. w trybie
                wznowienia albo po błędzie odczytu). K4.1 kończy się OK
                („kandydat pochodzi ze snapshotu"), bo zero kandydatów to zero
                nieznanych — raport QA pokazuje zaliczoną bramkę doboru, choć nie
                zweryfikowała ona niczego, a realny problem ujawnia się dopiero jako
                fatalny błąd K4.2 z mylącą przyczyną.
        naprawa: Zmienić K4.1 na sprawdzenie, które może zawieść na realnych danych: np. że każdy kandydat z kręgu `exact` faktycznie ma `active` prawdziwe oraz zgodny kraj i język, oraz że przy niepustym `salesmen.complete` i niepustym `match_country`/`match_lang` liczności kręgów zgadzają się z przeliczeniem po snapshocie.

  [1.25] Krytyk K4.3 „odtwarzalność decyzji" przelicza `decide()` drugi raz na dokładnie tych samych danych czystą funkcją — rozjazd jest niemożliwy, więc deklarowana kontrola niedeterminizmu nic nie wykrywa.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:430
        status: prawdopodobne
        skutek: Snapshot z Działu 2 zwraca handlowców w kolejności zależnej od
                bazy (brak `ORDER BY`) i przy remisie obciążenia oraz
                `last_assigned_at` decyduje kolejność wierszy — dokładnie ryzyko,
                które K4.3 miał wyłapać. Krytyk sortuje tę samą, już zwróconą
                tablicę, dostaje ten sam wynik i raportuje OK; ponowione zdarzenie na
                świeżym odczycie bazy przydziela leada komu innemu, a bramka nadal
                pokazuje „odtwarzalność sprawdzona".
        naprawa: Przeliczyć decyzję na przetasowanym wejściu (np. `array_reverse( $salesmen )` — deterministycznie, bez `shuffle`) i porównać `user_id`, `match` oraz `pool`; wtedy każda zależność od kolejności wejścia daje rozjazd i bramka faktycznie zawodzi.

  [1.25] Komunikat błędu K4.2 przy braku właściciela nazywa stan, który nie odpowiada temu, co się stało: przy wszystkich handlowcach nieaktywnych twierdzi, że handlowcy byli dostępni.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:348
        status: prawdopodobne
        skutek: Wszyscy handlowcy zostają czasowo wyłączeni (urlopy, `active = 0` w
                usermeta `mp_*`), `total` wynosi np. 5. Proces przerywa się fatalnie
                z komunikatem sugerującym błąd algorytmu doboru mimo dostępnych
                ludzi, więc operator szuka usterki w regułach kraj × język zamiast
                włączyć któregoś handlowca — realna przyczyna (zero aktywnych)
                nie pada nigdzie w komunikacie.
        naprawa: Rozróżniać w K4.2 trzy przypadki po liczbie aktywnych rekordów w `salesmen.complete`: brak handlowców w ogóle, handlowcy istnieją ale żaden nie jest aktywny, oraz są aktywni a mimo to nie wybrano właściciela — i podawać liczbę aktywnych w danych błędu.

  [1.25] Wynik odczytu z bazy nie jest w ogóle weryfikowany w agencie — nieudany odczyt zostaje opakowany w status OK, a jego jedyny objaw to komunikat krytyka o złej strukturze, mylnie wskazujący na błąd danych zamiast na błąd bazy.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-01.php:55
        status: prawdopodobne
        skutek: Zapytanie do wp_mp_leads kończy się błędem (np. tabela BD-3
                niedostępna / błąd SQL), get_results zwraca null, helper przekazuje
                null dalej. Agent 1.1 mimo to zwraca MP_Result::ok, a niepowodzenie
                odczytu ujawnia się dopiero u krytyka jako fail z kodem
                'invalid_structure' i komunikatem „Brak lub zła struktura danych:
                leads”. Operator dostaje diagnozę wskazującą na wadliwą
                strukturę wyniku agenta, a nie na awarię bazy, co kieruje
                debugowanie w złą stronę; jeśli helper zamiast null zwróciłby
                pustą tablicę, awaria bazy przeszłaby całkowicie bezgłośnie jako
                „brak historii klienta”.
        naprawa: W agencie 1.1 sprawdzić wynik odczytu przed zbudowaniem MP_Result: gdy zwrócono null (albo gdy $wpdb->last_error jest niepuste), zwrócić MP_Result::fail z odrębnym kodem, np. 'db_read_failed', zamiast ok() — dzięki temu status działu odpowiada temu, co naprawdę się stało, i awaria bazy nie jest mylona z brakiem dopasowań.

  [1.25] Sciezka bez mbstring cicho psuje segmentacje polskich nazw firm, a wynik i tak wraca jako ok — nieudana normalizacja konczy sie 'sukcesem' z blednym segmentem 'Inne'.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:86
        status: prawdopodobne
        skutek: PHP zbudowane bez rozszerzenia mbstring; firma 'USŁUGI BLACHARSKIE
                SP. Z O.O.' bez pola segment. strtolower daje 'usŁugi
                blacharskie...', needle 'usług' nie trafia, petla konczy sie bez
                dopasowania i segment = 'Inne' zamiast 'Usługi'. QA4 przepuszcza
                (niepuste), wiec bledna segmentacja wchodzi do oferty/priorytetow bez
                zadnego ostrzezenia.
        naprawa: Uzaleznic dzialanie dzialu od mbstring (twardy wymog przy bootstrapie wtyczki) albo w fallbacku zrobic jawne mapowanie polskich wielkich liter (strtr) przed strtolower; jesli normalizacja jest niepewna, zwrocic wynik z flaga niskiej pewnosci zamiast cichego 'Inne'.

  [1.25] Wyjatek z granica slowa dla needle 'it' laimie deklarowana w dokumentacji semantyke 'fragment nazwy' i daje falszywe negatywy dla firm IT bez separatora po 'IT'.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:90
        status: prawdopodobne
        skutek: Firma 'ITSolutions Sp. z o.o.' lub 'IT24 Sp. z o.o.' bez pola segment:
                haystack 'itsolutions...' / 'it24...', wzorzec \bit\b nie trafia (po
                't' stoi znak slowa), zaden inny needle tez nie pasuje, wiec segment =
                'Inne' zamiast 'IT'. Wedlug dokumentacji (dopasowanie fragmentu) obie
                firmy powinny trafic do segmentu IT.
        naprawa: Zamienic obustronna granice na granice tylko z lewej strony i wobec liter: '/(?<!\p{L})it/u' — nie trafi w 'architektura' ani 'kapitałowa', ale trafi w 'itsolutions' i 'it24'. Rownolegle dopisac ten wyjatek do docs/dzial-04/segmentacja-konfiguracja.md, zeby dokumentacja opisywala faktyczna regule.

  [1.25] Blad preg_match jest rzutowany na bool i nieodrozniallny od braku dopasowania — awaria dopasowania konczy sie cicho segmentem 'Inne'.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:90
        status: prawdopodobne
        skutek: company_name zaimportowane ze starego systemu w ISO-8859-2 (np. 'IT
                ŁÓDŹ' w kodowaniu latin2) trafia do kontekstu; w konfiguracji bez
                mbstring haystack pozostaje niepoprawnym UTF-8, preg_match zwraca
                false, needle 'it' nigdy nie moze trafic, segment = 'Inne'. W logu ani
                w wyniku nie ma sladu, ze dopasowanie w ogole sie nie wykonalo.
        naprawa: Sprawdzic wynik jawnie: $m = preg_match(...); if ( false === $m ) — zalogowac/zwrocic MP_Result::fail (lub wynik z flaga bledu) zamiast traktowac blad jak brak dopasowania.

  [1.25] Deklarowany „defense-in-depth” agenta 5.3 nie działa dla żadnego wejścia poza handlerem AJAX: jedyne miejsce inkrementu to pre-gate AJAX, więc dla innego punktu wejścia licznik pozostaje zerowy, a warunek over_limit() nigdy nie będzie prawdziwy.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-05.php:162
        status: prawdopodobne
        skutek: Dodany zostaje drugi punkt wejścia uruchamiający pipeline (np.
                endpoint REST albo shortcode POST) bez wywołania increment().
                Atakujący zalewa go 1000 żądaniami z jednego IP: transient nigdy
                nie rośnie, over_limit() zawsze zwraca false, rate_ok = true, QA5
                przepuszcza każde żądanie — zabezpieczenie deklarowane jako druga
                warstwa nie zatrzymuje niczego.
        naprawa: Przenieść inkrement do samego agenta 5.3 (idempotentnie na żądanie, np. flaga w MP_Context ustawiana przez pre-gate) albo udokumentować wprost, że rate limit obowiązuje wyłącznie przez pre-gate AJAX, i wymusić jego wywołanie w każdym punkcie wejścia uruchamiającym pipeline.

  [1.25] Znacznik czasu zgody zapisywany jest w czasie lokalnym strony, bez wariantu GMT i bez informacji o strefie — dowod zgody jest niejednoznaczny w czasie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-06.php:80
        status: prawdopodobne
        skutek: Przy cofnieciu zegara (koniec czasu letniego) ta sama godzina lokalna
                wystepuje dwa razy: zapis `2026-10-25 02:30:00` moze oznaczac dwie
                rozne chwile, wiec kolejnosci dowodow zgody nie da sie odtworzyc.
                Analogicznie po zmianie strefy czasowej w ustawieniach WordPress
                starsze i nowsze znaczniki `consent_*_at` sa w roznych strefach, wiec
                porownania (np. "zgoda przed/po zmianie klauzuli") daja bledny wynik.
        naprawa: Zapisywac czas UTC: `$now = current_time( 'mysql', true );` albo dodac rownolegle kolumny `consent_rodo_at_gmt` / `consent_marketing_at_gmt` (konwencja WordPress: para lokalna + GMT).

  [1.25] Bramka QA działu 9 potwierdza „proces rozpoczęty" bez sprawdzenia, czy proces ma w ogóle identyfikator (process_id/lead_id). Agent 9.1 zwraca sukces również dla process_id = 0, a agent 9.3 świadomie zapisuje wtedy wiersz logu z lead_id = NULL i mimo to ustawia process_started = true.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-09.php:130
        status: prawdopodobne
        skutek: Kontekst dociera do działu 9 bez lead_id (np. dział 7 nie utworzył
                leada, a przebieg został wznowiony od dalszego działu). Wtedy:
                process_id = 0, w wp_mp_activity_log powstaje wpis process_started z
                lead_id = NULL i meta_json {"process_id":0}, agent 9.3 zwraca
                process_started = true, a QA9 zwraca d9_process_ready = true. Pipeline
                raportuje rozpoczęcie obsługi leada, którego nie ma, a log
                aktywności zawiera sierocy wpis telemetryczny nie do powiązania z
                żadnym leadem.
        naprawa: W agencie 9.1 zwracać MP_Result::fail (np. kod 'missing_lead_id'), gdy `$process_id < 1`, i/lub dołożyć w QA9 warunek `(int) $context->get( 'process_id', 0 ) > 0` obok kontroli stage i process_started.

  [1.25] Komentarz przy 9.2 deklaruje, że started_ts jest znacznikiem „monotonicznym", podczas gdy microtime( true ) czyta zegar ścienny systemu — ta sama klasa źródła co current_time(). Wartość może się cofnąć lub przeskoczyć, a dział 11 liczy z niej czas trwania.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-09.php:64
        status: prawdopodobne
        skutek: Między pobraniem started_ts w dziale 9 a odczytem w dziale 11 zegar
                systemowy zostaje skorygowany (krok NTP, ręczna zmiana czasu). Dział
                11 wylicza wtedy czas trwania jako wartość ujemną albo zawyżoną o
                wielkość korekty i zapisuje ją jako prawdziwą telemetrię — bez
                żadnego sygnału błędu, bo nic nie waliduje znaku ani rzędu
                wielkości różnicy.
        naprawa: Albo poprawić komentarz (usunąć słowo „monotoniczny", bo microtime() nim nie jest), albo — jeśli odporność na skok zegara jest wymagana — użyć hrtime( true ) i przeliczać nanosekundy na sekundy; niezależnie od wyboru dział 11 powinien odrzucać ujemny czas trwania.

  [1.25] Zaokraglenie 'polowkowe w gore' przez bcadd(..., '0.5', 0) dziala poprawnie tylko dla wartosci dodatnich — dla ujemnych bcadd ze skala 0 obcina w strone zera, wiec sciezka BCMath i sciezka fallback daja rozne wyniki, wbrew komentarzowi deklarujacemu ich zgodnosc.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:44
        status: prawdopodobne
        skutek: Wartosc ujemna w snapshocie (korekta, rabat kwotowy, blednie
                skonfigurowana cena '-1.00'): sciezka BCMath liczy bcmul -> '-100.00',
                bcadd('-100.00','0.5',0) -> '-99' (obciecie w strone zera), a fallback
                round(-100.0) -> -100. Ta sama oferta wyceniona na serwerze z
                rozszerzeniem BCMath i bez niego rozni sie o 1 grosz — czyli
                dokladnie rozjazd miedzy srodowiskami, ktory dzial mial eliminowac.
                Zaden agent ani QA tego nie wykryje, bo obie wartosci sa intami i sumy
                pozostaja spojne.
        naprawa: Obsluzyc znak jawnie, np. dodac '0.5' dla wartosci >= 0 i '-0.5' dla ujemnych (albo uzyc jednej implementacji zaokraglania dla obu sciezek), i pokryc testem przypadek ujemny oraz .5 dokladnie.

  [1.25] Kod bledu i komunikat QA nie odpowiadaja rzeczywistej przyczynie: 'missing_subtotal' / 'Brak sumy kontrolnej pozycji.' jest zwracany takze wtedy, gdy suma istnieje, ale jest ujemna.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:129
        status: prawdopodobne
        skutek: Pozycja w snapshocie ma qty ujemne (korekta/zwrot) lub cene ujemna ->
                subtotal_grosze wychodzi ujemny -> dzial przerywa z komunikatem 'Brak
                sumy kontrolnej pozycji.' i kodem 'missing_subtotal'. Obsluga i logi
                wskazuja na brak danych z Agenta 4.2, podczas gdy faktyczna przyczyna
                to zle qty/cena w danych wejsciowych — diagnostyka kieruje na zla
                warstwe pipeline'u.
        naprawa: Rozdzielic przypadki: sprawdzic obecnosc klucza (null === $context->get('subtotal_grosze') -> 'missing_subtotal'), a ujemna wartosc raportowac osobnym kodem np. 'negative_subtotal'; dodatkowo w Agencie 4.2 odrzucac qty < 1 z wlasnym kodem bledu zamiast cicho mnozyc przez 0 lub liczbe ujemna.

  [1.25] Pole `method` ze słownika reguł nie jest nigdy odczytywane — metoda naliczania jest zaszyta na sztywno (od `subtotal_grosze`). Konfiguracja sugeruje wybór metody per reguła, którego kod nie realizuje ani nie waliduje.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-05.php:103
        status: prawdopodobne
        skutek: Przy rozszerzeniu słownika o regułę z `'method' => 'per_item'`
                (pole wygląda na obsługiwane) rabat i tak zostanie policzony jako
                `intdiv( $subtotal_grosze * percent, 100 )`. Dla koszyka, gdzie tylko
                część pozycji miała podlegać rabatowi, kwota rabatu wyjdzie
                zawyżona, dział zwróci OK, a krytyk K5.2 i QA5 tego nie wychwycą
                — nie sprawdzają metody.
        naprawa: Albo usunąć pole `method` ze słownika (metoda jest jedna, jawnie w kodzie), albo dodać jawną walidację: `if ( 'total' !== $best['method'] ) return MP_OB_Result::fail( ..., 'unsupported_discount_method' );` i przenieść `method` do wyniku.

  [1.25] Agent 8.2 wchodzi w tryb 'correction' na podstawie samego `existing_offer_number` i nie waliduje `existing_version`; rzutowanie `(int)` zamienia brakującą lub niepoprawną wersję w wartość, która albo daje mylący komunikat STOP, albo cicho przechodzi jako wersja 1.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-08.php:116
        status: prawdopodobne
        skutek: (a) Snapshot Działu 2 ma `existing_offer_number = 'OF/2026/000123'`,
                ale nie ma klucza `existing_version` (np. korekta wywołana z draftu,
                gdzie wersja nie została odczytana). Agent 8.2 zwraca version 1,
                parent_version null, tryb 'correction'; Krytyk 8.2 przepuszcza; QA8
                odrzuca dział komunikatem 'Numeracja niejednoznaczna: ani czysto nowy
                numer, ani czysta korekta wersji' (kod 'numbering_mode_ambiguous') —
                status opisuje niejednoznaczność trybu, choć faktyczną przyczyną
                jest brak wersji w snapshocie; diagnostyka prowadzi w złe miejsce.
                (b) `existing_version` ma wartość niebędącą liczbą albo 0 (np.
                '' z kolumny NULL odczytanej jako pusty napis) — `(int)` daje 0,
                wynik to (OF/2026/000123, version 1, parent 0), co przechodzi Krytyka
                8.2 i QA8 i przy zapisie duplikuje istniejący wiersz wersji 1 tej
                oferty.
        naprawa: W Agencie 8.2, gdy `$existing_number` jest niepuste, wymagać `isset( $numbering['existing_version'] )` oraz wartości całkowitej >= 1 i w przeciwnym razie zwracać własny fail z kodem 'missing_existing_version' / 'invalid_existing_version'. Dodatkowo w Krytyku 8.2 wymagać `parent_version >= 1` dla trybu 'correction'.

  [1.25] Rok jest wyliczany dwukrotnie i niezależnie — raz w Agencie 8.1, raz w Krytyku 8.1 — a fallback `current_time( 'Y' )` łamie deklarowaną w komentarzu zasadę „jednego odczytu w jednym punkcie w czasie”.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-08.php:89
        status: prawdopodobne
        skutek: Snapshot Działu 2 nie zawiera klucza `year` (np. starszy/awaryjny
                ścieżka budowy kontekstu). Przebieg startuje 31.12 o 23:59:59, Agent
                8.1 wylicza rok 2026 i kandydata OF/2026/000124; Krytyk 8.1 uruchamia
                się po północy, `current_time('Y')` zwraca 2027, regex oczekuje
                OF/2027/… i dział pada z 'invalid_candidate_number' mimo poprawnego
                kandydata. Awaria jest bezpieczna (STOP, nie zły numer), ale blokuje
                ofertę i jest nieodtwarzalna w diagnostyce.
        naprawa: Wyliczyć rok raz — albo wymagać `numbering['year']` (fail z jasnym kodem, gdy brak, skoro Dział 2 ma go dostarczać), albo pozwolić Krytykowi 8.1 czytać rok wyłącznie z tego samego źródła co agent (np. agent zwraca `year` w danych wyniku, a krytyk waliduje względem niego).

  [1.26] Ostrzezenie w adminie twierdzi, ze wtyczka 'sprobowala automatycznie dolozyc link ... w wykrytym menu strony (element <nav>)' i kaze to sprawdzic na stronie, mimo ze proba wstrzykniecia moze byc calkowicie wylaczona filtrem 'mp_lead_intake_menu_html_fallback'. Komunikat opisuje wtedy dzialanie, ktore nigdy nie nastapilo.
        mp-lead-intake/includes/class-mp-page.php
        status: prawdopodobne
        skutek: Deweloper motywu wylacza fallback HTML filtrem (typowa reakcja na
                wtyczke modyfikujaca cudzy markup). Admin widzi ostrzezenie, otwiera
                front, przeszukuje nawigacje w poszukiwaniu linku, ktorego wtyczka
                nawet nie probowala dodac, i traci czas na weryfikacje nieistniejacego
                mechanizmu zamiast od razu dodac wpis menu recznie.
        naprawa: W maybe_admin_notice() sprawdzic ten sam filtr co maybe_start_menu_buffer() i przy wylaczonym fallbacku drukowac wariant komunikatu bez zdania o probie automatycznego wstawienia — samo polecenie recznego dodania linku pod podanym adresem.

  [1.26] Fallback HTML wstawia link 'Zapytanie ofertowe' do PIERWSZEGO elementu <nav> w wyrenderowanym dokumencie, bez rozrozniania roli tej nawigacji. Klient moze zobaczyc pozycje oferty doklejona do okruszkow, nawigacji pomocniczej lub paginacji, gdzie czyta sie ona jako cos innego niz link do formularza.
        mp-lead-intake/includes/class-mp-page.php
        status: prawdopodobne
        skutek: Motyw renderuje na wpisie najpierw <nav class="breadcrumbs"> (Strona
                glowna / Blog / Tytul wpisu), a dopiero nizej menu glowne. Wariant B
                dopasowuje pierwszy <nav> i dokleja <a> przed jego </nav>, wiec klient
                czyta sciezke 'Strona glowna / Blog / Tytul wpisu Zapytanie ofertowe'
                — link wyglada na fragment okruszkow (albo na kolejny krok sciezki),
                a nie na pozycje menu, wiec nie zostaje klikniety; jednoczesnie
                znacznik data-mp-lead-intake blokuje wstawienie linku w prawdziwym
                menu.
        naprawa: Ograniczyc dopasowanie do nawigacji glownej: preferowac <nav> z atrybutem/klasa wskazujaca menu glowne (np. role="navigation" z aria-label, klasy main/primary/site-navigation) albo <nav> zawierajacy <ul> z wieloma <li>, a przy braku takiego dopasowania nie wstrzykiwac nic i zostawic wylacznie ostrzezenie w adminie.

  [1.26] W notify_admin() blokada częstotliwości jest ustawiana PRZED wysyłką, a wynik wp_mail() nie jest sprawdzany. Nieudana wysyłka nie zostawia śladu i blokuje kolejne powiadomienie z tego działu na 15 minut.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:130
        status: prawdopodobne
        skutek: Serwer SMTP odrzuca wiadomość (np. chwilowy błąd 421). wp_mail()
                zwraca false, ale transient już jest ustawiony — kolejne błędy w
                tym samym dziale przez 15 minut również nie wywołają maila.
                Administrator nie zobaczy żadnego komunikatu o serii zatrzymanych
                leadów, mimo że kod „zdaje raport”, iż powiadomienie zostało
                wysłane.
        naprawa: Ustawiać transient dopiero po pomyślnej wysyłce: if ( wp_mail( $to, $subject, $body ) ) { set_transient( $throttle_key, 1, 15 * MINUTE_IN_SECONDS ); } — a przy false dopisać wpis do logu aktywności, że powiadomienie się nie udało.

  [1.26] Surowy $e->getMessage() trafia do kolumny description w wp_mp_activity_log oraz do treści maila, bez ograniczenia długości i bez filtrowania. Komunikaty wyjątków rutynowo zawierają dane wejściowe, więc dane osobowe leada mogą wylądować w logu przeglądanym przez pracowników i w niezaszyfrowanej poczcie.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:83
        status: prawdopodobne
        skutek: Wyjątek z warstwy bazy lub z subskrybenta mp_lead_created niesie w
                komunikacie fragment zapytania z danymi leada (e-mail, telefon).
                Treść wpada do description w BD-3, gdzie widzi ją każdy pracownik
                mający dostęp do Logu aktywności, i do maila wysyłanego przez
                wp_mail() bez szyfrowania — dane osobowe klienta wychodzą poza
                rekord leada, do którego dostęp miał być ograniczony.
        naprawa: Ograniczyć i oczyścić komunikat przed zapisem/wysyłką, np. $safe = sanitize_text_field( mb_substr( $e->getMessage(), 0, 200 ) ); w description i w mailu zostawić typ wyjątku, plik i linię, a pełny komunikat kierować do error_log() po stronie serwera.

  [1.26] Numer złożony z 10 jednakowych cyfr jest odrzucany jako placeholder, ale komunikat mówi o niepoprawnej sumie kontrolnej — czyli podaje człowiekowi fałszywy powód odrzucenia.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:45
        status: prawdopodobne
        skutek: Osoba wpisująca testowo 0000000000 (albo klient robiący literówkę
                na klawiaturze numerycznej) widzi zarzut o błędnej sumie kontrolnej.
                Pracownik, który zweryfikuje ten numer kalkulatorem NIP, zobaczy, że
                suma kontrolna jest poprawna, i uzna weryfikator za zepsuty —
                zgłosi fałszywy błąd zamiast poprosić klienta o prawdziwy numer.
        naprawa: Rozróżnić powód odrzucenia (np. checksum_valid zwracające kod powodu) i dla placeholdera dać komunikat typu 'Ten numer wygląda na wypełnienie testowe — podaj rzeczywisty NIP firmy'.

  [1.26] Komunikat błędu `wrong_status` nie podaje ani stanu, w jakim jest oferta, ani żadnej czynności do wykonania — pracownik nie może na jego podstawie nic zrobić.
        mp-offer-builder/includes/class-mp-offer-builder-approval.php:255
        status: prawdopodobne
        skutek: Pracownik klika „Zatwierdź” przy ofercie w statusie innym niż
                draft i approved (np. odrzuconej lub wycofanej). Dostaje czerwony
                komunikat bez nazwy stanu i bez wskazówki — nie wie, czy ma
                utworzyć nową wersję oferty, przywrócić ją do szkicu, czy
                zgłosić błąd. Akcja zatrzymuje się, a sprawa trafia do
                administratora zamiast zostać rozwiązana na miejscu.
        naprawa: Dołączyć do komunikatu bieżący status oferty i następny krok, np. „Oferta ma status %s — zatwierdzić można tylko szkic. Utwórz nową wersję oferty.”, przekazując status przez `WP_Error` (dane błędu) do notice().

  [1.26] Wpis w dzienniku BD-2 twierdzi, że zdarzenie `mp_offer_approved` zostało wystawione, ale powstaje ZANIM `do_action()` zostanie wywołane, i nigdy nie jest korygowany, gdy obsługa zdarzenia padnie.
        mp-offer-builder/includes/class-mp-offer-builder-approval.php:166
        status: prawdopodobne
        skutek: Plugin 3 (albo inny subskrybent haka) kończy obsługę zatwierdzenia
                błędem krytycznym i nie zakłada zadania wysyłki. Pracownik otwiera
                historię oferty i czyta, że zdarzenie zostało wystawione, więc
                zakłada, że moduł sprzedażowy dostał ofertę i wysyłka jest w
                toku. Nikt nie ponawia zatwierdzenia (blokuje je kod
                `already_approved`), a oferta nigdy nie trafia do klienta.
        naprawa: Rozdzielić fakty w dzienniku od skutków: w opisie zapisywać tylko samo zatwierdzenie dokumentu, a po powrocie z `do_action()` dopisywać drugi wpis potwierdzający obsługę zdarzenia (albo zapisywać w meta_json wynik `has_action`/liczbę odbiorców), żeby brak obsługi był widoczny w historii.

  [1.26] Gdy numer dzialu jest nieznany, log_exception() wstawia go do tematu maila, tresci maila i opisu w BD-2 jako liczbe 0 — czlowiek widzi komunikat o awarii w nieistniejacym "dziale 0" zamiast informacji, ze miejsca awarii nie ustalono.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:107
        status: prawdopodobne
        skutek: Wyjatek leci poza petla dzialow (np. przy inicjalizacji kontekstu),
                wywolanie idzie z $dept_num = 0. Administrator dostaje maila
                "Nieoczekiwany wyjatek w pipeline (dzial 0)" i pracownik szuka bledu w
                nieistniejacym dziale nr 0 zamiast potraktowac awarie jako
                nieprzypisana do dzialu.
        naprawa: Dla $dept_num <= 0 podstawiac w temacie/tresci/opisie etykiete tekstowa, np. "dzial: nieznany", zamiast liczby 0.

  [1.26] Ostrzeżenie o nginx wyświetla się tylko wtedy, gdy `$_SERVER['SERVER_SOFTWARE']` zawiera literalnie 'nginx'; przy pustej lub innej wartości administrator nie zobaczy żadnego komunikatu, mimo że .htaccess i tak nie działa.
        mp-offer-builder/mp-offer-builder.php:123
        status: prawdopodobne
        skutek: Serwis stoi na OpenResty (SERVER_SOFTWARE = 'openresty/1.25.x') lub
                Caddy, albo na nginx+PHP-FPM, gdzie parametr SERVER_SOFTWARE nie jest
                przekazywany do FastCGI (wtedy $server = ''). Wszystkie te serwery
                ignorują pliki .htaccess, ale warunek zwraca return i administrator
                nie dostaje żadnej informacji — pozostaje w przekonaniu, że
                katalog prywatny z ofertami PDF ma obronę w głąb, której
                faktycznie nie ma.
        naprawa: Odwrócić warunek: pokazywać ostrzeżenie, gdy NIE wykryto Apache/LiteSpeed (`stripos($server,'apache')` / `'litespeed'`), z osobnym wariantem tekstu dla nieznanego/pustego SERVER_SOFTWARE ('nie udało się rozpoznać serwera — jeśli to nie Apache, dodaj poniższy blok').

  [1.26] Nazwa nadawcy trafia do naglowka `From` w postaci zakodowanej encjami HTML, wiec kazdy klient widzi w skrzynce przeklamana nazwe firmy.
        mp-sales-workflow/includes/class-mp-sw-mailer.php:94
        status: prawdopodobne
        skutek: Witryna nazywa sie „Kowalski & Syn” (albo zawiera cudzyslow lub
                apostrof). Opcja przechowuje `Kowalski &amp; Syn`, filtr
                `wp_mail_from_name` zwraca ten sam ciag, a klient dostaje maila od
                nadawcy widocznego jako „Kowalski &amp; Syn”. Powiadomienie o
                ofercie wyglada na wygenerowane przez zepsuty automat i obniza
                wiarygodnosc korespondencji handlowej.
        naprawa: Przed czyszczeniem zdekodowac encje, tak jak robi to rdzen WordPressa w mailach systemowych: `self::header_safe( wp_specialchars_decode( get_bloginfo( 'name' ), ENT_QUOTES ) )`.

  [1.26] Wpis w dzienniku podaje jedna liczbe `count` bedaca suma wierszy z dwoch roznych tabel, wiec czytajacy dziennik pracownik nie moze stwierdzic, czy zanonimizowano wiersz procesu, czy tylko kolejke.
        mp-sales-workflow/includes/class-mp-sw-privacy.php:152
        status: prawdopodobne
        skutek: Po reklamacji klienta („moje dane nadal u was sa") pracownik otwiera
                dziennik, widzi wpis MP3-E000 z `count: 1` i uznaje, ze usuniecie
                zadzialalo. W rzeczywistosci `1` moglo pochodzic wylacznie z jednego
                wiersza kolejki, podczas gdy UPDATE na wierszu procesu zwrocil `false`
                (blad) — adres klienta nadal jest w `client_email`. Dziennik nie
                pozwala tego rozroznic.
        naprawa: Logowac dwa osobne pola, np. `flow_rows` i `queue_rows` (oraz flage bledu z `$wpdb->last_error`), zamiast jednej sumy.

  [1.26] Komunikat bledu 'invalid recipient' jest twardo wpisany po angielsku, bez __() i bez informacji, co zrobic ani ktorego adresu dotyczy.
        mp-sales-workflow/includes/class-mp-sw-queue.php:155
        status: prawdopodobne
        skutek: Handlowiec szuka, czemu klient nie dostal potwierdzenia, i w kolumnie
                last_error widzi 'invalid recipient' — angielski wpis wsrod polskich
                komunikatow, ktory nie mowi, jaki adres byl niepoprawny ani gdzie go
                poprawic i czy trzeba zakolejkowac wiadomosc ponownie. Efekt:
                zgloszenie ladzie u programisty zamiast zostac naprawione w karcie
                leada.
        naprawa: Zapisac tlumaczony i wykonalny komunikat, np. __( 'Nieprawidłowy adres e-mail odbiorcy — popraw adres w karcie leada i zakolejkuj powiadomienie ponownie.', 'mp-sales-workflow' ), ewentualnie z oczyszczona wartoscia adresu, ktora zostala odrzucona.

  [1.26] find_gaps() nie zabezpiecza się przed pustą listą języków — przy $langs = array() pętla języków nie wykonuje się ani razu i funkcja raportuje brak braków, mimo że wszystkie treści mogą być puste.
        mp-sales-workflow/includes/class-mp-sw-templates.php:149
        status: prawdopodobne
        skutek: Wywołanie find_gaps( MP_SW_Templates::all(), array() ) (np. gdy
                konfiguracja witryny nie zwróciła listy języków) dla zestawu, w
                którym filtr wyzerował 'pl'/'en', zwraca pustą listę braków.
                Snapshot Działu 2 pokazuje człowiekowi 'brak braków', po czym
                system wysyła klientowi e-mail z pustym tematem i pustą treścią
                — dokładnie ta sytuacja, której docblock ('braki mają być
                widoczne, a nie ciche') ma zapobiegać.
        naprawa: Na wejściu find_gaps() sprawdzić, czy $langs jest niepuste; przy pustej liście dopisać do wyniku jawny brak (np. 'langs.missing') albo użyć domyślnego zestawu array( 'pl', 'en' ).

  [1.26] Lista braków zwracana przez find_gaps() i prezentowana człowiekowi w snapshocie Działu 2 składa się z surowych identyfikatorów technicznych, bez informacji, co należy zrobić.
        mp-sales-workflow/includes/class-mp-sw-templates.php:146
        status: prawdopodobne
        skutek: Filtr witryny gubi wersję szablonu oferty. Administrator otwiera
                snapshot Działu 2 i widzi wyłącznie 'offer_sent.version' —
                komunikat nie mówi, że chodzi o szablon e-maila do klienta, jaki
                jest skutek (wysyłki bez odtwarzalnej wersji w dzienniku) ani jak to
                naprawić, więc pozycja zostaje zignorowana.
        naprawa: Zwracać strukturę zamiast gołych ciągów (klucz, czytelna nazwa szablonu, odbiorca, czego brakuje, sugerowane działanie) albo dorobić metodę mapującą identyfikator braku na zdanie dla człowieka używane w snapshocie.

  [1.26] Komunikaty bledu krytyka K7.1 podaja wewnetrzny klucz szablonu i nie mowia pracownikowi, co ma zrobic, zeby odblokowac zatrzymany proces.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:360
        status: prawdopodobne
        skutek: Handlowiec zmienia status procesu prowadzonego po angielsku, brakuje
                wersji 'en' szablonu. Zmiana statusu jest zablokowana, a jedyne co
                widzi to 'Szablon tpl_offer_sent nie ma wersji w jezyku en'. Nie wie,
                czy to jego blad, gdzie sa szablony ani czy ma czekac — proces stoi,
                a zgloszenie do administratora nie zawiera nic wiecej.
        naprawa: Dopisac do obu komunikatow zdanie z konkretnym dzialaniem, np. „Uzupelnij wersje jezykowa w Ustawieniach → Szablony e-mail albo zglos to administratorowi (kod: template_missing)”, zachowujac klucz szablonu jako dane techniczne obok, nie zamiast instrukcji.

  [1.26] Wartownik transakcyjny (TOCTOU) jest budowany wylacznie z pierwszego zadania listy $tasks['fire']; pozostale wypadajace zadania nie maja sprawdzenia stanu w transakcji, choc ich powiadomienia sa zapisywane w tej samej transakcji.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:272
        status: prawdopodobne
        skutek: W jednym przebiegu wypadaja dwa zadania o roznych wartosciach
                guard_status. Sprawdzany jest tylko warunek pierwszego; jesli on
                przechodzi, transakcja sie zatwierdza razem z powiadomieniem drugiego
                zadania, ktorego warunek juz nie obowiazuje. Odbiorca (handlowiec, a
                przy szablonach klienckich takze klient) dostaje przypomnienie o
                etapie, ktory w miedzyczasie zostal zamkniety — komunikat sprzeczny
                z aktualnym stanem sprawy.
        naprawa: Sprawdzac wartownika dla kazdego wypadajacego zadania (np. lista warunkow zamiast jednego pola 'guard'), a zadania, ktorych warunek nie jest juz spelniony, domykac jako anulowane i usuwac ich powiadomienia z kolejki przed COMMIT.

  [2.6] Test dla bledu P2-K2 nie przyszedl w jednym commicie z naprawa.
        mp-offer-builder/tests/koncowe/zatwierdzenie-oferty.php
        status: prawdopodobne
        skutek: Nie ma sladu, ze ten test kiedykolwiek FAIL-owal na kodzie sprzed
                naprawy. Moze wiec sprawdzac cos innego niz sadzimy — a wtedy nawrot
                bledu przejdzie przez niego bez zatrzymania.
        naprawa: Przy nastepnej naprawie: `git stash` zrodla, uruchomic test, potwierdzic FAIL, `git stash pop`, i dopiero wtedy commitowac oba pliki razem.

OBSERWACJE (73):
  [1.6] Zdarzenie „mp_lead_intake_after_form" wystawiane, ale nikt go nie slucha w projekcie.
        mp-lead-intake/includes/class-mp-form.php:174
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.6] Zdarzenie „mp_lead_verified" wystawiane, ale nikt go nie slucha w projekcie.
        mp-lead-intake/includes/class-mp-vat-verifier.php:232
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.6] Zdarzenie „mp_sw_queue_halted" wystawiane, ale nikt go nie slucha w projekcie.
        mp-sales-workflow/includes/class-mp-sw-mailer.php:196
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.6] Zdarzenie „mp_sw_flow_anonymized" wystawiane, ale nikt go nie slucha w projekcie.
        mp-sales-workflow/includes/class-mp-sw-privacy.php:161
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.6] Zdarzenie „mp_sw_notification_sent" wystawiane, ale nikt go nie slucha w projekcie.
        mp-sales-workflow/includes/class-mp-sw-queue.php:184
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.6] Zdarzenie „mp_sw_notification_failed" wystawiane, ale nikt go nie slucha w projekcie.
        mp-sales-workflow/includes/class-mp-sw-queue.php:212
        status: prawdopodobne
        skutek: Albo to swiadomy punkt rozszerzen dla integratora (wtedy w porzadku i
                powinno byc opisane w dokumentacji), albo odbiorca zginal przy
                refaktorze.

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-lead-intake/includes/db/class-mp-db.php:590
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-lead-intake/includes/db/class-mp-db.php:623
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-lead-intake/includes/db/class-mp-db.php:658
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-offer-builder/includes/db/class-mp-offer-builder-db.php:617
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-sales-workflow/includes/db/class-mp-sales-workflow-db.php:258
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-sales-workflow/includes/db/class-mp-sales-workflow-db.php:329
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.8] Zapytanie ze zmienna bez `prepare()`.
        mp-sales-workflow/includes/db/class-mp-sales-workflow-db.php:402
        status: prawdopodobne
        skutek: Wartosc sterowana przez uzytkownika trafia do zapytania bez
                przygotowania (CWE-89).

  [1.23] Status „done" wpisany literalem, choc istnieje dla niego stala.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:349
        status: prawdopodobne
        skutek: Zmiana wartosci stalej ominie to miejsce. Rekord zostanie w stanie,
                ktorego reszta kodu juz nie rozpoznaje.
        naprawa: Uzyc stalej STATUS_* zamiast napisu.

  [1.11] Kolumna osobowa „ip_address" nie pojawia sie w kodzie anonimizacji.
        mp-lead-intake/includes/db/class-mp-db.php
        status: prawdopodobne
        skutek: Po anonimizacji ta kolumna zachowa dane osobowe. Rekord bedzie
                wygladal na zanonimizowany, bo adres e-mail sie zmienil.

  [1.12] Kwota liczona zmiennoprzecinkowo (rzutowanie na float).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-04.php:48
        status: prawdopodobne
        skutek: Typ float nie umie zapisac 0,10 dokladnie. Po kilku pozycjach suma
                rozjedzie sie o grosz — i to grosz, ktorego nie da sie wytlumaczyc
                klientowi ani ksiegowej. Pieniadze trzyma sie w groszach jako liczbe
                calkowita.
        naprawa: Przeliczyc na grosze (int) i wykonywac dzialania na liczbach calkowitych.

  [1.12] Cena z WooCommerce pobierana bez ustalenia, czy zawiera VAT (ekran administratora).
        mp-offer-builder/includes/admin/class-mp-offer-builder-admin.php:149
        status: prawdopodobne
        skutek: W sklepie skonfigurowanym na ceny BRUTTO ta wartosc juz zawiera VAT.
                Doliczenie VAT-u drugi raz zawyza oferte o pelna stawke — u nas o
                23% (blad P2-K3). Zadna bramka tego nie zlapala, bo suma pozostawala
                wewnetrznie spojna.
        naprawa: Sprowadzic do netto przez wc_get_price_excluding_tax() po sprawdzeniu wc_prices_include_tax(); przy braku WooCommerce — twardy blad, nie domysl.

  [1.19] Wynik `wp_mail()` nie jest sprawdzany.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:107
        status: prawdopodobne
        skutek: Odrzucenie przez serwer poczty przejdzie niezauwazone. Handlowiec
                bedzie przekonany, ze oferta poszla do klienta.
        naprawa: Zapisac wynik i odnotowac porazke w dzienniku wysylek.

  [1.19] Wynik `wp_mail()` nie jest sprawdzany.
        mp-lead-intake/includes/pipeline/class-mp-pipeline-logger.php:145
        status: prawdopodobne
        skutek: Odrzucenie przez serwer poczty przejdzie niezauwazone. Handlowiec
                bedzie przekonany, ze oferta poszla do klienta.
        naprawa: Zapisac wynik i odnotowac porazke w dzienniku wysylek.

  [1.19] Wynik `wp_mail()` nie jest sprawdzany.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:104
        status: prawdopodobne
        skutek: Odrzucenie przez serwer poczty przejdzie niezauwazone. Handlowiec
                bedzie przekonany, ze oferta poszla do klienta.
        naprawa: Zapisac wynik i odnotowac porazke w dzienniku wysylek.

  [1.19] Wynik `wp_mail()` nie jest sprawdzany.
        mp-offer-builder/includes/pipeline/class-mp-ob-pipeline-logger.php:142
        status: prawdopodobne
        skutek: Odrzucenie przez serwer poczty przejdzie niezauwazone. Handlowiec
                bedzie przekonany, ze oferta poszla do klienta.
        naprawa: Zapisac wynik i odnotowac porazke w dzienniku wysylek.

  [1.19] Wynik `wp_mail()` nie jest sprawdzany.
        mp-sales-workflow/includes/class-mp-sw-mailer.php:209
        status: prawdopodobne
        skutek: Odrzucenie przez serwer poczty przejdzie niezauwazone. Handlowiec
                bedzie przekonany, ze oferta poszla do klienta.
        naprawa: Zapisac wynik i odnotowac porazke w dzienniku wysylek.

  [1.24] Plik testu bez ani jednej asercji.
        mp-sales-workflow/tests/koncowe/bramka-integracyjna-p1-p2-p3.php
        status: prawdopodobne
        skutek: Ten plik przechodzi ZAWSZE, takze wtedy, gdy sprawdzana funkcja jest
                calkowicie zepsuta. W podsumowaniu wyglada jak dowod poprawnosci.

  [1.24] Asercja oczekuje zera od wywolania — sprawdz, czy zero nie jest tez wynikiem awarii.
        mp-sales-workflow/tests/security/scenariusze-s1-s12.php:561
        status: prawdopodobne
        skutek: Dokladnie ten ksztalt dal falszywy PASS w tym projekcie (TEST-F1):
                straznik zwrocil zera, bo sie wywrocil, a test wlasnie zer oczekiwal.
                Test powinien najpierw potwierdzic, ze sprawdzana droga w ogole sie
                wykonala.
        naprawa: Dolozyc asercje pozytywna (np. licznik wywolan > 0) przed asercja na zero.

  [1.15] Test dla bledu P1-K2 istnieje, ale nie widac w nim sladu tego bledu.
        mp-lead-intake/tests/koncowe/granica-transakcji-i-role.php
        status: prawdopodobne
        skutek: Prawdopodobnie test sprawdza cos innego w tym samym obszarze. Przy
                nastepnym refaktorze ktos go uprosci, nie wiedzac, czego pilnowal.
        naprawa: Dopisac w naglowku testu, ktory blad z rejestru on zabezpiecza.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-lead-intake/includes/db/class-mp-db.php:656
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (3 w jednym obiegu).
        mp-offer-builder/includes/db/class-mp-offer-builder-db.php:322
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona
                120 zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie
                to przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-offer-builder/includes/db/class-mp-offer-builder-db.php:615
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (3 w jednym obiegu).
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:280
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona
                120 zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie
                to przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-sales-workflow/includes/db/class-mp-sales-workflow-db.php:239
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-sales-workflow/includes/db/class-mp-sales-workflow-db.php:376
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:576
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.16] Zapytanie do bazy wewnatrz petli (1 w jednym obiegu).
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php:615
        status: prawdopodobne
        skutek: Liczba zapytan rosnie z liczba pozycji. Oferta na 40 pozycji wykona 40
                zapytan zamiast jednego. Przy generowaniu PDF-a w tle konczy sie to
                przekroczeniem limitu czasu, a uzytkownik widzi tylko oferte, ktora
                „sie nie wygenerowala".
        naprawa: Pobrac komplet jednym zapytaniem (WHERE ... IN) przed petla.

  [1.25] Agent 3.1 nie normalizuje NIP-u, choć agenci 3.2 i 3.3 czytają to samo pole kontekstu z usuwaniem znaków niecyfrowych. Wejście w potocznym formacie z myślnikami/spacjami nigdy nie dochodzi do sumy kontrolnej i jest odrzucane komunikatem o błędnej sumie kontrolnej.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:65
        status: potwierdzone
        skutek: Formularz z NIP wpisanym jako '123-456-32-18' albo '1234563218 '
                (spacja na końcu): checksum_valid() zwraca false zanim policzy sumę
                → nip_valid=false → MP_Flag_Critic K3.1 odrzuca parę, a QA3
                kończy dział błędem 'nip_invalid'. Poprawny lead firmowy zostaje
                odrzucony, a komunikat 'Niepoprawna suma kontrolna NIP' opisuje coś,
                co nigdy się nie wydarzyło (suma nie została policzona — nie
                zgadzał się tylko format).
        naprawa: W MP_D3_Agent_Nip::run() (i/lub na wejściu checksum_valid) normalizować tak samo jak w 3.2/3.3: `$nip = preg_replace( '/\D+/', '', (string) $context->get( 'nip', '' ) );`. Rozdzielić też komunikat błędu formatu (zła długość / brak NIP) od komunikatu o błędnej sumie kontrolnej.

  [1.25] Rabat jest rozdzielany na klasy podatkowe względem `subtotal_grosze` z kontekstu, a nie względem faktycznej sumy klas; resztę z zaokrągleń bezwarunkowo dorzuca się do największej klasy. Przy dowolnej różnicy między tymi dwiema podstawami suma netto klas przestaje być równa `net_grosze` i VAT liczy się od zaniżonej podstawy — mimo że komentarz obok deklaruje dokładnie odwrotną gwarancję.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-06.php:250
        status: prawdopodobne
        skutek: `subtotal_grosze = 10000`, `discount_total = 1000`, pozycje: klasa 23%
                o wartości 8000 i klasa 8% o wartości 1000 (jedna pozycja bez klucza
                `line_grosze` → liczona jako 0, suma klas 9000 ≠ 10000). Udziały:
                23% → floor(1000*8000/10000) = 800, 8% → floor(1000*1000/10000) =
                100, `$assigned = 900`, reszta 100 trafia do klasy 23% → jej netto
                7100, klasy 8% → 900. Suma netto klas = 8000, a `net_grosze = 9000`.
                VAT = 1633 + 72 = 1705 zamiast ok. 1935 przy prawidłowej podstawie
                — podatek zaniżony o ~230 gr, a `gross_grosze` mimo to spina się
                arytmetycznie i przechodzi QA.
        naprawa: Liczyć proporcje względem `array_sum( $class_subtotal )` (albo najpierw wymusić równość tej sumy z `$subtotal`, patrz ustalenie o `lines_subtotal_mismatch`) oraz po podziale zweryfikować niezmiennik: `array_sum( class_net ) === $net_grosze`, w przeciwnym razie FAIL zamiast OK.

  [1.25] W jednym wstawianym wierszu mieszane są dwie podstawy czasu: `salesman_assigned_at` zapisuje czas lokalny WordPressa, a `vat_checked_at` — GMT.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-07.php:175
        status: potwierdzone
        skutek: Instalacja ze strefą Europe/Warsaw (+2h latem): dla leada utworzonego
                o 12:00 czasu lokalnego wiersz ma `salesman_assigned_at = 12:00` i
                `vat_checked_at = 10:00`. Każde porównanie tych kolumn ze sobą albo
                filtr wieku sprawdzenia liczony w czasie lokalnym (np. „ponów
                weryfikację po X minutach") jest przesunięty o offset strefy.
        naprawa: Ustalić jedną podstawę czasu dla całej tabeli (najlepiej GMT, jak w `class-mp-vat-verifier.php`) i konwertować na czas lokalny dopiero przy prezentacji; jeśli lokalna podstawa jest wymagana, dodać kolumnę `*_gmt` zamiast mieszać jednostki w jednym wierszu.

  [1.25] Walidacja statusu w Agencie 10.1 jest warunkiem nigdy nieprawdziwym — porównuje stałą sama ze sobą.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-10.php:192
        status: prawdopodobne
        skutek: Nie istnieje wejście (żaden kontekst, żaden tryb numbering_mode,
                żaden stan draftu), dla którego ta gałąź się wykona — literał
                'draft' zawsze należy do ALLOWED_STATUSES. Deklarowana w dokumentacji
                kontrola „statusu ze słownika” nie chroni więc przed niczym: gdy
                w przyszłości status zacznie pochodzić z kontekstu (np. z plugina
                3), zapis pójdzie do bazy bez żadnej walidacji, bo test przy
                literale nigdy nie zgłosił problemu i sprawia wrażenie
                działającego.
        naprawa: Albo walidować faktyczne źródło statusu — np. $status = (string) $context->get( 'status', 'draft' ); i to $status wstawiać do nagłówka oraz sprawdzać przeciw ALLOWED_STATUSES — albo usunąć martwy warunek i zastąpić go komentarzem, że status jest w tym pipeline stałą (decyzja B).

  [1.25] Odbiorca-klient dostaje zawsze TPL_OFFER_SENT, niezaleznie od tego, jakie przejscie wywolalo efekt — brak odpowiednika salesman_template() po stronie klienta.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:185
        status: prawdopodobne
        skutek: Jesli maszyna stanow (Dzial 5) emituje EFFECT_NOTIFY_CLIENT przy
                wiecej niz jednym przejsciu (np. przy akceptacji lub odrzuceniu
                oferty, a nie tylko przy jej wyslaniu), klient dostanie tresc
                „oferta wyslana” dla zdarzenia, ktore czyms innym bylo — status
                w wiadomosci nie odpowiada temu, co naprawde sie stalo, i zaden krytyk
                tego nie wykryje, bo K7.1 sprawdza wylacznie istnienie szablonu i
                wersji jezykowej. Dossier nie zawiera Dzialu 5, wiec liczby przejsc z
                tym efektem nie da sie tu potwierdzic.
        naprawa: Wprowadzic `client_template( $to_status )` analogiczne do salesman_template() (mapa przejscie → szablon) albo — jesli EFFECT_NOTIFY_CLIENT ma z zalozenia jedno zrodlo — dodac w A7.1 asercje na `$transition['to']` i zakonczyc bledem, gdy efekt przychodzi z innego przejscia niz oczekiwane.

  [1.25] `event.status` planu jest zapisany na sztywno jako `'done'` w momencie budowania planu — przed wykonaniem jakiejkolwiek operacji zapisu; w A8.1 nie istnieje gałąź dająca inną wartość.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-08.php
        status: prawdopodobne
        skutek: Zdarzenie, w którym wartownik z `flow.guard` lub kontrola
                `lock_version` blokuje aktualizację wiersza procesu (UPDATE trafia w
                0 wierszy), ale transakcja nie jest wycofywana — dziennik dostaje
                wpis o statusie „done”, mimo że proces nie został zmieniony.
                Ponieważ dossier nie zawiera pary A8.2/A8.3, nie da się
                rozstrzygnąć, czy któraś z nich nadpisuje ten status; jeśli nie
                nadpisuje, dziennik nigdy nie odnotuje zdarzenia nieudanego, a po
                ROLLBACK nie zostanie po nim żaden ślad (skutek udokumentowany w
                nagłówku pliku).
        naprawa: Ustalać `event.status` w A8.2 na podstawie faktycznego wyniku zapisu (liczba dotkniętych wierszy, wynik kontroli wartownika i tokenu blokady), a w planie zostawiać wartość wstępną typu `pending`; zdarzenia zakończone konfliktem wersji lub blokadą wartownika logować poza transakcją, żeby ROLLBACK ich nie kasował.

  [1.25] Dwie galezie wczesnego zwrotu w A5.1 buduja tablice `transition` bez klucza `known_status`, ktory jest obecny w zwrocie glownym. Dane przekazywane dalej maja rozny ksztalt zaleznie od galezi.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-05.php:163
        status: prawdopodobne
        skutek: Konsument tablicy `transition` (Dzial 8 lub dziennik) odwolujacy sie
                do `$transition['known_status']` dostaje przy zdarzeniach
                `task.due`/`dashboard.view` klucz nieistniejacy — w PHP 8 to warning
                i wartosc null, interpretowana jako "status nieznany". Osobno: wiersz
                o statusie spoza slownika (np. pozostalosc po starszej wersji, `from`
                = 'draft_old') plus `status.change` na te sama wartosc daje
                `allowed=true` i wpis w dzienniku o legalnym przejsciu do statusu,
                ktorego maszyna nie zna.
        naprawa: Ujednolicic ksztalt tablicy `transition` we wszystkich trzech zwrotach (zawsze ustawiac `known_status`), a w galezi `$from === $to` uzaleznic `allowed=true` od `in_array( $to, MP_Sales_Workflow_DB::statuses(), true )`.

  [1.25] client.vat_status — jedyne pole z whitelisty sterujace podatkiem — nie ma ani whitelisty wartosci, ani normalizacji, w odroznieniu od wariant i lang.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-01.php:109
        status: prawdopodobne
        skutek: Zadanie w trybie 'new' z client.vat_status = "Valid" (inna wielkosc
                liter niz wartosc oczekiwana przez Dzial 6) przechodzi kontrakt bez
                bledu; jesli Dzial 6 porownuje scisle, mechanizm reverse_charge sie
                nie wlacza i oferta dla kontrahenta UE dostaje krajowa stawke VAT
                zamiast 0% (albo odwrotnie przy innej literowce) — bez zadnego
                sygnalu bledu. Kod Dzialu 6 nie jest czescia dossier, wiec skutek
                koncowy nie zostal potwierdzony.
        naprawa: Dodac stala ALLOWED_VAT_STATUSES i potraktowac vat_status tak jak wariant/lang: strtolower(trim()) + in_array(..., true), z jawnym bledem kontraktu dla wartosci spoza listy (pusty string dopuszczony jako 'brak statusu').

  [1.25] Oba warunki Krytyka 1.1 sa nieosiagalne dla wyniku ok agenta — krytyk nigdy nic nie odrzuci.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-01.php:228
        status: prawdopodobne
        skutek: Dla dowolnego wejscia obie galezie fail w krytyku sa martwe: para
                agent+krytyk raportuje 'zweryfikowane' bez jakiejkolwiek niezaleznej
                kontroli. Regresja w agencie, ktora przepuscilaby np. pozycje bez
                product_id albo klienta z wartoscia nieskalarna (patrz ustalenia
                wyzej), przechodzi przez krytyka i bramke QA bez sygnalu — bo krytyk
                powtarza tylko ten sam niezmiennik, ktory agent juz wymusil.
        naprawa: Przeniesc do krytyka kontrole niezalezna od agenta: sprawdzic, ze kazdy element items ma calkowity product_id > 0 i qty > 0, ze client zawiera wylacznie klucze z ALLOWED_CLIENT_KEYS o wartosciach skalarnych oraz ze offer_mode jest spojny z offer_id (draft <=> offer_id !== null).

  [1.25] Lista sekcji wymaganych przez bramkę QA2 nie obejmuje `lead` ani `offer` — czyli dokładnie tych sekcji, które niosą inwariant I-3 (adres z bazy, nie z koperty) i potwierdzenie oferty.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-02.php
        status: prawdopodobne
        skutek: Refaktor skracający ścieżkę (np. warunkowe pomijanie odczytu
                leada, gdy koperta niesie mało danych) usuwa klucz `lead` ze
                snapshotu. Bramka QA2 sprawdza pięć sekcji i przechodzi na zielono,
                a Dział 8 dostaje snapshot bez sekcji odbiorcy — brak wykrycia
                dokładnie w tym miejscu, które ma tego pilnować.
        naprawa: Dopisać 'lead' i 'offer' do `sections()`, tak aby lista kontrolna bramki pokrywała się z kluczami faktycznie budowanego snapshotu.

  [1.25] Bramka anty-znacznikowa reaguje na sam ciąg "{{" w całym dokumencie, także gdy pochodzi on z danych, a nie z szablonu.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-09.php:78
        status: prawdopodobne
        skutek: Nazwa/opis pozycji albo notatka pobrana z BD-2/WooCommerce zawiera
                sekwencję "{{" (np. wklejony fragment innego szablonu w opisie
                produktu). Cała generacja oferty pada z kodem unfilled_placeholder, a
                komunikat wskazuje na błąd szablonu, choć szablon jest poprawny —
                oferty dla tego produktu nie da się wygenerować do czasu edycji
                danych.
        naprawa: Sprawdzać dopasowanie do wzorca faktycznego znacznika (np. /\{\{\s*[a-z0-9_]+\s*\}\}/i) zamiast gołego "{{", albo prowadzić bramkę na szablonie przed scaleniem danych.

  [1.25] Dwa z trzech warunków kontrolnych K9.1 nie mogą stać się prawdziwe przy obecnym A9.1 — krytyk „jednokrotność" faktycznie sprawdza tylko jeden hak, a duplikat HOOK_QUEUE_READY nie jest kontrolowany wcale.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:213
        status: prawdopodobne
        skutek: Regresja w A9.1 polegająca na przeniesieniu `$dispatched[] = ...`
                poza `if ( emit(...) )` (albo na dodaniu trzeciego wystawienia
                HOOK_QUEUE_READY dla tego samego zdarzenia) przechodzi przez K9.1 bez
                zgłoszenia: obie nazwy haków pozostają unikalne, a licznik
                `emit_times` nie obejmuje HOOK_QUEUE_READY. Krytyk potwierdzi „nic
                dwa razy", choć niczego takiego nie zweryfikował dla kolejki, a
                odbiorcy dostaną podwójny sygnał o gotowej kolejce.
        naprawa: Oprzeć K9.1 na faktach z emitera, a nie na tablicy zbudowanej przez agenta: sprawdzać `MP_SW_D9_Emitter::times()` dla KAŻDEGO haka z `dispatched` (pętla po `array( HOOK_FLOW_UPDATED, HOOK_QUEUE_READY )`) i porównywać to z listą zwróconą przez agenta.

  [1.25] Odpowiedź raportuje w polu `tasks.closed` sumę dwóch różnych list planu (`close` + `fire`), więc liczba „zamkniętych zadań" nie odpowiada liczbie zadań faktycznie zamkniętych.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:278
        status: prawdopodobne
        skutek: Jeśli `fire` oznacza zadania uruchamiane/wyzwalane, a nie zamykane,
                albo jeśli to samo zadanie może wystąpić jednocześnie w `close` i
                `fire`, wywołujący dostanie np. `tasks.closed: 3` przy dwóch
                faktycznie zamkniętych zadaniach. Interfejs pokaże liczbę, której
                nie da się odtworzyć z bazy, a każdy licznik zbudowany na tej
                odpowiedzi (raport dzienny, test regresji) będzie zawyżony.
        naprawa: Raportować listy osobno (`'closed' => count( $plan['close'] ), 'fired' => count( $plan['fire'] )`, oba dopisane do `allowed_keys()`), albo — jeśli scalenie jest zamierzone — liczyć unikalne identyfikatory: `count( array_unique( array_merge( (array) $plan['close'], (array) $plan['fire'] ) ) )`.

  [1.25] Dział deklaruje w nagłówku Action Scheduler jako mechanizm uruchomienia kolejki, a implementuje WP-Cron (`wp_schedule_single_event`), który nie gwarantuje uruchomienia bez ruchu na stronie.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-09.php:115
        status: prawdopodobne
        skutek: Instalacja ze stałą `DISABLE_WP_CRON` i bez systemowego wywołania
                `wp-cron.php` (albo strona bez ruchu po godzinach): zadanie
                `mp_sw_run_queue` zostaje wpisane do `wp_options`, ale nigdy nie
                zostaje uruchomione. Powiadomienia leżą w bazie w nieskończoność,
                a dział 9 zwrócił `queue_scheduled: true` i `ok: true` — zgodnie
                z własnym kontraktem, więc nic tego nie wychwyci.
        naprawa: Albo użyć `as_schedule_single_action()` z fallbackiem do `wp_schedule_single_event()` (zgodnie z dokumentacją działu), albo poprawić nagłówek i dokumentację, tak by opisywały faktyczny mechanizm, i dopisać monitoring: kontrolę wieku najstarszego niewysłanego powiadomienia w kolejce.

  [1.25] Dokumentacja dzialu opisuje inne zrodlo czasu trwania niz kod: mowi o liczeniu wzgledem `started_at` z dzialu 9, podczas gdy kod uzywa `started_ts` (microtime) i celowo odrzucil wariant ze stringa 'mysql'.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-11.php:75
        status: prawdopodobne
        skutek: Osoba modyfikujaca dzial 9 zgodnie z dokumentacja usuwa lub przestaje
                ustawiac `started_ts` (bo dokumentacja wymienia tylko `started_at`);
                dzial 11 cicho przechodzi w galaz `duration_ms = null` opisana w
                ustaleniu powyzej i raport przestaje zawierac czas trwania, mimo ze
                zadna kontrola tego nie zglosi.
        naprawa: Zaktualizowac docs/dzial-11/zrodla-wordpress.md tak, by opisywal faktyczny kontrakt: `started_ts` (microtime z dzialu 9) jako zrodlo czasu trwania, a `current_time('mysql')` wylacznie jako `finished_at`.

  [1.25] Rabat czytany jest z klucza kontekstu bez sufiksu jednostki (`discount_total`), w odróżnieniu od wszystkich pozostałych kwot (`*_grosze`), a mimo to formatowany jako grosze.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: Jeśli Dział 5 zapisuje rabat pod inną nazwą (np.
                'discount_total_grosze'), get() zwróci wartość domyślną 0 →
                oferta z rabatem 500 zł pokaże 'Rabat: 0,00 zł' przy jednocześnie
                obniżonym net_total; jeśli zapisuje w złotych, ta sama linia
                pokaże kwotę 100× zaniżoną. W obu wariantach wynik działu to OK.
                Dossier nie zawiera kodu Działu 5, więc jest to sygnał do
                sprawdzenia, nie potwierdzona awaria.
        naprawa: Zweryfikować nazwę i jednostkę klucza wystawianego przez Dział 5 i ujednolicić do konwencji '*_grosze'; ewentualnie failować, gdy klucz w kontekście nie istnieje, zamiast cicho przyjmować 0.

  [1.25] Strażnik podmiany lokalizacji opiera się na Locale::VALID_LOCALE, a dossier nie zawiera dokumentacji ICU rozróżniającej VALID_LOCALE od ACTUAL_LOCALE — a to od tego rozróżnienia zależy, czy strażnik w ogóle wykryje opisaną w komentarzu awarię. Dodatkowo klasa Locale jest używana bez własnego class_exists, a cała ścieżka intl nie jest wykonywana w harnessie (brak rozszerzenia intl potwierdzony w dokumentacji działu), więc pozostaje niesprawdzona testem.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-07.php
        status: prawdopodobne
        skutek: (a) Build intl z okrojonymi danymi ICU, w którym
                getLocale(VALID_LOCALE) raportuje żądane 'pl_PL', choć dane
                pochodzą z root/en → strpos przechodzi → format() daje angielskie
                separatory → polska oferta z '2,099.98 zł', czyli dokładnie ta
                awaria, którą komentarz uznaje za naprawioną, a fallback znowu się
                nie uruchamia. (b) Środowisko, w którym NumberFormatter pochodzi z
                polyfilla nieudostępniającego klasy Locale → fatal error 'Class
                "Locale" not found' i przerwanie całego Działu 7 zamiast zejścia na
                tor zapasowy.
        naprawa: Porównywać ACTUAL_LOCALE (lub obie wartości naraz), osłonić dostęp warunkiem class_exists('Locale') i dodać test formatowania uruchamiany na środowisku z intl, żeby ta gałąź przestała być martwa w testach.

  [1.25] Status 'draft' jest zaszyty na sztywno w dwóch miejscach (ładunek zdarzenia i odpowiedź JSON) — nie jest odczytywany ze stanu faktycznie zapisanego przez Dział 10. Raportowany status to deklaracja, nie odzwierciedlenie tego, co trafiło do bazy.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-11.php:85
        status: prawdopodobne
        skutek: Generowanie wersji 2 oferty, której rekord ma w bazie inny status
                niż 'draft': plugin 3 dostaje zdarzenie ze statusem 'draft' sprzeczne
                ze stanem BD i cofa sprawę sprzedażową do etapu roboczego, a
                użytkownik widzi w odpowiedzi status niezgodny z listą ofert.
        naprawa: Przekazywać status z kontekstu ustawionego przez Dział 10 (rzeczywiście zapisana wartość), np. (string) $context->get( 'status', 'draft' ), i użyć tej samej zmiennej w zdarzeniu oraz w odpowiedzi.

  [1.25] Nie da się z dossier potwierdzić, że wartość zwracana przez MP_Lead_Intake_DB::insert_activity() jest identyfikatorem wiersza, a nie liczbą wstawionych wierszy — a cały dział (log_id, K8.3, QA8) opiera się na tym założeniu.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-08.php:98
        status: prawdopodobne
        skutek: Jeżeli insert_activity() zwraca bezpośrednio wynik wpdb::insert(),
                to log_id zawsze wynosi 1 przy udanym zapisie. Kontrole K8.3 i QA8
                (log_id > 0) nadal przechodzą, ale do kontekstu i dalszych działów
                trafia identyfikator wskazujący na cudzy wiersz logu — odwołania
                do wpisu historii prowadzą do złego rekordu. Weryfikacja wymaga
                wglądu w MP_Lead_Intake_DB::insert_activity(), którego dossier nie
                zawiera; zgłoszone jako obserwacja do sprawdzenia, nie jako
                potwierdzony błąd.
        naprawa: Potwierdzić, że insert_activity() zwraca `$wpdb->insert_id` po udanym INSERT i false/0 przy błędzie; jeśli zwraca wynik wpdb::insert(), zmienić na odczyt insert_id.

  [1.25] A1.3 dla zdarzenia bez poprawnego klucza zwraca `event_id` jako pusty string, a nie NULL — a idempotencja opiera się wg dokumentacji na indeksie UNIQUE, który wielokrotne wartości dopuszcza wyłącznie dla NULL-i.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-01.php
        status: prawdopodobne
        skutek: Jeżeli K1.3 przepuszcza pusty `event_id`, a warstwa zapisu wstawia go
                dosłownie: dwa NIEZWIĄZANE ze sobą zdarzenia bez klucza trafiają
                do tabeli z tą samą wartością `''`. Pierwsze przechodzi, drugie
                łamie UNIQUE i zostaje odrzucone jako duplikat, mimo że dotyczy
                innego procesu — zdarzenie ginie z komunikatem o powtórzeniu.
        naprawa: Domknąć to jednoznacznie: albo K1.3 odrzuca pusty `event_id` kodem błędu, albo warstwa zapisu wstawia dla niego literalny NULL (a nie `''`), zgodnie z cytowanym zachowaniem UNIQUE. Wymaga potwierdzenia w K1.3 i w includes/db/class-mp-sales-workflow-db.php.

  [1.25] Tozsamosc aktora uzyta w tescie wlasnosci procesu pochodzi z ladunku zdarzenia, a nie ze snapshotu, z ktorego pochodza uprawnienia — dossier nie zawiera Dzialu 1/2, wiec zgodnosc obu zrodel nie jest w tym pliku niczym zagwarantowana.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-03.php:107
        status: prawdopodobne
        skutek: Jesli ktorykolwiek tor wejscia pozwala ustawic `event.actor.user_id`
                na wartosc inna niz uzytkownik, dla ktorego Dzial 2 zbudowal snapshot
                uprawnien, to podanie identyfikatora wlasciciela procesu czyni warunek
                `$owner !== $user_id` falszywym, in_reach() nie jest wolane wcale i
                handlowiec bez `view_team` przechodzi K3.1 na cudzym procesie (dostaje
                'capability_granted' zamiast 404). Dossier nie obejmuje Dzialu 1/2,
                wiec nie da sie z niego rozstrzygnac, czy pole jest przypinane do
                biezacego uzytkownika — to jest ustalenie do potwierdzenia, nie
                potwierdzony blad.
        naprawa: Brac identyfikator aktora z tego samego snapshotu, co uprawnienia (np. `$from_db['user_id']`), albo dodac w A3.1 asercje zgodnosci obu wartosci i odmawiac z odrebnym powodem, gdy sie roznia.

  [1.25] Deklarowany zakres Agenta 3.1 jest szerszy niż to, co kod faktycznie sprawdza. Nazwa i opis agenta obiecują trzy kontrole ("produkt, konkretny wariant, status opublikowany"), a implementacja sprawdza istnienie klucza w snapshocie i jedną flagę `purchasable`. Czy ta flaga faktycznie pokrywa wariant i status publikacji, wynika wyłącznie z Działu 2, którego w dossier nie ma.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-03.php:50
        status: prawdopodobne
        skutek: Jeżeli Dział 2 wylicza `purchasable` bez uwzględnienia post_status
                i bez rozstrzygnięcia wariantu (np. z samego
                `WC_Product::is_purchasable()` na produkcie nadrzędnym), to pozycja
                wskazująca produkt w statusie draft albo produkt wariantowy bez
                wybranej wariacji przejdzie Agenta 3.1 i trafi do kalkulacji jako
                pozycja zwalidowana. Weryfikacja wymaga wglądu w
                class-mp-ob-department-02.php, którego dossier nie zawiera —
                ustalenie jest wskazaniem miejsca do sprawdzenia, nie potwierdzonym
                błędem.
        naprawa: Sprawdzić w Dziale 2, jakie warunki wchodzą do `purchasable`. Jeśli status publikacji i rozstrzygnięcie wariantu nie są w niej zawarte, dodać w Agencie 3.1 osobne kontrole odpowiednich pól snapshotu (np. `status`, `variation_id`) z osobnymi komunikatami; jeśli są — zawęzić opis agenta do tego, co realnie sprawdza, albo odnotować w docblocku, które pole snapshotu je agreguje.

  [1.25] Deklarowana operacja działu „rotacja po ostatnim przypisaniu" (A4.2) jest w kodzie kryterium drugorzędnym — pierwszeństwo ma bieżące obciążenie, więc rotacja nie zachodzi tak, jak opisuje dokumentacja działu.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-04.php:120
        status: prawdopodobne
        skutek: Handlowiec A ma 3 otwarte procesy i nie dostał leada od miesiąca, B
                ma 2 otwarte i dostał leada minutę temu. Każdy nowy lead z tego
                samego kręgu trafia do B aż do wyrównania liczby otwartych —
                rotacja „po ostatnim przypisaniu" praktycznie nie działa dla
                handlowców o różnym obciążeniu, co rozjeżdża się z tym, co
                dział deklaruje i co weryfikuje uzasadnienie A4.3.
        naprawa: Uzgodnić dokumentację z kodem albo kod z dokumentacją: albo dopisać w docblocku i pliku dokumentacji, że kryterium nadrzędnym jest obciążenie, a rotacja rozstrzyga remisy, albo odwrócić kolejność kryteriów w komparatorze `pick()`.

  [1.25] Dokumentacja dzialu 4 mowi, ze kod kraju jest uzywany 'pozniej' przy weryfikacji VIES w dziale 3, czyli w dziale o nizszym numerze niz ten, ktory ten kod ustala.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-04.php:38
        status: prawdopodobne
        skutek: Jesli pipeline uruchamia dzialy rosnaco (1..N), dzial 3 wykonuje
                zapytanie VIES zanim agent 4.1 cokolwiek ustawi — czyta wtedy
                puste/nieznormalizowane country i weryfikuje VAT dla zlego lub pustego
                kraju, a dzial 4 dopiero potem raportuje country='PL'. Skutek: wynik
                weryfikacji VAT nie odpowiada krajowi zapisanemu przy leadzie.
        naprawa: Sprawdzic faktyczna kolejnosc rejestracji dzialow w pipeline. Jesli dzial 3 poprzedza dzial 4, przeniesc ustalanie kraju przed weryfikacje VIES (albo do dzialu 3) i poprawic zapis w dokumentacji; jesli kolejnosc jest inna niz numeracja, opisac to wprost w docs/dzial-04.

  [1.25] Rate limit oparty wyłącznie o REMOTE_ADDR — za reverse proxy/CDN licznik zlicza cały ruch witryny jako jednego klienta. Ryzyko już opisane w kodzie jako SHOULD FIX; zgłaszam jako potwierdzenie, nie jako nowe ustalenie.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-05.php:118
        status: prawdopodobne
        skutek: Witryna zostaje postawiona za Cloudflare. REMOTE_ADDR to adres proxy,
                wspólny dla wszystkich; po 5 zgłoszeniach z dowolnych IP w oknie
                każdy kolejny formularz od dowolnego użytkownika kończy się fail
                'security_failed'. Dodatkowo przy braku REMOTE_ADDR (CLI/cron)
                wszystkie przebiegi dzielą wspólny klucz 'unknown'.
        naprawa: Przy wdrożeniu za proxy przełączyć client_ip() na zaufany nagłówek proxy (np. CF-Connecting-IP) walidowany listą zaufanych adresów proxy — zgodnie z notatką w kodzie, nigdy na gołe X-Forwarded-For.

  [1.25] Wartość zwracana przez wpdb::insert() to liczba wstawionych wierszy, nie identyfikator wiersza — a dział 9 propaguje ją dalej pod nazwą process_log_id. Jeżeli MP_Lead_Intake_DB::insert_activity() zwraca wynik wpdb::insert() zamiast $wpdb->insert_id, process_log_id jest fałszywym identyfikatorem.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-09.php:109
        status: prawdopodobne
        skutek: Jeśli insert_activity() przekazuje surowy wynik wpdb::insert(), to
                przy każdym udanym zapisie $log_id = 1, więc process_log_id w
                kontekście zawsze wynosi 1. Kolejny dział lub raport, który po tym
                ID odczyta wiersz z wp_mp_activity_log, dostanie stale ten sam,
                przypadkowy wpis (pierwszy w tabeli) zamiast właśnie zapisanego
                process_started. Kontrola `if ( ! $log_id )` tego nie wykryje, bo 1
                jest wartością prawdziwą.
        naprawa: Zweryfikować, co zwraca MP_Lead_Intake_DB::insert_activity(); jeśli jest to wynik wpdb::insert(), zmienić wrapper tak, by po udanym zapisie zwracał (int) $wpdb->insert_id, a przy błędzie false.

  [1.25] Bramka jakości deklaruje warunek „odtwarzalność-rabatu”, ale QA Agent 5 sprawdza wyłącznie obecność trzech kluczy — nie weryfikuje żadnej zależności między nimi.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-05.php:165
        status: prawdopodobne
        skutek: Jeśli suma `amount_grosze` we wpisach `discounts` przestanie
                odpowiadać `discount_total` (np. po dodaniu drugiego wpisu rabatowego
                w kolejnej wersji działu), QA5 nadal zwróci `discount_complete =>
                true`, bramka zaakceptuje dział, a oferta pójdzie dalej z kwotą
                rabatu niezgodną z wykazem reguł na dokumencie.
        naprawa: Dołożyć w QA5 realne sprawdzenie: suma `amount_grosze` po `discounts` === `discount_total`, `rules_version` === `MP_OB_D5_Agent_Discount_Rules::RULES_VERSION`, oraz że każdy `rule_id` istnieje w słowniku.

  [1.25] Przekroczenie 999 999 ofert w roku daje kandydata dłuższego niż format i trwały STOP działu, bez ścieżki wyjścia.
        mp-offer-builder/includes/pipeline/departments/class-mp-ob-department-08.php:65
        status: prawdopodobne
        skutek: `last_number = 'OF/2026/999999'` → `next_seq = 1000000` → kandydat
                'OF/2026/1000000' → Krytyk 8.1 zwraca 'invalid_candidate_number'. Od
                tego momentu każda nowa oferta w tym roku pada w Dziale 8; korekty
                (własna gałąź) też, bo Agent 8.1 nadal jest pierwszy w parach i
                sam kandydat jest poprawnie sparsowany, ale odrzucany przez krytyka.
        naprawa: Wykryć wyczerpanie zakresu jawnie w Agencie 8.1 (`$next_seq > 999999` → fail z kodem 'number_range_exhausted' i czytelnym komunikatem), zamiast pozwalać, żeby objawiło się jako błąd formatu w krytyku.

  [1.26] Surowe komunikaty techniczne z warstwy sieciowej (WP_Error z wp_remote_get) i kody błędów VIES trafiają do danych wyniku pod kluczem 'vat_error', bez sanityzacji ani oznaczenia jako wewnętrzne.
        mp-lead-intake/includes/pipeline/departments/class-mp-department-03.php:155
        status: prawdopodobne
        skutek: Przy awarii łącza wartość 'vat_error' przyjmuje treść w rodzaju
                'cURL error 7: Failed to connect to proxy.wewn.firma.local port 3128'
                albo 'MS_UNAVAILABLE'. Jeśli którakolwiek warstwa prezentacji
                (podsumowanie leada dla pracownika, mail, komunikat zwrotny w
                formularzu) wypisuje pola błędów z wyniku, człowiek zobaczy
                wewnętrzną nazwę hosta proxy lub nieczytelny kod zamiast
                informacji, co dalej. Dossier nie zawiera warstwy prezentacji, więc
                sam fakt wyświetlenia nie jest tu udowodniony — do sprawdzenia w
                miejscu renderowania.
        naprawa: Rozdzielić pola: techniczny szczegół zapisywać pod kluczem wyłącznie logowym (np. 'vat_error_debug', filtrowanym przed prezentacją), a do warstwy widocznej dla człowieka przekazywać stałą, zrozumiałą treść typu 'Nie udaliśmy się połączyć z VIES — weryfikacja VAT zostanie powtórzona automatycznie'.

  [1.26] Blok konfiguracji podany administratorowi jako gotowa recepta zawiera zaszytą na sztywno ścieżkę `/uploads/mp-offer-builder-private/`, niezależną od faktycznej ścieżki uploadów danej instalacji.
        mp-offer-builder/mp-offer-builder.php:131
        status: prawdopodobne
        skutek: Instalacja z przedefiniowanym katalogiem uploadów (stała UPLOADS,
                filtr upload_dir, starsza multisite mapująca pliki pod `/files/`) —
                URI plików PDF nie zawiera segmentu `/uploads/`, więc regex
                `location` nigdy nie dopasowuje. Administrator wkleja blok,
                ostrzeżenie zapewnia go, że 'to ustawienie domyka obronę w głąb',
                a katalog pozostaje bezpośrednio dostępny po URL-u.
        naprawa: Wygenerować ścieżkę w komunikacie dynamicznie z wp_upload_dir() (podkatalog używany przez MP_Offer_Builder_Storage) zamiast wpisywać ją na stałe, i dopisać w komunikacie zalecenie zweryfikowania blokady jednym żądaniem do przykładowego URL-a.

  [1.26] Pole 'blocked' w podsumowaniu przebiegu ma dwa rozne znaczenia — raz to liczba WSZYSTKICH oczekujacych wierszy, raz liczba wierszy z jednej paczki.
        mp-sales-workflow/includes/class-mp-sw-queue.php:74
        status: prawdopodobne
        skutek: Osoba czytajaca raport przebiegu crona widzi 'blocked: 12'. Nie da sie
                z tej liczby odczytac, czy bezpiecznik zatrzymal cala kolejke (wtedy
                12 = calosc oczekujacych, w praktyce mogloby byc 4000), czy tylko 12
                sztuk z jednej paczki przy dzialajacej reszcie wysylki. Przy pelnym
                zatrzymaniu male 'blocked' uspokaja, mimo ze sklep nie wysyla nic.
                Uwaga: skutek zalezy od tego, czy wywolujacy w ogole pokazuje to
                podsumowanie czlowiekowi — dossier nie zawiera kodu wywolujacego
                run().
        naprawa: Rozdzielic klucze: 'blocked' liczone konsekwentnie na wierszach przetworzonych w tym przebiegu plus osobne 'halted' => true/false i 'pending' => self::pending(); uzupelnic docblock o wszystkie zwracane klucze.

  [1.26] Odbiorca z PUSTYM adresem e-mail trafia do listy odbiorcow bez zadnego sygnalu — find_salesman() zwraca `email => ''` jako normalny wynik, a run() wstawia go do wiersza kolejki tak samo jak adres poprawny.
        mp-sales-workflow/includes/pipeline/departments/class-mp-sw-department-07.php:174
        status: prawdopodobne
        skutek: Handlowiec, ktory prowadzi proces, zostaje usuniety z WP albo wypada
                ze snapshotu Dzialu 2 (obie listy: complete i incomplete). Zdarzenie
                przypisania/zmiany statusu kolejkuje powiadomienie z pustym `email`.
                Wysylka po COMMIT nie ma dokad pojsc — nikt nie dostaje informacji o
                przypisaniu ani o wypadnietym follow-upie, a przebieg konczy sie
                sukcesem, wiec zaden czlowiek nie widzi, ze powiadomienie przepadlo.
        naprawa: Odrzucac odbiorcow z pustym `email` juz w agencie i zglaszac to jako FAIL dzialu (albo wyraznie oznaczony skip z powodem), zamiast kolejkowac wiersz z pustym adresem; jesli taki krytyk istnieje w dalszej, urwanej czesci pliku — potwierdzic to i zamknac ustalenie.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-lead-intake/tests/index.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-lead-intake/tests/process-harness/bench.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-lead-intake/tests/process-harness/index.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-lead-intake/tests/process-harness/run-process.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-lead-intake/tests/process-harness/wp-stubs.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-offer-builder/tests/process-harness/run-process.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-offer-builder/tests/process-harness/wp-stubs.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

  [2.7] Zadna para nie otworzyla tego pliku.
        mp-sales-workflow/tests/security/index.php
        status: potwierdzone
        skutek: Brak ustalen dla tego pliku NIE oznacza, ze jest poprawny — oznacza,
                ze nikt do niego nie zajrzal. Raport bez tej informacji wprowadzalby w
                blad.

--- rozliczenie przebiegu ---
glebokosc:         gleboki
odczytow plikow:   230
zapytan do modelu: 65
dzial 1:           7,373.2 s, par 26/26
dzial 2:           1,740.0 s, par 11/11
czas calkowity:    9,113.4 s

WERDYKT: NO GO — 9 ustalen krytycznych
